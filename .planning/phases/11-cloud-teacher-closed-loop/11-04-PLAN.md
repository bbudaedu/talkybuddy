---
phase: 11-cloud-teacher-closed-loop
plan: 04
type: execute
wave: 4
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - server/diagnose.py
  - web/teacher.html
  - tests/test_tcloud_e2e.py
  - docs/TCLOUD_VERIFY.md
autonomous: false
requirements: [TCLOUD-01, TCLOUD-02]

must_haves:
  truths:
    - "診斷結果自帶來源標記，儀表板可當場區分「真的來自 Bedrock」與「規則式 mock 降級」（SC4 的「非 mock」唯一可稽核依據）"
    - "離線視窗累積的 pending 紀錄，在插回網路後不需孩子再說話就轉成儀表板上的新診斷（SC2 + SC4 端到端）"
    - "network_mode 為 edge 時，診斷路徑零出境（allow_cloud 第三道閘門成立，Phase 9 不回歸）"
    - "consent 未授權時，孩子的對話文字不會被送往 Bedrock"
    - "送往 Bedrock 的 prompt 中，student_text 已經過 deidentify"
  artifacts:
    - server/diagnose.py
    - web/teacher.html
    - tests/test_tcloud_e2e.py
    - docs/TCLOUD_VERIFY.md
  key_links:
    - "diagnose.generate_diagnosis() → result['source']：cloud 分支成功才標 cloud，降級到 _rule_based_diagnosis 一律標 rule"
    - "store.add_diagnosis() → /api/diagnoses → web/teacher.html 診斷卡來源徽章"
    - "api_network_mode → opportunistic_sync() → generate_diagnosis() → add_diagnosis()：決賽橋段的完整鏈路"
  prohibitions:
    - "音訊資料不得出現在上傳 payload"
    - "consent 閘門之前不得有任何雲端呼叫"
    - "學生姓名不得進入上傳白名單"
    - "不得新增背景定時輪詢 timer"
    - "不得重寫 server/bedrock_converse.py 或 diagnose.py 的 converse 呼叫"
---

<objective>
證明「邊緣對話 → 教師洞察」這條敘事閉環在決賽現場真的會動，而且顯示的是**真實而非 mock** 的診斷。

Purpose: TCLOUD-02 的實作**已經存在**（`server/bedrock_converse.py`；`server/diagnose.py:625-661` 已走 `bedrock_converse.converse_text` / `resolve_config(role="diag")`），本 plan **不重寫它**。剩下的缺口是驗證：ROADMAP SC4 要求儀表板顯示「**真實（非 mock）**」的診斷，但 `generate_diagnosis()` 目前**不回傳任何來源標記**，而 Bedrock → relay → 規則式 mock 的降級鏈是**刻意靜默**的（`server/diagnose.py:676-683`）。因此現況下沒有任何人——包含評審與我們自己——能分辨儀表板上那張診斷卡是 Bedrock 產出還是本地規則式產出。這正是 NPU-03 所稱「淪為音箱」的同型風險：畫面照跑、看起來成功、實際上雲端層根本沒被走到。

補上來源標記是**驗證工具**，不是新功能：沒有它，SC4 的「非 mock」無法被稽核。
Output: 診斷的 `source` 欄位、儀表板來源徽章、端到端閘門測試、決賽彩排 runbook，以及一次真機 checkpoint。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cloud-teacher-closed-loop/11-CONTEXT.md
@.planning/phases/11-cloud-teacher-closed-loop/11-01-SUMMARY.md
@.planning/phases/11-cloud-teacher-closed-loop/11-02-SUMMARY.md
@.planning/phases/11-cloud-teacher-closed-loop/11-03-SUMMARY.md
</context>

## Artifacts this phase produces（本 plan 部分）

| 類型 | 符號 | 檔案 |
|---|---|---|
| dict key | `generate_diagnosis()` 回傳新增 `source`（值域 `"cloud"` \| `"rule"`） | `server/diagnose.py` |
| JS function | `renderDiagnosisSource()` | `web/teacher.html` |
| DOM id | `diagSrc` | `web/teacher.html` |
| test file | `tests/test_tcloud_e2e.py` | `tests/` |
| doc | `docs/TCLOUD_VERIFY.md` | `docs/` |

## ⚠ 旗標假設（specless edge probe 回報 unresolved，未靜默丟棄）

本 phase 無 SPEC.md，`## Edge Coverage` 與 `## Prohibitions` 兩段皆不存在，確定性 edge probe 對兩條需求皆回傳 `category: unclassified` / `status: unresolved` / `verification: null`（`coverage.unresolved = 2`）。依規則，`unresolved` 是**明示假設**，在此登錄、由本 plan 承接驗證，**不得自動消解**：

| Requirement | probe 結果 | 本 plan 承接的假設（需由 Task 4 checkpoint 確認或推翻） |
|---|---|---|
| TCLOUD-01 | `unclassified` / `unresolved` | 假設「機會式上傳成功」的判準是：離線視窗的 pending 筆數在 edge→cloud 切換後歸零，且該批紀錄確實成為新診斷的輸入。此判準由 11-01/11-02 的單元測試涵蓋，但**單機拓樸下沒有真正的跨機上傳**（見 11-02 拓樸決策），因此「上傳」在決賽現場的實際語意是本機升級 + Bedrock 出境。此語意差異必須在 SUMMARY 與 `docs/TCLOUD_VERIFY.md` 中誠實載明，不得以「已完成上傳」一語帶過。 |
| TCLOUD-02 | `unclassified` / `unresolved` | 假設「四維診斷經 direct converse 產出」的判準是：`/api/status` 的 `cloud_provider` 為 `bedrock`，**且**該次診斷的 `source` 為 `cloud`。單看前者不足——有憑證不代表該次呼叫成功，降級是靜默的。Task 1 補上的 `source` 欄位就是為了讓這條假設可被驗證。 |

## 刻意不做（避免範圍蔓延）

- **不重寫** `server/bedrock_converse.py` 或 `server/diagnose.py:625-661` 的 converse 呼叫——已交付，見 11-CONTEXT.md `<domain>` 表。
- **不強化** `deidentify()` 的語意層（中文人名／諧音規避）——CONTEXT.md `<deferred>` 已排除。
- **不改** `/api/sync` 接收端契約。
- **不擴** 多學生支援。

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 診斷來源標記 source: cloud | rule（SC4 的可稽核依據）</name>
  <files>server/diagnose.py, tests/test_tcloud_e2e.py</files>

  <read_first>
    - `server/diagnose.py:646-695` — `generate_diagnosis()` 全段：`allow_cloud` 與 `consent_granted()` 兩道前置閘門、Bedrock → relay → 規則式的降級鏈、`result is None` 的兜底、以及末端 `companion_directive` / `level_state` 的收斂保底。**插入點在降級鏈結束、收斂保底開始之前**
    - `server/diagnose.py:468-482` — `_rule_based_diagnosis()` 回傳的鍵集合（確認目前無 `source` 鍵，不會撞名）
    - `server/diagnose.py:624-640` — `_call_bedrock_api()`：成功路徑的回傳形態
    - `server/agents/homework.py:13, 437, 542` — 本專案既有的 `"source": "cloud" | "rule"` provenance 慣例，**值域與鍵名要逐字沿用**，不要另創 `provenance` / `origin` 之類的新詞
    - `server/store.py:268-291` — `add_diagnosis()` / `list_diagnoses()`：payload 整包 `json.dumps`，新增鍵不需 schema 遷移
    - `tests/test_diagnose_network_gate.py`（全檔）— 既有的診斷閘門測試寫法，本任務的測試要與它並存不衝突
  </read_first>

  <behavior>
    - 無任何雲端憑證（`resolve_config` 皆回 None）→ 回傳的 `source` 為 `"rule"`
    - Bedrock 分支成功回傳 → `source` 為 `"cloud"`
    - Bedrock 分支拋例外、relay 也不可用 → `source` 為 `"rule"`（降級必須誠實標記，不得沿用 cloud）
    - `allow_cloud=False` → `source` 為 `"rule"`，且 Bedrock 與 relay 皆未被呼叫
    - `consent_granted()` 為 False → `source` 為 `"rule"`，且 Bedrock 與 relay 皆未被呼叫
    - `source` 值域恆為 `"cloud"` 或 `"rule"` 二者之一，不會是 None 或缺鍵
  </behavior>

  <action>
在 `server/diagnose.py::generate_diagnosis()` 內，於降級鏈結束後、`companion_directive` 收斂保底之前，依**實際走到哪一條路**設定 `result["source"]`。

判定規則（以既有的 `result` 變數狀態為準，不要另建旗標變數以外的機制）：
- 進入雲端分支且 `_call_bedrock_api()` 或 `_call_anthropic_api()` 其中之一成功回傳非 None 時 → `"cloud"`
- 其餘所有情形（未進雲端分支、雲端全數失敗、`result is None` 而落到 `_rule_based_diagnosis()`）→ `"rule"`

實作上最小的寫法是：在 `if result is None: result = _rule_based_diagnosis(...)` 這一行的**前後**分別決定值——雲端分支結束後若 `result` 非 None 即為 `"cloud"`，落入規則式兜底即為 `"rule"`。務必在**兩個**賦值點都設定，確保任何路徑都不會缺鍵。

鍵名與值域**逐字沿用** `server/agents/homework.py` 的既有慣例（`"source"`，值為 `"cloud"` 或 `"rule"`），讓儀表板的既有心智模型可以直接套用——該慣例已經渲染在 agent 產出卡上（見 `web/teacher.html` 的 agent 區塊說明文字）。

更新 `generate_diagnosis()` 的 docstring：說明新增的 `source` 鍵，並寫明它存在的理由——降級鏈是**刻意靜默**的（失敗不阻塞是 demo 韌性的既有設計），所以必須有一個欄位讓呼叫端與儀表板事後分辨這一份診斷究竟由誰產出；沒有它，「顯示真實非 mock 診斷」無法被稽核。

**不得**改動降級鏈本身的順序、逾時、例外處理或閘門位置——本任務只加一個標記欄位。**不得**因為要標記而把靜默降級改成拋例外：韌性優先是既有且正確的決策。

新建 `tests/test_tcloud_e2e.py`，檔頭 docstring 說明本檔驗 TCLOUD-01/02 的端到端閉環。先覆蓋以上六個 behavior。雲端分支以 `monkeypatch.setattr` 假造 `bedrock_converse.resolve_config` 與 `diagnose._call_bedrock_api`，不打真 AWS。
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tcloud_e2e.py tests/test_diagnose_network_gate.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "source" server/diagnose.py` 大於等於 2
    - `source` 在 `generate_diagnosis` 內被賦值於至少兩個位置：`.venv/bin/python -c "import ast,sys; src=open('server/diagnose.py').read(); fn=[n for n in ast.walk(ast.parse(src)) if isinstance(n,ast.FunctionDef) and n.name=='generate_diagnosis'][0]; seg=ast.get_source_segment(src,fn); sys.exit(0 if seg.count('[\"source\"]')+seg.count(\"['source']\") >= 2 else 1)"` 結束碼 0
    - 降級鏈未被改成拋例外：`.venv/bin/python -c "import ast,sys; src=open('server/diagnose.py').read(); fn=[n for n in ast.walk(ast.parse(src)) if isinstance(n,ast.FunctionDef) and n.name=='generate_diagnosis'][0]; seg=ast.get_source_segment(src,fn); sys.exit(0 if '_rule_based_diagnosis' in seg and 'raise' not in seg else 1)"` 結束碼 0
    - 無憑證時標記為 rule：`.venv/bin/python -c "
from server import diagnose
d = diagnose.generate_diagnosis([{'student_text':'hello','scores':{}}], None, allow_cloud=False)
assert d['source']=='rule', d.get('source')
print('OK')"` 印出 `OK`
    - 值域封閉：`.venv/bin/python -m pytest tests/test_tcloud_e2e.py -q -k "source"` 有測試命中且通過
    - 既有診斷測試不回歸：`.venv/bin/python -m pytest tests/ -q -k "diagnose"` 全數通過
  </acceptance_criteria>

  <done>`generate_diagnosis()` 回傳恆帶 `source`，值域為 `"cloud"` / `"rule"`；雲端成功才標 cloud、任何降級一律標 rule；降級鏈順序與靜默韌性未被改動；測試全綠。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 端到端閉環與三道出境閘門的自動化驗證</name>
  <files>tests/test_tcloud_e2e.py</files>

  <read_first>
    - `server/diagnose.py:530-541` — `_build_diagnosis_prompt()`：**這是決賽現場真正跨出裝置的那段**。注意 `student_text` 經 `guardrails.deidentify()`（第 535 行），而 `ai_response_text`（第 536 行）**沒有**
    - `server/diagnose.py:658-675` — `allow_cloud` 與 `consent_granted()` 兩道前置閘門的實際短路順序
    - `server/app.py:254-314` — `api_network_mode` 在 11-02 改動後的完整鏈路
    - `.planning/phases/11-cloud-teacher-closed-loop/11-02-SUMMARY.md` — `opportunistic_sync()` 的最終簽章與回傳鍵
    - `.planning/phases/11-cloud-teacher-closed-loop/11-01-SUMMARY.md` — `UPLOAD_FIELDS` 的最終欄位清單
    - `tests/test_agent_privacy.py:1-30` — **要照抄的驗證手法**：不 mock `deidentify`（用真的），只攔截送上雲的 prompt 字串，再對字串本身做斷言。這比「斷言 deidentify 被呼叫過」強得多
    - `tests/test_diagnose_network_gate.py` — 既有的 `allow_cloud` 閘門測試，避免重複覆蓋
  </read_first>

  <behavior>
    - 離線視窗鏈路：3 筆 pending（`network_mode="edge"` 期間寫入）→ 切 cloud → `pending_count()` 歸零，且 `list_diagnoses()` 多出一筆新診斷
    - `network_mode="edge"` 時呼叫診斷 → 攔截器記錄到的出境呼叫次數為 0
    - `config.CONSENT_GRANTED=False` 時呼叫診斷 → 出境呼叫次數為 0，且回傳 `source` 為 `rule`
    - 攔截送往 Bedrock 的 prompt 字串 → 其中不含互動原文裡的電話數字（用真的 `deidentify`，斷言字串本身）
    - 攔截送往 Bedrock 的 prompt 字串 → 其中不含 `store.student_display_name()` 的值（姓名不得隨診斷 prompt 出境）
  </behavior>

  <action>
在 `tests/test_tcloud_e2e.py` 補上五組端到端測試，全部離線執行、不打真 AWS。

**攔截手法**（照抄 `tests/test_agent_privacy.py` 的形態）：`monkeypatch.setattr` 掉 `server.bedrock_converse.converse_text`，換成一個把收到的 system + prompt 字串存進 list、並回傳一段合法診斷 JSON 的假函式。同時 `monkeypatch.setattr` `bedrock_converse.resolve_config` 讓它回傳一個假 cfg，使雲端分支得以進入。**不要** monkeypatch `guardrails.deidentify`——要驗的就是它在真實資料上的效果。

**離線視窗鏈路測試**：以 `store.add_interaction()` 寫入 3 筆 `synced=False` 的紀錄模擬離線視窗（欄位比照 `server/pipeline.py:303-316` 實際寫入的形狀），再用 FastAPI TestClient 帶有效 token POST `/api/network_mode` 切 `cloud`，斷言回應的 `synced` 為 3、`store.pending_count()` 為 0、且 `store.list_diagnoses()` 比呼叫前多一筆。這條測試就是決賽橋段「插回網路 → 不必等孩子再講話 → 儀表板出現新診斷」的自動化版本。

**兩道閘門測試**：分別以 `allow_cloud=False` 與 `monkeypatch.setattr(config, "CONSENT_GRANTED", False)` 呼叫 `generate_diagnosis()`，斷言攔截 list 長度為 0。斷言「沒有出境」必須用攔截器計數，**不可**只斷言回傳值——回傳值在降級後與雲端失敗時長得一樣，區分不出「沒送」與「送了但失敗」。

**prompt 內容測試**：互動的 `student_text` 放一段含電話號碼的文字，跑完後對攔截到的 prompt 字串斷言原始數字串不在其中。姓名測試則把 `store.student_display_name()` 的回傳值拿來斷言不在 prompt 字串中。

**發現但不在本 phase 修的事，要寫進測試檔的 docstring 而非默默略過**：`server/diagnose.py:536` 的 `ai_response_text` **未經** `deidentify()`，因此 AI 學伴的回覆若覆述了孩子的名字（例如回應「Hi Mimi!」），該名字會原文進入 Bedrock prompt。這是既有實作的邊界、不是本 phase 引入的退步，且 CONTEXT.md `<deferred>` 已把 `deidentify` 語意層強化排除在外。請在檔頭 docstring 明確記錄此殘留風險，並在 SUMMARY 中要求登錄為新的 Known-Gap（見 Task 3）。**不要**順手修它——那會改動 11-CONTEXT.md 未授權的範圍。
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_tcloud_e2e.py -q</automated>
  </verify>

  <acceptance_criteria>
    - 測試檔以真實 `deidentify` 驗證（未被 mock 掉）：`grep -c "monkeypatch.setattr.*deidentify" tests/test_tcloud_e2e.py` 回傳 0
    - 攔截器手法到位：`grep -c "converse_text" tests/test_tcloud_e2e.py` 大於等於 1
    - 五組行為皆有測試：`grep -c "^def test_" tests/test_tcloud_e2e.py` 大於等於 5
    - 殘留風險已記錄：`grep -c "ai_response_text" tests/test_tcloud_e2e.py` 大於等於 1
    - `.venv/bin/python -m pytest tests/test_tcloud_e2e.py -q` 全數通過
    - 全專案無回歸：`.venv/bin/python -m pytest tests/ -q` 相對本 plan 開工前無新增失敗
  </acceptance_criteria>

  <done>五組端到端測試存在且通過：離線鏈路歸零並產生新診斷、兩道閘門的出境呼叫計數為 0、prompt 字串不含電話數字與學生姓名；`ai_response_text` 的殘留風險已寫進 docstring。</done>
</task>

<task type="auto">
  <name>Task 3: 儀表板來源徽章與決賽彩排 runbook</name>
  <files>web/teacher.html, docs/TCLOUD_VERIFY.md</files>

  <read_first>
    - `web/teacher.html:230-233` — 診斷卡區塊，其中 `.diag-src` 那一行目前是**硬編**的「由 Hermes Agent（持久記憶）＋ AWS Bedrock（Claude）產出」，與 `:196` 的硬編姓名是同一類問題：它無條件宣稱走了 Bedrock，即使該次是規則式降級
    - `web/teacher.html` 中 agent 產出區塊的 `source` 徽章渲染寫法（搜尋 `agentBox` 的 render 函式）— 既有的 `cloud` / `rule` 徽章樣式，本任務要沿用同一套視覺語彙
    - `web/teacher.html:284` — `esc()` 轉義 helper
    - `web/teacher.html:577` 起的 `renderAll()` — 診斷卡渲染的呼叫位置
    - `.planning/phases/11-cloud-teacher-closed-loop/11-03-SUMMARY.md` — 11-03 對 `renderAll()` 與 `state` 的實際改動，避免衝突
    - `docs/RELAY_VERIFY.md`（全檔）— **要照抄的文件體例**：目的、「驗得到／驗不到」對照表、環境變數、逐步驟指令、預期輸出。本任務的 runbook 沿用同一結構
    - `server/app.py:165-180` — `/api/status` 的 `cloud_provider` 欄位（runbook 的現場佐證指令要用它）
  </read_first>

  <action>
**1. 儀表板來源徽章**（`web/teacher.html`）：

把 `.diag-src` 那一行的硬編宣稱改為依最新診斷的 `source` 欄位動態渲染。給該元素加上 `id="diagSrc"`，新增 `renderDiagnosisSource()` 並在 `renderAll()` 的診斷卡段落中呼叫。

文案規則（繁體中文，值必須誠實反映實際來源）：
- `source` 為 `"cloud"` → 顯示「由 AWS Bedrock（Claude）direct converse 產出」
- `source` 為 `"rule"` → 顯示「離線規則式產出（未走雲端）」
- 缺鍵或無診斷 → 顯示「來源未知」

視覺沿用 agent 產出區塊既有的 `cloud` / `rule` 徽章樣式，不要另創新 CSS class。所有值經 `esc()` 或 `textContent` 進 DOM。

**不得**保留任何無條件宣稱走了 Bedrock 的文字——那正是這個 phase 要消滅的東西：畫面宣稱雲端、實際降級，沒人分辨得出來。**不得**新增 `setInterval`（11-03 已把 timer 數釘死為 1）。

**2. 決賽彩排 runbook**（新建 `docs/TCLOUD_VERIFY.md`）：

比照 `docs/RELAY_VERIFY.md` 的體例撰寫，至少含以下段落：

- **目的**：把「邊緣對話 → 教師洞察」閉環在真機上跑過一次，並留下可稽核證據。
- **驗得到／驗不到對照表**：明確列出本 phase 驗得到（consent 閘門、白名單、去識別化在真資料上生效、edge→cloud 觸發、Bedrock 真實產出、儀表板真姓名）與**驗不到**（跨機同步壓測、`deidentify` 對中文人名的遮罩、`ai_response_text` 的去識別化、多學生名冊）。誠實記錄限制是本文件的核心價值。
- **單機拓樸的誠實說明**：照 11-02 拓樸決策的結論寫明——決賽現場「上傳」的實際語意是本機 pending 升級 + Bedrock 出境，`push_pending()` 的跨機 HTTP 路徑由單元測試涵蓋但現場不會走到。不得以「已完成上傳」一語帶過。
- **環境變數**：列出 `TALKYBUDDY_CLOUD_PROVIDER`、`TALKYBUDDY_CONSENT_GRANTED` 及 AWS 憑證相關變數（**只寫變數名與用途，絕不寫入任何實際金鑰值**）。
- **逐步驟彩排腳本**：對應 Task 4 checkpoint 的五個步驟，每步附可貼上的指令與預期輸出。
- **現場佐證指令**：`curl` 打 `/api/status` 觀察 `cloud_provider` 欄位、以及打 `/api/diagnoses` 觀察最新一筆的 `source` 欄位——兩者合起來才構成「大腦真的在 Bedrock」的證據（單看 `cloud_provider` 不足，理由見本 plan 上方旗標假設表）。
- **失敗時的降級劇本**：雲端不可用時現場要怎麼講、要切到哪個畫面。決賽只有一次機會，這段不能臨場才想。
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/ -q -k "teacher or student_identity or tcloud"</automated>
  </verify>

  <acceptance_criteria>
    - 硬編的 Bedrock 宣稱已消失：`grep -c '由 Hermes Agent（持久記憶）＋ AWS Bedrock（Claude）產出' web/teacher.html` 回傳 0
    - 徽章依資料渲染：`grep -c 'diagSrc' web/teacher.html` 大於等於 2（HTML + JS）
    - 三種來源文案皆在：`grep -c '離線規則式產出\|來源未知' web/teacher.html` 大於等於 2
    - timer 數量未變：`grep -c 'setInterval' web/teacher.html` 回傳 1
    - runbook 存在且含必要段落：`grep -c '驗不到' docs/TCLOUD_VERIFY.md` 大於等於 1 且 `grep -c 'cloud_provider' docs/TCLOUD_VERIFY.md` 大於等於 1 且 `grep -c 'source' docs/TCLOUD_VERIFY.md` 大於等於 1
    - runbook 未寫入任何實際金鑰：`grep -cE 'AKIA[0-9A-Z]{16}|sk-[A-Za-z0-9]{20}' docs/TCLOUD_VERIFY.md` 回傳 0
    - 單機拓樸誠實說明已載明：`grep -c '單機\|本機' docs/TCLOUD_VERIFY.md` 大於等於 1
  </acceptance_criteria>

  <done>診斷卡來源徽章依 `source` 動態渲染、無條件宣稱雲端的硬編文字消失、timer 數量不變；`docs/TCLOUD_VERIFY.md` 依 `RELAY_VERIFY.md` 體例完成且含「驗不到」與單機拓樸的誠實說明、零金鑰外洩。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 真機端到端彩排（決賽橋段）</name>

  <read_first>
    - `docs/TCLOUD_VERIFY.md` — Task 3 產出的彩排腳本，本 checkpoint 逐步照它執行
    - `edge/runtime/run_edge.sh` — 真機啟動指令與埠號
  </read_first>

  <action>
本任務**不寫任何程式碼**。執行者的工作是：確認 11-01 至 11-04 Task 1-3 全數完成且 `.venv/bin/python -m pytest tests/ -q` 全綠後，把下方 `<how-to-verify>` 的五個步驟原文呈交給使用者，並停下來等待回覆。

使用者回報後：
- 若回報 `source` 為 `cloud` → 記錄兩個實際值（`source`、`cloud_provider`）到 SUMMARY，標記 TCLOUD-02 驗證通過。
- 若回報 `source` 為 `rule` → **不得**標記為通過。依使用者回報的 `cloud_provider` 值與日誌，判斷是憑證、區域、模型 ID 或逾時問題，並把診斷結論寫進 SUMMARY 的待辦。
- 若「待同步」未歸零 → 檢查回應是否帶 `consent_required`，據以判斷是 consent 閘門攔下（預期行為）還是觸發未生效（缺陷）。
  </action>

  <what-built>
    - 11-01：`push_pending()` 的 consent 閘門、上傳白名單投影、上傳瞬間去識別化、部分失敗安全標記
    - 11-02：`network_mode` edge→cloud 轉換瞬間觸發 + 回合結束兜底觸發（無新增輪詢）
    - 11-03：教師儀表板學生姓名改由 `/api/student_profile` 讀取，硬編字串已移除
    - 11-04：診斷 `source` 來源標記、儀表板來源徽章、端到端閘門測試、`docs/TCLOUD_VERIFY.md`
  </what-built>

  <how-to-verify>
    請依 `docs/TCLOUD_VERIFY.md` 的彩排腳本在 Genio 520 真機上執行，並記錄每一步的實際結果：

    1. **起始狀態**：啟動 `edge/runtime/run_edge.sh`，開教師儀表板。確認學生卡顯示的姓名與學生編號是從 API 來的真實值（非硬編），畫面不是 `–`。
    2. **拔網路**：以學生端畫面的按鈕切到 edge 模式（或實際拔掉乙太網路線）。與孩子完成 2–3 輪離線對話。確認回覆正常、無多秒靜默（Phase 9 不回歸）。
    3. **觀察佇列**：教師儀表板「待同步」數字應隨離線回合遞增。
    4. **插回網路**：切回 cloud 模式。**不要再讓孩子說話。** 觀察：
       - 「待同步」數字是否在 5 秒輪詢內歸零
       - 最新 AI 診斷卡是否出現新的一筆
       - 診斷卡的來源徽章顯示什麼——**這是本次彩排最關鍵的一格**
    5. **佐證**：依 runbook 的現場佐證指令，記錄 `/api/status` 的 `cloud_provider` 值與 `/api/diagnoses` 最新一筆的 `source` 值。

    **判定標準：**
    - `source` 為 `cloud` → SC4「真實（非 mock）診斷」成立，TCLOUD-02 驗證通過
    - `source` 為 `rule` → **不算通過**。這代表雲端層被靜默降級了。請回報 `cloud_provider` 的值與 `run_edge.sh` 的日誌尾段，據以判斷是憑證、區域、模型 ID 還是逾時問題。**不要**因為畫面上有診斷就當作成功——那正是本 plan 要防的失效模式。

    若步驟 4 的「待同步」未歸零，請一併回報 `POST /api/network_mode` 的回應內容（是否帶 `consent_required`）。
  </how-to-verify>

  <resume-signal>回報每一步的實際結果（特別是 `source` 與 `cloud_provider` 兩個值），並輸入「approved」或描述遇到的問題</resume-signal>

  <verify>
    <human-check>使用者已在 Genio 520 真機上完成五個步驟，並回報 `/api/diagnoses` 最新一筆的 `source` 值與 `/api/status` 的 `cloud_provider` 值</human-check>
  </verify>

  <acceptance_criteria>
    - 使用者回報中明確含 `source` 的實際值（`cloud` 或 `rule`），而非「看起來正常」這類主觀描述
    - 使用者回報中明確含 `cloud_provider` 的實際值（`bedrock` / `relay` / `none`）
    - 步驟 2 的離線對話回覆正常、無多秒靜默（Phase 9 不回歸）
    - 步驟 4 的「待同步」數字在 5 秒輪詢內歸零，或已回報 `POST /api/network_mode` 的回應內容供判斷
    - 步驟 1 的學生卡姓名為真實值而非佔位符 `–`
  </acceptance_criteria>

  <done>真機彩排五步驟皆已執行並回報實際值；`source == "cloud"` 時判定 TCLOUD-02 通過，`source == "rule"` 時已記錄診斷結論與待辦，兩種情形皆已寫入 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `server/diagnose.py` → AWS Bedrock | **決賽現場真正跨出裝置的邊界**：孩子的對話文字經診斷 prompt 出境 |
| 降級鏈 → 儀表板宣稱 | 認知邊界：畫面宣稱的來源必須等於實際來源，否則構成對評審的誤導 |
| runbook 文件 → git repo | 憑證外洩面：驗證文件常是金鑰被誤 commit 的地方 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-12 | Information Disclosure | `diagnose._build_diagnosis_prompt` | high | mitigate | Task 2 以攔截 prompt 字串的方式驗證三道閘門（`allow_cloud`、`consent_granted`、`deidentify`）在真實資料上成立；不 mock `deidentify`，斷言字串本身不含電話數字與學生姓名 |
| T-11-13 | Information Disclosure | `diagnose.py:536` 的 `ai_response_text` | medium | accept | **殘留風險，明確承認**：該欄位未經 `deidentify()`，AI 回覆若覆述孩子姓名則原文出境。屬既有實作邊界，CONTEXT.md `<deferred>` 已將 `deidentify` 語意層強化排除。處置：Task 2 寫入測試檔 docstring，並要求 SUMMARY 登錄為新 Known-Gap，供未來 milestone 收斂。**不在本 phase 修**（未授權範圍） |
| T-11-18 | Spoofing | 儀表板來源宣稱 | high | mitigate | Task 1 的 `source` 欄位 + Task 3 的動態徽章，移除無條件宣稱走 Bedrock 的硬編文字。降級時畫面必須誠實顯示「離線規則式產出」 |
| T-11-19 | Information Disclosure | `docs/TCLOUD_VERIFY.md` | high | mitigate | runbook 只寫環境變數**名稱**與用途，絕不寫入實際金鑰值；acceptance 以正規表達式掃 AKIA / sk- 前綴，命中即失敗 |
| T-11-20 | Repudiation | 彩排結果 | medium | mitigate | Task 4 checkpoint 要求逐步回報實際值（`source`、`cloud_provider`），並明訂 `source == "rule"` 不算通過——防止「畫面上有東西就當成功」的自我欺瞞 |
| T-11-SC | Tampering | 套件安裝 | n/a | accept | 本 plan 零新套件（純標準函式庫、vanilla JS、markdown），無 npm/pip/cargo 安裝步驟，故不觸發 Package Legitimacy Gate |
</threat_model>

<verification>
1. `.venv/bin/python -m pytest tests/test_tcloud_e2e.py -q` 全綠
2. `.venv/bin/python -m pytest tests/ -q` 相對開工前無新增失敗
3. `grep -c 'setInterval' web/teacher.html` 為 1
4. `docs/TCLOUD_VERIFY.md` 存在且無金鑰字串
5. Task 4 checkpoint 已回報 `source` 與 `cloud_provider` 的實際值並取得 approved
</verification>

<success_criteria>
- 五條 `must_haves.truths` 各有自動化測試或 checkpoint 回報覆蓋
- 診斷 `source` 值域封閉且降級誠實標記
- 儀表板無任何無條件宣稱走 Bedrock 的文字
- 真機彩排回報 `source == "cloud"`（否則視為 TCLOUD-02 未通過並回到除錯）
- 兩條旗標假設（TCLOUD-01 / TCLOUD-02 的 unresolved probe 項）已由 checkpoint 確認或明確推翻
</success_criteria>

<output>
Create `.planning/phases/11-cloud-teacher-closed-loop/11-04-SUMMARY.md` when done

SUMMARY 必須額外記錄兩件事：
1. 單機拓樸下「上傳」的實際語意（本機升級 + Bedrock 出境），不得以「已完成上傳」一語帶過
2. 新 Known-Gap：`server/diagnose.py:536` 的 `ai_response_text` 未經 `deidentify()`
</output>
