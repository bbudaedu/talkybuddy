---
phase: 08-cpu-only-offline-edge-turn-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - edge/runtime/audio_io.py
  - edge/runtime/local_client.py
  - tests/test_audio_io.py
autonomous: true
requirements: [ELOOP-01]
must_haves:
  truths:
    - "audio_io.capture_16k_mono_wav() 回傳的 bytes 帶 RIFF/WAVE header、取樣率 16000、單聲道、S16_LE，直接命中 server/pipeline.py 既有 RIFF-sniff fast path（不觸發 WavSpecMismatchError）"
    - "audio_io 預設走 arecord/aplay 子行程（零 pip 編譯風險）；sounddevice 僅在明確授權下作為升級路徑，import 失敗/缺 PortAudio 一律降級不炸"
    - "local_client.py 以 WebSocket client 連 ws://127.0.0.1:8787/ws/talk，送 binary WAV frame + {\"type\":\"audio_end\"}，收 {\"type\":\"tts_audio\",...} 後以 audio_io 播放——沿用既有 wire protocol，不改 server"
  artifacts:
    - edge/runtime/audio_io.py
    - edge/runtime/local_client.py
    - tests/test_audio_io.py
  key_links:
    - "audio_io.capture_16k_mono_wav() → 16k mono WAV bytes → local_client WS send → server/app.py::ws_talk → pipeline._webm_to_wav RIFF fast path（soundfile 直讀，零 ffmpeg）"
    - "server/app.py::ws_talk tts_audio 事件的 wav_b64 key → base64 decode → audio_io.play_wav_bytes → 喇叭"
  prohibitions:
    - "不得產生非 16k mono 的 WAV（會在 edge profile 觸發 WavSpecMismatchError，靜默偽成功風險）"
    - "不得引入 ffmpeg 轉檔（邊緣端刻意不裝 ffmpeg，見 provision_device.sh）"
    - "不得把 sounddevice 列為唯一擷取方案；不得在未經 blocking-human 套件核可下於裝置安裝 sounddevice（[SUS] 套件，見 threat_model T-08-SC）"
    - "不得改動 server/app.py 或 server/pipeline.py 的 wire protocol / 契約（local_client 是既有協定換一個講話的人）"
    - "不得做前端瀏覽器 loopback UI（Deferred，非本 phase 驗收必要）"
---

<objective>
新增裝置端音訊 I/O 與離線對話 client，讓 Genio 520 不靠瀏覽器也能收音、放音，跑通完整聽→想→說迴圈（D-04）：`edge/runtime/audio_io.py`（ALSA 擷取/播放，統一介面，arecord/aplay 子行程為主、sounddevice 為授權升級）＋`edge/runtime/local_client.py`（WebSocket client 迴圈，沿用既有 /ws/talk 協定）。

Purpose: ELOOP-01 要求「真機完整聽→想→說迴圈」，D-04 選定裝置端直接走 ALSA、擷取 16k mono WAV 直接命中 07-01 的 RIFF-sniff fast path，不透過瀏覽器 WebSocket。local_client 是既有 wire protocol 換一個 Python 客戶端（Pattern 1），server/app.py 的 routing/WS 狀態機完全不用改，是本 phase 風險最低的部分。音訊格式必須嚴格 16k mono，否則 edge profile 會 raise WavSpecMismatchError（不靜默偽成功原則）。

Output: edge/runtime/audio_io.py、edge/runtime/local_client.py、tests/test_audio_io.py。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cpu-only-offline-edge-turn-loop/08-CONTEXT.md
@.planning/phases/08-cpu-only-offline-edge-turn-loop/08-PATTERNS.md
@server/pipeline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: edge/runtime/audio_io.py — ALSA 擷取/播放統一介面（arecord/aplay 主、sounddevice 授權升級），附單元測試</name>
  <read_first>
    - server/pipeline.py（lines 59-105：_is_wav_riff 與 _webm_to_wav 的 RIFF-sniff fast path；命中條件 sample_rate==16000 and channels==1，否則 edge raise WavSpecMismatchError——audio_io 必須產出剛好命中的格式）
    - .planning/phases/08-cpu-only-offline-edge-turn-loop/08-PATTERNS.md（§edge/runtime/audio_io.py，lines 146-153：WAV byte 契約、try/except degrade idiom、arecord subprocess 參數）
    - .planning/phases/08-cpu-only-offline-edge-turn-loop/08-RESEARCH.md（Standard Stack Supporting 表 arecord/aplay 條目；Pitfall 5 lines 397-407：sounddevice aarch64 wheel 不保證，先試 subprocess）
    - edge/runtime/provision_device.sh（lines 21-23「不裝 ffmpeg」硬約束；已裝 numpy/soundfile）
    - server/cloud_llm.py（try/except Exception→None 降級 idiom 參考）
  </read_first>
  <behavior>
    - capture_16k_mono_wav(seconds) 回傳 bytes：前 12 bytes 為 RIFF/WAVE magic，soundfile 解析得 samplerate==16000、channels==1（可用真實錄一小段或組出 header 驗證格式；單元測試用 mock subprocess + 假 WAV bytes，不需真麥克風）
    - 當 sounddevice 不可用（ImportError/OSError）→ 自動降級呼叫 arecord 子行程；降級路徑不拋例外
    - arecord 子行程以固定 argv（非 shell 字串）呼叫：-f S16_LE -r 16000 -c 1，輸出檔讀回為 bytes
    - play_wav_bytes(wav) 把 WAV bytes 交給 aplay（或 sounddevice）播放；子行程失敗不拋、記 log
    - 單元測試 mock subprocess.run 驗證 argv 含 S16_LE/16000/1，並驗證回傳 bytes 命中 _is_wav_riff
  </behavior>
  <action>
    新增 edge/runtime/audio_io.py，提供統一介面：`capture_16k_mono_wav(seconds: float = 4.0) -> bytes`、`play_wav_bytes(wav: bytes) -> None`、以及一個觸發等待點 `wait_for_trigger() -> None`（本 phase 可為簡單 Enter/按鍵或固定秒數錄音佔位，VAD 觸發非驗收必要）。實作以 arecord/aplay 子行程為預設路徑（零 pip 編譯風險，符合裝置無 gcc/cmake 現況）：capture 用 `subprocess.run(["arecord", "-D", "default", "-f", "S16_LE", "-r", "16000", "-c", "1", "-d", str(int(seconds)), out_path], check=True)` 寫暫存檔再讀回 bytes（用固定 argv 串列，絕不用 shell=True 或字串插值，避免注入——見 threat_model T-08-03）；play 用 `subprocess.run(["aplay", wav_path])`。sounddevice 僅作為授權升級路徑：以 `try: import sounddevice` 包起來，ImportError/OSError（缺 PortAudio）即降級回 arecord/aplay（比照 EdgeLLM/CloudLLM 的 try/except Exception 降級 idiom，降級邊界不拋例外）。輸出格式必須嚴格 16k mono S16_LE WAV（帶 RIFF/WAVE header），使 server/pipeline.py 的 RIFF-sniff fast path 命中、不觸發 WavSpecMismatchError。新增 tests/test_audio_io.py：monkeypatch subprocess.run 驗證 arecord argv 含 "S16_LE"/"16000"/"1"、驗證 capture 回傳 bytes 通過 server.pipeline._is_wav_riff、驗證 sounddevice import 失敗時走 arecord 降級路徑（不拋）。測試不需真硬體/真麥克風。
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_audio_io.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_audio_io.py 全綠，涵蓋：arecord argv 含 S16_LE/16000/1、capture 回傳 bytes 命中 _is_wav_riff、sounddevice 不可用時降級 arecord 不拋例外
    - `grep -n "shell=True" edge/runtime/audio_io.py` 無輸出（子行程一律固定 argv 串列）
    - `grep -n "ffmpeg" edge/runtime/audio_io.py` 無輸出（不引入 ffmpeg）
    - capture_16k_mono_wav 產出格式為 16k mono S16_LE（單元測試以 _is_wav_riff + soundfile 讀 samplerate/channels 驗證）
  </acceptance_criteria>
  <done>audio_io 以 arecord/aplay 為主路徑產出命中 RIFF fast path 的 16k mono WAV，sounddevice 為安全降級升級路徑，單元測試綠。</done>
</task>

<task type="auto">
  <name>Task 2: edge/runtime/local_client.py — 離線對話 WebSocket client 迴圈</name>
  <read_first>
    - server/app.py（lines 344-487：ws_talk 協定——binary frame 累積、{"type":"audio_end"} 觸發整包處理、送回 asr_result/reply/tts_audio；line 404-406 tts_audio 的 wav_b64 key 為 base64 編碼 WAV）
    - .planning/phases/08-cpu-only-offline-edge-turn-loop/08-PATTERNS.md（§edge/runtime/local_client.py，lines 156-188：wire protocol analog、核心骨架、startup-ordering 約束、需先讀 app.py 確認 tts_audio payload key）
    - .planning/phases/08-cpu-only-offline-edge-turn-loop/08-RESEARCH.md（Pattern 1，lines 202-233：local_client 是 WS client 不是取代 server；health-check 就緒後才連）
    - edge/runtime/audio_io.py（Task 1 產出：capture_16k_mono_wav / play_wav_bytes / wait_for_trigger）
    - server/app.py（lines 354-362：/ws/talk 需 ?token= 驗證——local_client 需帶合法 token 或使用既有 dev/seed token 機制；實作時確認取得 token 的方式）
  </read_first>
  <acceptance_criteria>
    - local_client.py 定義 async run_loop()：連 ws://127.0.0.1:8787/ws/talk（host/port 可經 env 覆寫），送 binary WAV frame + {"type":"audio_end"}，收訊息迴圈中對 tts_audio 事件以 base64 decode wav_b64 後呼叫 audio_io.play_wav_bytes 播放
    - 使用 audio_io.capture_16k_mono_wav 取得上行 WAV，audio_io.play_wav_bytes 播放下行
    - tts_audio payload 的 key 名（wav_b64）以實際讀 server/app.py line 404-406 為準，非照抄骨架
    - /ws/talk 的 ?token= 驗證有被處理（帶合法 token 連線；取得方式於實作時對照 server/auth 與既有 seed 帳號機制）
    - 有 `if __name__ == "__main__"` 進入點，可經 `python3 -m edge.runtime.local_client` 啟動
    - `.venv/bin/python -c "import ast; ast.parse(open('edge/runtime/local_client.py').read())"` 通過（語法正確）；import 期不需真連線
  </acceptance_criteria>
  <action>
    新增 edge/runtime/local_client.py，以 websockets 套件（provision_device.sh 已裝）實作 async run_loop()：先確認 uvicorn health（沿用 curl-loop 或短逾時探測，不在 server 未就緒時硬連），取得 /ws/talk 合法 token（對照 server/auth 與既有 seed 帳號；實作時讀 server/app.py 確認 token 取得路徑），`websockets.connect(ws://127.0.0.1:8787/ws/talk?token=...)`。迴圈：`audio_io.wait_for_trigger()` → `wav = audio_io.capture_16k_mono_wav()` → `ws.send(wav)`（binary frame）→ `ws.send(json.dumps({"type":"audio_end"}))` → 接收訊息，對 `event["type"]=="tts_audio"` 以 `base64.b64decode(event["wav_b64"])`（key 名以實讀 app.py 為準）交 `audio_io.play_wav_bytes` 播放，收到回合結束訊號（如 reply 之後的 tts_audio/tts_unavailable）跳出內層迴圈進下一輪。host/port 走 env（TALKYBUDDY_EDGE_WS_HOST 預設 127.0.0.1、port 8787），連線目標為 loopback。加 `if __name__ == "__main__": asyncio.run(run_loop())`。此檔為 server-ready 後手動/腳本啟動的獨立行程，不阻塞、不改 server 端任何協定。
  </action>
  <verify>
    <automated>.venv/bin/python -c "import ast; ast.parse(open('edge/runtime/local_client.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>local_client.py 以既有 /ws/talk 協定送收音訊、經 audio_io 收放音，語法正確可作為獨立離線對話行程啟動（真機端到端跑通於 08-05 驗證）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 音訊子行程 argv | arecord/aplay 以固定 argv 呼叫；若用字串插值/shell=True 可能命令注入 |
| 裝置端 pip 安裝 | sounddevice 為 [SUS] 套件（查核工具無下載量資料，非真實可疑）；裝置無 gcc，wheel 不保證 |
| local_client → /ws/talk | loopback WS；/ws/talk 已有 ?token= 驗證，local_client 需帶合法 token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Tampering / Elevation of Privilege | audio_io 子行程呼叫 | medium | mitigate | arecord/aplay 一律以固定 argv 串列呼叫，絕不 shell=True 或字串插值；驗收含 `grep shell=True` 無輸出 |
| T-08-SC | Tampering (supply chain) | sounddevice pip 安裝 | high | mitigate | 預設路徑不安裝任何新套件（走 arecord/aplay）；若確需 sounddevice，須先過 08-05 的 blocking-human 套件核可（[SUS]，核可不可自動通過），並先在裝置實測 pip 是否觸發原始碼編譯（Pitfall 5） |
| T-08-06 | Spoofing | local_client 連 /ws/talk 未帶 token | low | mitigate | 連線帶合法 token（沿用 server/auth 既有機制）；目標僅 loopback 127.0.0.1 |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_audio_io.py -x -q`
- `.venv/bin/python -c "import ast; ast.parse(open('edge/runtime/local_client.py').read())"`
- `grep -n "shell=True\|ffmpeg" edge/runtime/audio_io.py`（預期無輸出）
- `.venv/bin/python -m pytest tests/ -x -q`（全套件無回歸）
</verification>

<success_criteria>
- audio_io 產出命中 RIFF fast path 的 16k mono WAV，arecord/aplay 為主、sounddevice 為安全降級升級路徑
- local_client 以既有 /ws/talk 協定收放音，不改 server 契約
- tests/test_audio_io.py 綠，local_client 語法正確
- 真機端到端跑通留待 08-05 硬體驗證
</success_criteria>

## Artifacts this phase produces（本 plan 產出符號）
- `edge/runtime/audio_io.py`（新檔）：`capture_16k_mono_wav(seconds=4.0) -> bytes`、`play_wav_bytes(wav: bytes) -> None`、`wait_for_trigger() -> None`
- `edge/runtime/local_client.py`（新檔）：`async run_loop()`、`__main__` 進入點
- `tests/test_audio_io.py`（新檔）
- 新 env var：`TALKYBUDDY_EDGE_WS_HOST`（local_client 連線目標，預設 127.0.0.1）

<output>
Create `.planning/phases/08-cpu-only-offline-edge-turn-loop/08-03-SUMMARY.md` when done
</output>
