---
phase: 08-cpu-only-offline-edge-turn-loop
plan: 04
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - edge/deploy/build.sh
  - edge/deploy/push.sh
  - edge/runtime/run_edge.sh
  - docs/DEPLOY_EDGE.md
autonomous: false
requirements: [ELOOP-02]
user_setup:
  - service: hti-g520-board
    why: "交叉編譯出的 llama-server native binary 必須在真機 Genio 520（Yocto，aarch64 glibc）上實際執行才能確認 glibc ABI 相容（Pitfall 1）；裝置經 SSH 可達（root@192.168.31.78，Tailscale-routed，見 edge/BOARD_BRINGUP_DECISION.md）"
    dashboard_config:
      - task: "確認開發機已裝 aarch64 交叉工具鏈（gcc-aarch64-linux-gnu / g++-aarch64-linux-gnu / cmake）；若 apt 路徑 glibc ABI 不相容，準備 ~/hackathon/ 的 Genio Yocto BSP SDK cross-toolchain（D-03 fallback）"
        location: "開發機 apt + ~/hackathon/ G520 SDK；真機 SSH root@192.168.31.78"
      - task: "確認 GGUF 模型檔（qwen2.5-1.5b-instruct-q4_k_m.gguf）存在於 repo models/ 目錄，供 push.sh 推送到裝置"
        location: "repo models/ 目錄"
must_haves:
  truths:
    - "edge/deploy/build.sh 交叉編譯出 aarch64 llama-server/llama-bench/llama-cli native binary，build flag 含 -march=armv8.2-a+dotprod+i8mm 且 GGML_NATIVE=OFF（D-02），並記錄所編譯 llama.cpp 的 git commit hash"
    - "edge/deploy/push.sh 除既有 server/、edge/runtime、web 外，另 rsync 交叉編譯產物（edge/deploy/bin/）與 GGUF 模型（models/*.gguf）到裝置對應路徑"
    - "edge/runtime/run_edge.sh 先以 python -m edge.runtime.run_llama_server 於背景拉起 llama-server，curl 迴圈等其 /health 就緒後才 exec uvicorn（llama-server 綁 127.0.0.1，uvicorn 既有 0.0.0.0 不動）"
    - "真機實測：交叉編譯的 llama-server 在 Genio 520 上 --version / ldd 成功（glibc ABI 相容，Pitfall 1），run_edge.sh 拉起 llama-server + uvicorn，/health 皆回 200——實際輸出經人工貼回，不靜默偽成功"
  artifacts:
    - edge/deploy/build.sh
    - edge/deploy/push.sh
    - edge/runtime/run_edge.sh
    - docs/DEPLOY_EDGE.md
  key_links:
    - "08-01 edge/runtime/run_llama_server.py（build_llama_server_argv + main os.execv）→ run_edge.sh 背景啟動 → llama-server 監聽 127.0.0.1:<port> → 08-02 EdgeLLM._call_llama_server 打得到：這是 llama-server 從『有 binary』到『真的在裝置上跑起來服務 EdgeLLM』的接線點（Blocker 4）"
    - "build.sh 交叉編譯 → edge/deploy/bin/llama-server → push.sh rsync → 裝置 TARGET_ROOT/edge/deploy/bin/llama-server → run_llama_server.py main() 相對定位解析：native binary 生成→部署→執行的完整鏈（ELOOP-02 核心，Blocker 3）"
    - "config.LLM_N_CTX / LLM_SERVER_PORT / LLM_THREADS（08-01）→ run_llama_server.py main() → run_edge.sh 啟動的 llama-server --ctx-size/--port/--threads：部署面承接 08-01 設定的搬遷終點"
  prohibitions:
    - "不得讓 run_edge.sh 啟動的 llama-server 綁對外可路由位址；host 一律走 run_llama_server.py 預設的 127.0.0.1（loopback），對外綁定驗證由 08-05 外部 curl 執行"
    - "不得使用 -march=armv8.7-a 或 GGML_NATIVE=ON（會編出 A78 不支援的 ISA，runtime SIGILL；D-02）"
    - "不得在 apt 交叉工具鏈 glibc ABI 失敗後於 apt 路徑反覆嘗試超過一次修正——依 D-03 立即切換 Yocto BSP SDK 工具鏈（build.sh 以 env 覆寫 CC/CXX，非新增第二條碼路）"
    - "不得使用來路不明的第三方預編譯 binary / fork；只從官方 ggml-org/llama.cpp clone，並記錄 commit hash（供應鏈完整性，T-08-02）"
    - "不靜默偽成功：真機 --version/ldd/health 未實際回應時，不得在 SUMMARY 標記成功；如實記錄失敗與 D-03 fallback 動作"
---

<objective>
把 ELOOP-02 的部署面補齊：在開發機交叉編譯 llama.cpp native binary（D-02 build flag），用既有 SSH/rsync 管線把 binary + GGUF 模型推上 Genio 520，並把 08-01 產出的 `run_llama_server.py` 接進 `run_edge.sh` 啟動序列（背景拉起 llama-server → 等 `/health` 就緒 → 再起 uvicorn），最後在真機實測 binary 能執行（glibc ABI 相容）且整條 server 迴路起得來。

Purpose: 08-01 只產出「組 llama-server 啟動 argv 的純函式 + launcher 骨架」，08-02 只把 EdgeLLM 改成打 llama-server 的 HTTP client——但在本 plan 之前，**裝置上根本沒有 llama-server binary、也沒有任何東西會去啟動它**（Blocker 3、Blocker 4）。沒有這個 plan，EdgeLLM.available() 在真機永遠回 False、pipeline 永遠只走 scaffold-only，ELOOP-02「native binary over localhost 真生成」無從成立。build.sh/push.sh 在 RESEARCH.md 明列為 MODIFIED，卻不在任何既有 plan 的 files_modified——本 plan 補上。

Output: 交叉編譯後的 build.sh、推送 binary+模型的 push.sh、接好 llama-server 啟動+health-gating 的 run_edge.sh、同步的 docs/DEPLOY_EDGE.md，以及一次真機 glibc ABI + 全 stack bring-up 的實測回報。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cpu-only-offline-edge-turn-loop/08-CONTEXT.md
@.planning/phases/08-cpu-only-offline-edge-turn-loop/08-PATTERNS.md
@edge/BOARD_BRINGUP_DECISION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: edge/deploy/build.sh — 交叉編譯 llama.cpp（-march=armv8.2-a+dotprod+i8mm，GGML_NATIVE=OFF）+ file/ldd 快篩</name>
  <read_first>
    - edge/deploy/build.sh（現況全檔 32 行：REPO_ROOT 相對定位、SRC_PATHS 檢查 for-loop、echo "  - X (OK)" / exit 1 錯誤慣例、lines 27-30 的 Phase 8 TODO 插入點）
    - .planning/phases/08-cpu-only-offline-edge-turn-loop/08-PATTERNS.md（§edge/deploy/build.sh，lines 192-209：延伸慣例、cmake 指令與 file/ldd 快篩來源行號）
    - .planning/phases/08-cpu-only-offline-edge-turn-loop/08-RESEARCH.md（Standard Stack Installation 區塊 lines 90-115：完整 cmake 交叉編譯指令；Pitfall 1 lines 318-339：glibc ABI 風險、file/ldd 快篩、D-03 失敗一次即切換；Version verification lines 116-117：記錄 git rev-parse HEAD）
    - .planning/phases/08-cpu-only-offline-edge-turn-loop/08-CONTEXT.md（D-02 鎖定 build flag、D-03 工具鏈取得與 fallback 規則）
  </read_first>
  <action>
    延伸 edge/deploy/build.sh（就地擴充，不重寫；保留既有 SRC_PATHS 檢查與 echo/exit 慣例）。把 lines 27-30 的 Phase 8 TODO 換成實際交叉編譯步驟：以環境變數參數化交叉工具鏈——`CROSS_CC="${TALKYBUDDY_CROSS_CC:-aarch64-linux-gnu-gcc}"`、`CROSS_CXX="${TALKYBUDDY_CROSS_CXX:-aarch64-linux-gnu-g++}"`（D-03：apt 泛用工具鏈為預設；Yocto BSP SDK fallback 時由呼叫端以這兩個 env 覆寫指向 SDK 工具鏈，不新增第二條碼路）。取得 llama.cpp 原始碼：若 repo 內 `third_party/llama.cpp`（或 env `TALKYBUDDY_LLAMACPP_SRC` 指定路徑）不存在則 `git clone https://github.com/ggml-org/llama.cpp.git`（只從官方 repo，供應鏈完整性）。以 08-RESEARCH.md lines 101-112 的 cmake 指令交叉編譯，關鍵旗標逐字沿用：`-DGGML_NATIVE=OFF`、`-DCMAKE_C_FLAGS` 與 `-DCMAKE_CXX_FLAGS` 皆為 `-march=armv8.2-a+dotprod+i8mm`（D-02，絕不用 armv8.7-a），`-DCMAKE_BUILD_TYPE=Release`，`--target llama-server llama-bench llama-cli`。把產物複製/連結到 `edge/deploy/bin/`（`llama-server`、`llama-bench`、`llama-cli`），使其與 08-01 `run_llama_server.py` main() 預設 binary 路徑（`edge/deploy/bin/llama-server`）一致。編譯後在開發機做快篩：`file edge/deploy/bin/llama-server` 確認為 aarch64 ELF（非 x86-64），沿用既有 echo "  - X (OK)" / `exit 1` 慣例回報。記錄所編譯 commit：`git -C <llamacpp_src> rev-parse HEAD` 寫入 `edge/deploy/bin/LLAMACPP_COMMIT.txt`，供 Day-N 可重現與供應鏈追溯。加註繁中說明 D-03 的「apt 先試、glibc 失敗一次即以 TALKYBUDDY_CROSS_CC/CXX 切換 Yocto SDK」止損規則。`edge/deploy/bin/` 與其下 binary 應加入 .gitignore（大型二進位不入庫；若 .gitignore 尚無此條目則補上）。
  </action>
  <verify>
    <automated>bash -n edge/deploy/build.sh && grep -q 'armv8.2-a+dotprod+i8mm' edge/deploy/build.sh && grep -q 'GGML_NATIVE=OFF' edge/deploy/build.sh && grep -q 'llama-server' edge/deploy/build.sh && grep -q 'rev-parse HEAD' edge/deploy/build.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - build.sh 語法正確（bash -n 通過）
    - 含 `-march=armv8.2-a+dotprod+i8mm` 與 `GGML_NATIVE=OFF`（D-02），不含 `armv8.7`
    - cmake `--target` 含 llama-server、llama-bench、llama-cli；產物落在 edge/deploy/bin/
    - 交叉工具鏈以 TALKYBUDDY_CROSS_CC/CXX 參數化（D-03 fallback 可切換，無第二條碼路）
    - 記錄 llama.cpp git commit hash（rev-parse HEAD → edge/deploy/bin/LLAMACPP_COMMIT.txt）
    - edge/deploy/bin/ 已列入 .gitignore
  </acceptance_criteria>
  <done>build.sh 就地擴充為交叉編譯 llama.cpp native binary（D-02 旗標、官方 repo、記錄 commit），產物置於 edge/deploy/bin/ 並通過 file 快篩；實際編譯執行留待 Checkpoint（需真工具鏈與真機 ABI 驗證）。</done>
</task>

<task type="auto">
  <name>Task 2: push.sh 推送 binary+GGUF + run_edge.sh 接線 llama-server 啟動與 /health gating（Blocker 4）</name>
  <read_first>
    - edge/deploy/push.sh（現況全檔 43 行：SSH_TARGET/TARGET_ROOT 變數、"確認 SSH 連線" 前置、rsync-per-path 區塊 lines 27-34、provision 註解）
    - edge/runtime/run_edge.sh（現況全檔 32 行：SCRIPT_DIR/TARGET_ROOT via BASH_SOURCE lines 16-18、export TALKYBUDDY_PIPELINE_PROFILE=edge、venv-first fallback lines 25-29、最後 exec uvicorn --host 0.0.0.0 --port 8787 line 31）
    - edge/runtime/run_llama_server.py（08-01 產出：main() 以相對定位解析 binary=edge/deploy/bin/llama-server 與 config.LLM_GGUF，os.execv 啟動 llama-server，讀 config.LLM_SERVER_HOST/PORT/THREADS/LLM_N_CTX）
    - server/config.py（LLM_GGUF = models/qwen2.5-1.5b-instruct-q4_k_m.gguf 相對 repo 根；LLM_SERVER_PORT 預設 8080）
    - .planning/phases/08-cpu-only-offline-edge-turn-loop/08-PATTERNS.md（§run_edge.sh lines 124-142：health-check wait 迴圈逐字片段、保留 uvicorn 0.0.0.0 line 31；§push.sh lines 213-220：rsync 新 binary 區塊慣例）
    - docs/DEPLOY_EDGE.md（既有部署流程/環境變數表，供同步更新）
  </read_first>
  <action>
    **push.sh**（就地擴充，保留「確認 SSH 連線」前置與既有 rsync 區塊）：在既有三個 rsync 區塊之後，新增兩個同款 `rsync -az` 區塊——(a) `edge/deploy/bin/` → `${SSH_TARGET}:${TARGET_ROOT}/edge/deploy/bin/`（交叉編譯產物 + LLAMACPP_COMMIT.txt），先 `ssh "${SSH_TARGET}" "mkdir -p '${TARGET_ROOT}/edge/deploy/bin' '${TARGET_ROOT}/models'"`；(b) GGUF 模型 `models/qwen2.5-1.5b-instruct-q4_k_m.gguf` → `${SSH_TARGET}:${TARGET_ROOT}/models/`（與 config.LLM_GGUF 在裝置上解析路徑一致）。rsync binary 後補一步 `ssh "${SSH_TARGET}" "chmod +x '${TARGET_ROOT}/edge/deploy/bin/llama-server' '${TARGET_ROOT}/edge/deploy/bin/llama-bench'"` 確保可執行位元。若 GGUF 不存在則以既有 echo/exit 慣例明確報錯（不靜默略過）。

    **run_edge.sh**（就地擴充，Blocker 4 的接線點）：在 `export TALKYBUDDY_PIPELINE_PROFILE=edge`（line 20）之後、`exec uvicorn`（line 31）之前，插入 llama-server 啟動段。沿用既有 PYTHON_BIN 選擇（venv-first）：以 `"${PYTHON_BIN}" -m edge.runtime.run_llama_server &`（背景）啟動 llama-server（run_llama_server.py main() 內部以 os.execv 換成 native binary，host 預設 127.0.0.1、port/threads/ctx 來自 config）；記其 PID。接著以 08-PATTERNS.md lines 137-140 的 curl 迴圈等 llama-server `/health` 就緒（探測 `http://127.0.0.1:${TALKYBUDDY_LLM_SERVER_PORT:-8080}/health`，`for i in $(seq 1 30); curl -sf ... && break; sleep 1`）。等待逾時（30 次未就緒）時**不**中止 uvicorn 啟動——僅印警告（EdgeLLM.available() 的短逾時設計本來就容忍 llama-server 稍晚/未就緒，pipeline 會走 scaffold-only，不 crash；見 RESEARCH.md Pattern 3 trade-off）。**絕不修改** line 31 的 `exec ... uvicorn ... --host 0.0.0.0 --port 8787`（uvicorn 的 0.0.0.0 是 07-03 既接受風險，與 llama-server 的 127.0.0.1 綁定無關）。確保 llama-server 綁定一律經 run_llama_server.py 的 127.0.0.1 預設，run_edge.sh 不硬編任何對外位址給 llama-server。

    **docs/DEPLOY_EDGE.md**（同步更新，不重寫）：在既有部署流程新增一節，說明 (1) build.sh 需先在開發機交叉編譯（D-02 旗標、apt→Yocto SDK fallback、記錄 commit hash）、(2) push.sh 現會一併推送 edge/deploy/bin/ 與 GGUF 模型、(3) run_edge.sh 現會先起 llama-server 並等 /health 再起 uvicorn，以及 (4) llama-server 綁 127.0.0.1（不對外）。
  </action>
  <verify>
    <automated>bash -n edge/deploy/push.sh && bash -n edge/runtime/run_edge.sh && grep -q 'edge/deploy/bin' edge/deploy/push.sh && grep -Eq 'models/.*gguf' edge/deploy/push.sh && grep -q 'run_llama_server' edge/runtime/run_edge.sh && grep -q '/health' edge/runtime/run_edge.sh && grep -q 'uvicorn server.app:app' edge/runtime/run_edge.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - push.sh、run_edge.sh 語法正確（bash -n 通過）
    - push.sh 新增 rsync edge/deploy/bin/ 與 GGUF 模型兩區塊，且 chmod +x binary；GGUF 缺失時明確報錯
    - run_edge.sh 在 uvicorn 之前以 python -m edge.runtime.run_llama_server 背景啟動 llama-server 並 curl /health 等待就緒（Blocker 4 接線）
    - run_edge.sh line 31 的 `exec ... uvicorn ... --host 0.0.0.0 --port 8787` 未被更動（既有 uvicorn 綁定保留）
    - run_edge.sh 未對 llama-server 硬編任何對外位址（綁定經 run_llama_server.py 127.0.0.1 預設）
    - docs/DEPLOY_EDGE.md 反映交叉編譯 + 推送 binary/模型 + llama-server 啟動/health-gating + 127.0.0.1 綁定
  </acceptance_criteria>
  <done>push.sh 會推送 binary+GGUF、run_edge.sh 會先起 llama-server 並等 /health 再起 uvicorn（run_llama_server.py 正式被接進啟動序列），DEPLOY_EDGE.md 同步；真機執行留待 Checkpoint。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: 真機交叉編譯 ABI 驗證 + 全 stack bring-up（Genio 520）</name>
  <action>人工於開發機執行 build.sh 交叉編譯、push.sh 推送，再於真機 Genio 520（SSH root@192.168.31.78）驗 llama-server binary 可執行（glibc ABI，Pitfall 1；失敗則依 D-03 切 Yocto SDK 重編一次）並跑 run_edge.sh 起 llama-server+uvicorn，確認 /health 200。Claude 無法代按硬體 / 代驗 ABI；需人工貼回實際輸出後續行。不靜默偽成功。</action>
  <what-built>Task 1/2 已把 build.sh 改成交叉編譯 llama.cpp（-march=armv8.2-a+dotprod+i8mm、官方 repo、記錄 commit）、push.sh 改成一併推送 binary+GGUF、run_edge.sh 改成先起 llama-server 等 /health 再起 uvicorn。以下步驟需真開發機交叉工具鏈 + 真機 Genio 520（SSH root@192.168.31.78，Tailscale-routed，見 edge/BOARD_BRINGUP_DECISION.md），Claude 無法代按硬體 / 代驗 glibc ABI。</what-built>
  <how-to-verify>
    1. 開發機交叉編譯：於 repo 根目錄設定 `TALKYBUDDY_EDGE_SSH_HOST=192.168.31.78` 後執行 `edge/deploy/build.sh`。確認產出 `edge/deploy/bin/llama-server` 且 `file edge/deploy/bin/llama-server` 顯示 `ELF 64-bit ... ARM aarch64`（非 x86-64）。記下 `cat edge/deploy/bin/LLAMACPP_COMMIT.txt`。
    2. 推送：執行 `edge/deploy/push.sh`，確認 binary、llama-bench 與 GGUF 模型都送達裝置（`ssh root@192.168.31.78 "ls -la /root/talkybuddy/edge/deploy/bin /root/talkybuddy/models"`）。
    3. **glibc ABI 快篩（Pitfall 1，D-03 止損）**：`ssh root@192.168.31.78 "/root/talkybuddy/edge/deploy/bin/llama-server --version"`。
       - 若印出版本字串 → 動態連結成功，繼續步驟 4。
       - 若出現 `GLIBC_2.XX not found` / `cannot execute binary file` → **立即**以 Yocto BSP SDK 工具鏈重編（`TALKYBUDDY_CROSS_CC=<sdk-gcc> TALKYBUDDY_CROSS_CXX=<sdk-g++> edge/deploy/build.sh`）後回步驟 1；**不要**在 apt 路徑上除錯第二次（D-03）。如實記錄採用了 apt 或 Yocto SDK 路徑。
    4. 全 stack bring-up：`edge/deploy/run.sh`（或於裝置 `nohup ./edge/runtime/run_edge.sh &`），然後：
       - 裝置本機驗 llama-server：`ssh root@192.168.31.78 "curl -sf http://127.0.0.1:8080/health"` 回 200/OK。
       - 驗 uvicorn：從開發端 `curl -sS -o /dev/null -w 'HTTP %{http_code}\n' http://192.168.31.78:8787/` 回 HTTP 200。
       - `ssh root@192.168.31.78 "curl -sS http://127.0.0.1:8787/api/status"`，確認 `network_mode=edge` 且 `llm` 欄位在 llama-server 起來後為 `true`（EdgeLLM.available() /health 探測成功）。
    貼回：file 架構、LLAMACPP_COMMIT.txt hash、採用的工具鏈路徑（apt / Yocto SDK）、llama-server --version 輸出、兩個 /health 與 /api/status 的實際輸出。不靜默偽成功：任一步未實際回應即如實記為 fail，不得標成功。
  </how-to-verify>
  <resume-signal>貼上上述真機實際輸出（含 llama-server --version、/health 200、/api/status 的 llm=true）後輸入 "approved"；若 glibc ABI 失敗，貼上已切 Yocto SDK 重編的結果或如實回報 blocker</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 開發機交叉編譯產物 → 裝置以 root 執行 | native binary 經 rsync 進入板卡並以無驗證 SSH 的 root 權限執行（07-03 已記錄 sshd 零驗證） |
| run_edge.sh 啟動的 llama-server 綁定位址 | llama-server 若綁對外位址即成第二個無驗證網路面（llama-server 本身無 guardrails 層） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure / Elevation of Privilege | run_edge.sh 啟動的 llama-server 綁定位址 | high | mitigate | run_edge.sh 不硬編對外位址；llama-server 綁定一律經 run_llama_server.py 的 127.0.0.1 預設（08-01）。實際綁定範圍由 08-05 從裝置外部 IP curl llama-server 埠驗證（預期連線被拒） |
| T-08-02 | Tampering (supply chain) | 交叉編譯的 native binary 來源 | high | mitigate | 只從官方 ggml-org/llama.cpp clone；build.sh 記錄 git rev-parse HEAD 到 LLAMACPP_COMMIT.txt；不使用第三方預編譯 binary/fork |
| T-08-07 | Denial of Service | run_edge.sh health-gating 卡死啟動 | low | mitigate | llama-server /health 等待迴圈逾時（30 次）後僅警告、不阻擋 uvicorn 啟動；pipeline 走 scaffold-only 降級不 crash |
</threat_model>

<verification>
- `bash -n edge/deploy/build.sh edge/deploy/push.sh edge/runtime/run_edge.sh`（三腳本語法正確）
- `grep -q 'armv8.2-a+dotprod+i8mm' edge/deploy/build.sh`（D-02 旗標）
- `grep -q 'run_llama_server' edge/runtime/run_edge.sh && grep -q '/health' edge/runtime/run_edge.sh`（Blocker 4 接線）
- Checkpoint：真機 llama-server --version 成功（glibc ABI）、/health 200、/api/status llm=true（實際貼回，不靜默偽成功）
</verification>

<success_criteria>
- ELOOP-02 部署面成立：native binary 交叉編譯（D-02 旗標、官方 repo、記錄 commit）→ 推送 binary+GGUF → run_edge.sh 拉起 llama-server 並 health-gate → 真機實測 binary 可執行且 /health 回 200
- run_llama_server.py 正式被接進 run_edge.sh 啟動序列（Blocker 4 關閉）
- llama-server 綁 127.0.0.1（對外綁定驗證交棒 08-05）
</success_criteria>

## Artifacts this plan produces（本 plan 產出/修改符號）
- `edge/deploy/build.sh`（改）：交叉編譯 llama.cpp → edge/deploy/bin/{llama-server,llama-bench,llama-cli} + LLAMACPP_COMMIT.txt
- `edge/deploy/push.sh`（改）：新增 rsync edge/deploy/bin/ 與 models/*.gguf + chmod +x
- `edge/runtime/run_edge.sh`（改）：背景啟動 llama-server（run_llama_server.py）+ curl /health 等待 → 再 exec uvicorn
- `docs/DEPLOY_EDGE.md`（改）：交叉編譯 + 推送 binary/模型 + llama-server 啟動/health-gating + 127.0.0.1 綁定
- `.gitignore`（改，條件式）：edge/deploy/bin/ 二進位排除
- 新 env var（build.sh）：`TALKYBUDDY_CROSS_CC`、`TALKYBUDDY_CROSS_CXX`、`TALKYBUDDY_LLAMACPP_SRC`

<output>
Create `.planning/phases/08-cpu-only-offline-edge-turn-loop/08-04-SUMMARY.md` when done
</output>
</content>
</invoke>
