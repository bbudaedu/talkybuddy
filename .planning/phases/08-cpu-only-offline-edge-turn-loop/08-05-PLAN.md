---
phase: 08-cpu-only-offline-edge-turn-loop
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - edge/runtime/measure_peak_rss.py
  - tests/test_measure_peak_rss.py
  - edge/EDGE_TURN_LOOP_VALIDATION.md
autonomous: false
requirements: [ELOOP-01, ELOOP-03, ELOOP-04]
user_setup:
  - service: hti-g520-board
    why: "首字/每回合延遲、跨行程峰值記憶體、零雲端封包稽核、llama-server 對外綁定——皆為硬體實測，必須在真機 Genio 520 上跑真引擎（不可用 PC 開發機數字替代，PROJECT.md 不靜默偽成功原則）；裝置 SSH root@192.168.31.78，Tailscale-routed"
    dashboard_config:
      - task: "Genio 520 已完成 08-04（binary+GGUF 部署、run_edge.sh 起 llama-server+uvicorn），麥克風/喇叭已接上供 local_client 端到端一輪"
        location: "真機 Genio 520 + edge/deploy 部署迴圈"
      - task: "開發機可從裝置區網外部 IP（192.168.31.78）對裝置發 curl，用於 llama-server 對外綁定驗證與零雲端封包稽核對照"
        location: "開發機（同 tailnet）"
must_haves:
  truths:
    - "真機以 TALKYBUDDY_PIPELINE_PROFILE=edge 跑完一次完整聽ASR→想LLM→說TTS 迴圈（local_client 端到端），全程零雲端網路呼叫，並有封包/log 稽核證據（非僅程式碼審閱）——ELOOP-01 + ROADMAP success criterion 1"
    - "on-device llama-bench（-t 1,2,4,6,8 掃描）+ 一次真回合首字/總延遲已實測，對照 D-05 門檻（首字 <800ms、單回合 <3–4 秒）記錄 go/no-go 判定與選定執行緒數——ELOOP-03"
    - "uvicorn 行程 VmHWM + llama-server 行程 VmHWM 跨行程加總於一輪對話期間量測，記錄峰值與 4GB 門檻餘裕（跨行程加總，非單一 PID）——ELOOP-04"
    - "從裝置外部 IP curl llama-server 埠 /health 連線被拒（llama-server 綁 127.0.0.1，未外洩到區網/tailnet）——ELOOP-02 安全，Open Question 2"
    - "以上皆為真機實際輸出，貼回並寫入 edge/EDGE_TURN_LOOP_VALIDATION.md；不靜默偽成功——未實測不得填數字"
  artifacts:
    - edge/runtime/measure_peak_rss.py
    - tests/test_measure_peak_rss.py
    - edge/EDGE_TURN_LOOP_VALIDATION.md
  key_links:
    - "measure_peak_rss.read_peak_rss_kb(pid) → /proc/<pid>/status VmHWM → sum_peak_rss([uvicorn_pid, llama_server_pid]) → 4GB 門檻判定：ELOOP-04 跨行程加總的唯一正確量測點（Pitfall 4）"
    - "llama-bench pp/tg tokens-per-sec + 真回合端到端計時 → D-05 go/no-go 門檻 → EDGE_TURN_LOOP_VALIDATION.md：ELOOP-03 硬體實測證據落點"
    - "tcpdump/log 稽核（一輪對話期間無對外雲端連線）+ 外部 curl llama-server 埠被拒 → EDGE_TURN_LOOP_VALIDATION.md：ROADMAP success criterion 1（零雲端）與 Open Question 2（綁定）的證據落點"
  prohibitions:
    - "不靜默偽成功：延遲/記憶體/零雲端/綁定任一數字未經真機實測貼回，不得寫入 VALIDATION.md 或標為 pass（PROJECT.md 決賽存亡原則）"
    - "不得用 PC 開發機 llama-bench 數字替代 Genio 520 真機數字（ELOOP-03 明訂硬體實測）"
    - "不得只量單一 PID 的 RSS——ELOOP-04 必須 uvicorn + llama-server 兩行程 VmHWM 加總（Pitfall 4）"
    - "不得以裝置本機 loopback curl 當作 llama-server 對外綁定的驗證——綁定範圍只有從裝置外部 IP 發 curl 才測得出（Open Question 2）"
    - "measure_peak_rss.py 不得以 shell=True 或字串插值呼叫外部指令；pgrep 走固定 argv 串列"
---

<objective>
補齊 Phase 8 的三個硬體實測驗收閘與零雲端稽核，全部在真機 Genio 520 上跑真引擎：ELOOP-03（on-device 首字/每回合延遲 + go/no-go 門檻）、ELOOP-04（三引擎跨行程峰值記憶體 < 4GB）、ELOOP-01 + ROADMAP success criterion 1（完整離線迴圈跑通且經封包/log 稽核證明零雲端呼叫），外加 llama-server 對外綁定驗證（Open Question 2）。先產出一個可單元測試的跨行程 VmHWM 量測工具，再以 checkpoint 收集真機實測輸出，最後寫入一份可稽核的驗證紀錄檔。

Purpose: 這三項是 Phase 8「存亡關鍵」的硬體證據——CONTEXT.md/PROJECT.md 明訂「不靜默偽成功」，延遲/記憶體/零雲端不可用假設或 PC 數字冒充。08-04 讓 stack 在真機起得來，本 plan 才能實際量。ELOOP-03/04 本質是硬體量測（manual-only），但記憶體加總邏輯（Pitfall 4 的跨行程 VmHWM 加總易出錯）可以、也應該做成可測工具，避免量測方法本身出錯而系統性低估。

Output: edge/runtime/measure_peak_rss.py（跨行程 VmHWM 加總工具，含單元測試）、edge/EDGE_TURN_LOOP_VALIDATION.md（延遲 go/no-go、記憶體加總、零雲端稽核、綁定驗證、選定執行緒數、llama.cpp commit 的真機實測紀錄）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cpu-only-offline-edge-turn-loop/08-CONTEXT.md
@.planning/phases/08-cpu-only-offline-edge-turn-loop/08-PATTERNS.md
@edge/BOARD_BRINGUP_DECISION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: edge/runtime/measure_peak_rss.py — 跨行程 VmHWM 加總工具 + 單元測試</name>
  <read_first>
    - .planning/phases/08-cpu-only-offline-edge-turn-loop/08-RESEARCH.md（Code Examples「峰值 RSS 跨行程加總」lines 441-455：read_peak_rss_kb 骨架；Pitfall 4 lines 373-395：必須 uvicorn + llama-server 兩行程 VmHWM 加總、量測時機涵蓋一輪完整對話）
    - server/cloud_llm.py（try/except 降級 idiom；固定 argv subprocess 慣例參考）
    - tests/conftest.py（既有 tmp_db autouse fixture、pytest 慣例）
  </read_first>
  <behavior>
    - read_peak_rss_kb(pid, proc_root="/proc") 讀 {proc_root}/{pid}/status 的 VmHWM: 行，回傳 kB（int）；檔不存在/無 VmHWM 行回 None（不拋）
    - sum_peak_rss(pids, proc_root="/proc") 對多個 pid 各自取 VmHWM 並加總（略過回 None 者），回傳總 kB
    - 提供以 MB 呈現與 4GB(=4096MB) 門檻比較的輔助（如 as_mb / over_threshold 判斷），純計算、無副作用
    - 單元測試以 tmp_path 造假 /proc/<pid>/status 檔（含 VmHWM: 1234 kB 行）驗證解析與加總；不需真行程
  </behavior>
  <action>
    新增 edge/runtime/measure_peak_rss.py。實作純函式 `read_peak_rss_kb(pid: int, proc_root: str = "/proc") -> int | None`：開 `{proc_root}/{pid}/status`，逐行找 `VmHWM:` 起頭者回傳其數值（kB，`int(line.split()[1])`），FileNotFoundError/無此行回 None（try/except，不拋）。實作 `sum_peak_rss(pids: list[int], proc_root: str = "/proc") -> int`：對每個 pid 取 read_peak_rss_kb，加總非 None 值。實作輔助 `kb_to_mb(kb: int) -> float` 與 `within_threshold(total_kb: int, limit_mb: int = 4096) -> bool`（純計算）。實作 `main()`：以固定 argv 串列 `subprocess.run(["pgrep", "-f", "uvicorn server.app:app"], ...)` 與 `["pgrep", "-f", "llama-server"]`（絕不 shell=True / 字串插值）取兩行程 PID，分別印各自 VmHWM 與加總（MB）並與 4096MB 門檻比較，印 PASS/FAIL 提示（供裝置上人工執行取數）。加 `if __name__ == "__main__": main()`。新增 tests/test_measure_peak_rss.py：用 tmp_path 造 `<tmp>/<pid>/status` 檔（內含 `VmHWM:\t 51200 kB` 之類行），斷言 read_peak_rss_kb 解析正確、缺檔回 None、sum_peak_rss 正確加總兩個假 pid、within_threshold 邊界正確。測試不呼叫 main、不起真行程。若 pytest 無法解析 `edge.runtime.measure_peak_rss`，沿用 08-01 已建立的 edge/__init__.py、edge/runtime/__init__.py（若尚不存在則補空檔）。
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_measure_peak_rss.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_measure_peak_rss.py 全綠：VmHWM 解析、缺檔回 None、跨兩行程加總、門檻邊界
    - `.venv/bin/python -c "from edge.runtime.measure_peak_rss import sum_peak_rss, read_peak_rss_kb"` 成功匯入
    - `grep -n "shell=True" edge/runtime/measure_peak_rss.py` 無輸出（pgrep 走固定 argv）
    - 提供 proc_root 參數使測試可指向 tmp 假 /proc（純函式、無真行程依賴）
  </acceptance_criteria>
  <done>跨行程 VmHWM 加總工具就緒且經單元測試守護（ELOOP-04 量測方法正確性有自動化保護），main() 可在裝置上取 uvicorn+llama-server 兩 PID 加總；真機實際數字由 Checkpoint 取得。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: 真機端到端一輪 + 延遲/記憶體/零雲端/綁定四項實測（Genio 520）</name>
  <action>人工於真機 Genio 520（SSH root@192.168.31.78，已接麥克風/喇叭）跑真引擎，實測四項並貼回實際輸出：A) llama-bench 執行緒掃描 + 一次真回合首字/總延遲的 D-05 go/no-go；B) uvicorn + llama-server 跨行程 VmHWM 加總 vs 4GB；C) 一輪對話期間 tcpdump/log 零雲端稽核；D) 從裝置外部 IP curl llama-server 埠應被拒。Claude 無法代跑真引擎 / 代量真機數字；不靜默偽成功，未實測不得填數字。</action>
  <what-built>08-04 已讓 llama-server + uvicorn 在真機起得來且 /health 200；08-03 已提供 local_client + audio_io；Task 1 已提供跨行程 VmHWM 加總工具。以下四項皆為硬體實測，需真機 Genio 520（SSH root@192.168.31.78）+ 已接麥克風/喇叭 + 開發端可對裝置外部 IP 發 curl。Claude 無法代跑真引擎 / 代量真機延遲與記憶體。不靜默偽成功：未實測不得填數字。</what-built>
  <how-to-verify>
    前置：裝置上 `run_edge.sh` 已起（llama-server 127.0.0.1:8080 + uvicorn 0.0.0.0:8787），`curl -sf http://127.0.0.1:8080/health` 與 `curl -sf http://127.0.0.1:8787/` 皆通。

    A) **ELOOP-03 延遲（llama-bench + 真回合計時）**
      1. `ssh root@192.168.31.78 "/root/talkybuddy/edge/deploy/bin/llama-bench -m /root/talkybuddy/models/qwen2.5-1.5b-instruct-q4_k_m.gguf -p 128 -n 128 -t 1,2,4,6,8 -r 3"`，記錄各執行緒的 pp（prompt eval t/s）與 tg（generation t/s）。
      2. 依 pp 估首字延遲 ≈ (實際 system+user prompt 估計 token 數 / pp) * 1000 ms，挑選最佳 --threads（pp 與 tg 最佳可能不同）。
      3. 跑一次完整真回合（啟動 local_client，對麥克風講一句），碼錶/log 量「收音結束 → 開始播放回覆」總延遲。
      4. 對照 D-05 門檻（首字 <800ms、單回合總延遲 <3–4 秒）判定 go/no-go；若 no-go，記錄採用的 fallback（縮短 prompt/scaffold、降 n_ctx、或該回合 scaffold-only）。
    B) **ELOOP-04 記憶體（跨行程 VmHWM 加總）**：在一輪對話進行中（ASR 解碼+LLM 生成+TTS 合成重疊瞬間）於裝置執行 `ssh root@192.168.31.78 "cd /root/talkybuddy && .venv/bin/python -m edge.runtime.measure_peak_rss"`（或分別 `grep VmHWM /proc/<uvicorn_pid>/status` 與 `/proc/<llama_server_pid>/status` 再加總）。記錄 uvicorn VmHWM、llama-server VmHWM、加總 MB，及與 4096MB 的餘裕。
    C) **零雲端稽核（ROADMAP success criterion 1）**：在跑完整一輪對話期間，於裝置端另一個 SSH session 抓封包：`ssh root@192.168.31.78 "timeout 30 tcpdump -nn -i any 'not port 22 and not net 127.0.0.0/8' -w /tmp/turn_audit.pcap"`（排除 SSH 管理與 loopback；tailnet/區網管理流量另行辨識排除），跑完後 `tcpdump -nn -r /tmp/turn_audit.pcap | head` 檢視是否有對外雲端主機（Bedrock/Anthropic/ElevenLabs 等公網 IP）連線。並對照 `ssh ... "curl -sS http://127.0.0.1:8787/api/status"` 顯示 `network_mode=edge`、`cloud_llm=false`/`cloud_tts=false`。把 pcap 摘要（無對外雲端連線）與 /api/status 貼回;pcap 檔留作證據。
    D) **llama-server 對外綁定驗證（Open Question 2）**：從開發端對裝置**外部 IP** 發 `curl -sS -m 5 http://192.168.31.78:8080/health`，預期連線被拒/逾時（llama-server 綁 127.0.0.1，不對外）。對照裝置本機 `curl http://127.0.0.1:8080/health` 應為 200——兩者差異即證明綁定正確。
    貼回：A 的 llama-bench 表 + 真回合總延遲 + go/no-go；B 的兩行程 VmHWM 與加總 MB；C 的 tcpdump 摘要 + /api/status；D 的外部 curl（被拒）對照本機 curl（200）。任一項未實測即如實記 fail/blocked，不得偽造。
  </how-to-verify>
  <resume-signal>貼上 A/B/C/D 四項的真機實際輸出（llama-bench 表、真回合延遲 go/no-go、跨行程 VmHWM 加總、tcpdump 零雲端摘要、外部 curl 被拒對照本機 200）後輸入 "approved"；任一項為 blocker 請如實回報</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 寫入 edge/EDGE_TURN_LOOP_VALIDATION.md 真機驗證紀錄</name>
  <files>edge/EDGE_TURN_LOOP_VALIDATION.md</files>
  <read_first>
    - .planning/ROADMAP.md（Phase 8 success criteria #1–#4，作為紀錄對照的驗收條目）
    - .planning/REQUIREMENTS.md（ELOOP-01..04 全文）
    - .planning/phases/08-cpu-only-offline-edge-turn-loop/08-CONTEXT.md（D-05 延遲門檻、不靜默偽成功原則）
    - edge/BOARD_BRINGUP_DECISION.md（既有真機紀錄格式與「真實輸出、非模擬」書寫慣例，對齊此檔風格）
  </read_first>
  <action>
    依前一 Checkpoint 貼回的真機實際輸出，建立 edge/EDGE_TURN_LOOP_VALIDATION.md，內容須含（皆為實測值，非臆造）：
    - 標題 + 驗證日期（實際執行當日）+ 板卡/OS/kernel（沿用 BOARD_BRINGUP_DECISION.md 的識別）+ 所用 llama.cpp commit（edge/deploy/bin/LLAMACPP_COMMIT.txt）與採用的交叉工具鏈路徑（apt / Yocto SDK）。
    - **ELOOP-03 延遲**：llama-bench 各執行緒 pp/tg 表、選定 --threads、真回合「收音結束→開始播放」總延遲、對照 D-05（800ms / 3–4s）的 go/no-go 判定；no-go 時記錄採用的 fallback。
    - **ELOOP-04 記憶體**：uvicorn VmHWM、llama-server VmHWM、加總 MB、對 4096MB 的餘裕、量測時機（一輪對話重疊瞬間）。
    - **ELOOP-01 + 零雲端（success criterion 1）**：完整一輪聽→想→說跑通紀錄 + tcpdump 稽核摘要（無對外雲端連線，附 pcap 檔路徑）+ /api/status（network_mode=edge、cloud_* false）。
    - **綁定驗證（Open Question 2）**：外部 IP curl llama-server 埠被拒 vs 本機 127.0.0.1 curl 200 的對照輸出。
    如實反映實測；任一項未實測（如硬體不可用）標記為 blocker，不得偽裝成功。
  </action>
  <verify>
    <automated>test -f edge/EDGE_TURN_LOOP_VALIDATION.md && grep -Eiq 'llama-bench' edge/EDGE_TURN_LOOP_VALIDATION.md && grep -Eiq 'VmHWM' edge/EDGE_TURN_LOOP_VALIDATION.md && grep -Eiq 'go/no-go|go / no-go|no-go' edge/EDGE_TURN_LOOP_VALIDATION.md && grep -Eiq 'tcpdump|network_mode' edge/EDGE_TURN_LOOP_VALIDATION.md && grep -Eq '2026-[0-9]{2}-[0-9]{2}' edge/EDGE_TURN_LOOP_VALIDATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - edge/EDGE_TURN_LOOP_VALIDATION.md 存在且含驗證日期（2026-…）
    - 含 ELOOP-03 延遲段（llama-bench + 真回合延遲 + go/no-go + 選定 threads）
    - 含 ELOOP-04 記憶體段（uvicorn + llama-server VmHWM 加總 + 4GB 餘裕）
    - 含零雲端稽核段（tcpdump 摘要 / network_mode=edge）與綁定驗證段（外部 curl 被拒 vs 本機 200）
    - 數字皆標註來源為真機實測（不靜默偽成功）；未實測項標為 blocker
  </acceptance_criteria>
  <done>一份有日期、可稽核的真機驗證紀錄存在，如實記錄 ELOOP-03 延遲 go/no-go、ELOOP-04 跨行程記憶體加總、ELOOP-01 零雲端稽核與 llama-server 對外綁定驗證。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| llama-server 綁定位址 vs 裝置區網/tailnet | 從裝置外部 IP 是否連得到 llama-server 埠，決定是否存在第二個無驗證推論端點 |
| 邊緣裝置 → 雲端（應為零） | edge profile 一輪對話期間不應有任何對外雲端連線；封包稽核為證據 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure / Elevation of Privilege | llama-server 對外綁定 | high | mitigate | Checkpoint D 從裝置外部 IP curl llama-server 埠，預期被拒（綁 127.0.0.1）；結果寫入 VALIDATION.md，若可從外部連上即為 high 缺口需回 08-04 修正綁定 |
| T-08-08 | Information Disclosure | 一輪對話期間意外對外雲端連線（洩漏衍生文字/觸發雲端） | medium | mitigate | Checkpoint C tcpdump 稽核一輪期間無對外雲端連線 + /api/status cloud_* false；作為 ROADMAP success criterion 1「經封包/log 稽核」的證據 |
| T-08-09 | Repudiation / 靜默偽成功 | 延遲/記憶體/零雲端數字未實測即宣稱 pass | high | mitigate | Checkpoint 為 blocking human-verify，resume-signal 要求貼回真機實際輸出；VALIDATION.md 明訂未實測標 blocker，不得偽造（PROJECT.md 原則） |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_measure_peak_rss.py -x -q`（跨行程 VmHWM 加總工具單元測試）
- `.venv/bin/python -m pytest tests/ -x -q`（全套件無回歸）
- Checkpoint：真機 A/B/C/D 四項實際輸出貼回（llama-bench、真回合延遲 go/no-go、VmHWM 加總、tcpdump 零雲端、外部 curl 被拒）
- `test -f edge/EDGE_TURN_LOOP_VALIDATION.md` 且含延遲/記憶體/零雲端/綁定四段與日期
</verification>

<success_criteria>
- ELOOP-03：真機 llama-bench + 真回合延遲實測，對照 D-05 訂出 go/no-go 與選定執行緒數
- ELOOP-04：uvicorn + llama-server 跨行程 VmHWM 加總 < 4GB 並記錄餘裕（Pitfall 4 加總方式）
- ELOOP-01 + success criterion 1：完整離線迴圈跑通且經 tcpdump/log 稽核證明零雲端呼叫
- Open Question 2：llama-server 對外綁定經外部 curl 驗證（被拒）
- 全部真機實測、寫入可稽核紀錄，不靜默偽成功
</success_criteria>

## Artifacts this plan produces（本 plan 產出符號）
- `edge/runtime/measure_peak_rss.py`（新檔）：`read_peak_rss_kb(pid, proc_root)`、`sum_peak_rss(pids, proc_root)`、`kb_to_mb`、`within_threshold`、`main()`
- `tests/test_measure_peak_rss.py`（新檔）
- `edge/EDGE_TURN_LOOP_VALIDATION.md`（新檔）：延遲 go/no-go、記憶體加總、零雲端稽核、綁定驗證、選定 threads、llama.cpp commit 的真機實測紀錄

<output>
Create `.planning/phases/08-cpu-only-offline-edge-turn-loop/08-05-SUMMARY.md` when done
</output>
</content>
