---
phase: 09-network-cut-demo-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/app.py
  - tests/test_e2e.py
  - tests/test_app_profile.py
  - tests/test_app_directive.py
  - tests/test_consent_gate.py
autonomous: true
requirements: [NETCUT-01]
must_haves:
  truths:
    - "主持人在 /ws/talk 連線持續開著（不重整頁面、不重連 WS）的情況下切換飛航模式後，下一回合實際改走 edge 引擎，cloud LLM 不再被呼叫（D-01 的 kill-switch 對進行中的對話真正生效）"
    - "切到 edge 後寫入 DB 的下一筆 interaction，其 network_mode 欄位為 'edge'"
    - "conn_pipe.network_mode 只從全域 pipeline.network_mode 讀取；全專案 conn_pipe.network_mode 的寫入來源唯一（POST /api/network_mode → pipeline.network_mode 仍是唯一真值寫入點）"
    - "POST /api/network_mode 需帶有效 JWT；缺 token 或 token 無效/過期 → 401，且不改變 pipeline.network_mode"
    - "瀏覽器↔本機 server 的 loopback WS 完全不受 network_mode 影響：切換不斷線、不重連、既有 try/except-swallow + idle-emit 契約不變"
  artifacts:
    - server/app.py
    - tests/test_e2e.py
    - tests/test_app_profile.py
    - tests/test_app_directive.py
    - tests/test_consent_gate.py
  key_links:
    - "POST /api/network_mode（server/app.py:229 唯一寫入者）→ 全域 pipeline.network_mode → 每回合 dispatch 前 conn_pipe.network_mode 再同步 → VoicePipeline._process_text 的 engines 清單閘門（server/pipeline.py:260-266）：這條鏈是 NETCUT-01 全部的因果路徑，斷在中間任一點 kill-switch 就是裝飾品"
    - "identity_from_header(authorization)（server/app.py:262-270 既有函式）→ api_network_mode：kill-switch 從『任何同網段裝置可翻轉』收斂為『須持有效 JWT』；前端 web/index.html:712 已送 authHeaders，無 UX 變更"
  prohibitions:
    - "不得新增第二個 conn_pipe.network_mode 的寫入者（只能從 pipeline.network_mode 讀）"
    - "不得加入 asyncio.Task.cancel() / 請求取消機制讓進行中的雲端呼叫中止（D-03 明示排除）"
    - "不得新增任何網路連通性偵測、心跳或探測邏輯（D-02 明示排除）"
    - "不得改動 edge/runtime/run_edge.sh 的 uvicorn --host 0.0.0.0 啟動行（既有已接受風險，該檔第 35 行明文禁止修改）"
    - "不得對 /api/network_mode 加上角色限制（student/tutor/device 皆須可翻轉；D-04 鎖定主持人就用學生畫面上的按鈕）"
    - "不得改動 /ws/talk 既有的 try/except Exception → emit idle 錯誤處理契約"
---

<objective>
修好 NETCUT-01 的核心阻擋缺陷：讓「按下飛航模式開關」對**已經開著的 /ws/talk 對話**真正生效，並把這個 kill-switch 從無驗證端點收斂成需帶 JWT 的信任邊界控制。

Purpose: RESEARCH.md Pitfall 1 以純程式碼推斷證實——`server/app.py:369` 只在 WebSocket accept 當下把全域 `pipeline.network_mode` 複製進 per-connection 的 `conn_pipe.network_mode` 一次，而 `web/index.html` 的 `connectWS()` 在頁面載入時只開一條長生命週期連線、之後不重連。`POST /api/network_mode`（D-01 指定沿用的 kill-switch）只寫全域值，沒有任何程式路徑會把新值回灌進已開著的連線。**照現況出貨，主持人在對話中途按下開關對這一場對話的效果是零**，只有重整頁面才會生效——這直接違反 NETCUT-01 與 ROADMAP Phase 9 success criterion #1 的核心承諾。此為本 phase 的第一順位修復，其餘工作（逾時、UI、彩排）在這條鏈修好之前全部沒有意義。

同時，這個開關在本 phase 被賦予「PROJECT.md 隱私硬限制的操作者可觀察保證」這個新角色（主持人宣稱「現在完全離線」），但它目前沒有任何身份驗證，而 uvicorn 綁 `0.0.0.0:8787`（`edge/runtime/run_edge.sh:63`，既有已接受風險）——同網段任何裝置都能翻轉它。前端已經送 `authHeaders`（`web/index.html:712`），補上 server 端驗證是零 UX 成本的信任邊界修復。

Output: `server/app.py` 兩處每回合再同步 + `/api/network_mode` JWT 閘門；`tests/test_e2e.py` 新增活體 WS 切換回歸測試與 401 測試；四個既有測試檔的 10 個 `/api/network_mode` 呼叫點補上 token。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-network-cut-demo-hardening/09-CONTEXT.md
@.planning/phases/09-network-cut-demo-hardening/09-RESEARCH.md
@.planning/phases/09-network-cut-demo-hardening/09-PATTERNS.md

**環境前置（執行任何 `<automated>` 前先跑一次，冪等）：** 本工作樹目前缺 `.venv/`（`.venv_ready` 標記在、目錄不在），但專案慣例（`run_tests.sh`、07/08 系列 plan）一律以 `.venv/bin/python` 執行測試。先確保直譯器可用再開工：
`[ -x .venv/bin/python ] || ln -sfn /home/budaedu/hackathon/talkybuddy/.venv .venv`
（`.venv` 已列在 `.gitignore:12-13`，此 symlink 不會進 repo。若該路徑亦不存在，改以 `python3 -m venv .venv` 後依 `scripts/setup_env.sh` 第 8-9 行的套件清單安裝 fastapi/uvicorn/pydantic/pytest/httpx/numpy/soundfile 即可跑本 plan 的測試。）
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 每回合 dispatch 前重新同步 conn_pipe.network_mode（NETCUT-01 核心修復）＋ 活體 WS 回歸測試</name>
  <files>server/app.py, tests/test_e2e.py</files>
  <read_first>
    - server/app.py:355-421（`ws_talk` 的 token 驗證 → `conn_pipe = VoicePipeline(...)` → `conn_pipe.network_mode = pipeline.network_mode  # 承接目前模式`（line 369，正確的「初值」，保留不動）→ `emit` / `send_turn_result` → `process_audio_buffer()` 內的 `try:` 區塊）
    - server/app.py:472-478（`elif mtype == "text_input":` 分支的 `try:` 區塊，第二個 dispatch 呼叫點）
    - server/app.py:206-259（`api_network_mode`：line 229 `pipeline.network_mode = mode` 是全域唯一寫入者，本 task 不動它）
    - server/pipeline.py:255-282（`_process_text` 的 engines 清單閘門 `if self.network_mode == "cloud" and ...`，line 260-266；本 task 不動）
    - tests/test_e2e.py:25-30（autouse `_reset_network_mode` fixture，模組級單例跨測試污染防護）與 tests/test_e2e.py:156-192（`test_ws_talk_text_input_full_flow`，starlette TestClient + `websocket_connect` 的既有範式）
    - .planning/phases/09-network-cut-demo-hardening/09-PATTERNS.md（§`server/app.py` — NETCUT-01 fix，逐行標出兩個插入點與「新行必須放在既有 try: 內」的約束）
  </read_first>
  <behavior>
    - 先 POST `/api/network_mode {"mode":"cloud"}` → 開 `/ws/talk` → 送第一次 `text_input`：cloud LLM stub 的呼叫計數 == 1
    - **不關閉該 WS**，把全域模式切成 `edge` → 在**同一條** WS 上送第二次 `text_input`：cloud LLM stub 呼叫計數仍為 1（沒有再被呼叫），edge LLM stub 呼叫計數 >= 1
    - 第二回合寫入 DB 的 interaction row，其 `network_mode` 欄位為 `"edge"`
    - 切換過程中 WS 不斷線：第二次 `text_input` 仍能正常收到 `reply` 事件（不需重連）
    - 修復前跑這條測試必為 RED（第二回合仍會呼叫 cloud stub）；修復後為 GREEN
  </behavior>
  <action>
    步驟 1（修復，兩處各一行）：在 `server/app.py` 的 `process_audio_buffer()` 內，把 `conn_pipe.network_mode = pipeline.network_mode` 插入既有 `try:` 區塊的**第一行**、緊接在 `result = await conn_pipe.run_turn_audio(data, emit)` 之前；在 `elif mtype == "text_input":` 分支內做完全相同的插入，緊接在 `result = await conn_pipe.run_turn_text(text, emit)` 之前。兩處都加上與檔案既有風格一致的繁中行末註解，說明「每回合前重新承接全域模式（D-01 kill-switch 對進行中連線生效）」。新行**必須在既有 `try:` 內**（不可放在 `try:` 之前）——這樣即使再同步本身出錯也走既有 `except Exception: await emit({"type": "state", "state": "idle"})` 路徑，維持「單輪失敗不斷線」契約。保留 line 369 的連線期初值賦值不動（那是正確的初值，不是 bug）。不要動 `api_network_mode`，不要在 `VoicePipeline` 內新增任何讀取全域的機制，不要引入連線註冊表（RESEARCH.md Pattern 1 明示最小外科式修法）。

    步驟 2（回歸測試）：在 `tests/test_e2e.py` 末尾新增 `test_network_mode_switch_affects_live_ws_session(monkeypatch)`，沿用同檔 `test_ws_talk_text_input_full_flow` 的 starlette `TestClient` + `client.websocket_connect(f"/ws/talk?token={tok}")` 範式（httpx ASGITransport 不支援 WS，必須用 TestClient）。在連線**之前**用 `monkeypatch.setattr(app_module, "cloud_llm_engine", <雲端 stub>)` 與 `monkeypatch.setattr(app_module, "llm_engine", <邊緣 stub>)` 換掉模組級引擎單例（`ws_talk` 在函式內才讀這些全域名，所以連線時會拿到 stub）；兩個 stub 各自實作 `available() -> True` 與 `generate(student_text, scaffold_result, directive=None)`，各自累加自己的呼叫計數並回傳可區分的字串（例如雲端回 `"雲端回覆：跟我說一遍：I see a cat"`、邊緣回 `"邊緣回覆：跟我說一遍：I see a cat"`）。另 `monkeypatch.setattr(app_module.tts_engine, "available", lambda: False)` 讓 TTS 走 `tts_unavailable` 快速路徑。token 用同檔既有 `auth.issue_token("STUDENT-AMING-004", "student")`。流程嚴格照上面 `<behavior>`：先 POST 切 cloud（帶 `Authorization: Bearer <tok>` header，因為 Task 2 會為該端點加驗證；先寫上不影響 Task 1 通過）、再開 WS 跑第一回合、**在 `with client.websocket_connect(...)` 區塊內**發第二個 POST 切 edge、再跑第二回合。若 starlette TestClient 在 WS session 開著時無法發 HTTP 請求（blocking portal 行為），退而改用 `app_module.pipeline.network_mode = "edge"` 直接設定全域值——這是 POST 的唯一相關副作用，等價且仍完整證明本次修復（若採此退路，在測試 docstring 明確註明原因，不得刪掉斷言）。最後用 `store.list_interactions(limit=10)` 斷言第二回合那筆 row 的 `network_mode == "edge"`。務必先在**未修復**的程式碼上跑一次確認為 RED，再套用步驟 1 讓它轉 GREEN（RED→GREEN 兩個 commit）。
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_e2e.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_e2e.py::test_network_mode_switch_affects_live_ws_session` 存在且通過
    - `tests/test_e2e.py` 全檔綠（既有 10 條測試無回歸）
    - `server/app.py` 中 `conn_pipe.network_mode = pipeline.network_mode` 出現 3 次：line 369 的連線期初值 + `process_audio_buffer()` 內 1 次 + `text_input` 分支內 1 次；驗證指令：`[ "$(grep -c 'conn_pipe.network_mode = pipeline.network_mode' server/app.py)" = "3" ]`
    - 兩個新插入點都在既有 `try:` 區塊內（其後緊接 `result = await conn_pipe.run_turn_*`），既有 `except Exception: ... emit idle` 未被改動
    - `server/app.py` 未新增任何 `asyncio.Task.cancel`、連通性探測、或第二個 `conn_pipe.network_mode` 寫入來源
  </acceptance_criteria>
  <done>不重整頁面、不重連 WS，切換飛航模式後的下一回合就真的走 edge，且有一條會在缺陷回歸時變紅的活體 WS 測試守著。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/network_mode 加上 JWT 閘門（kill-switch 信任邊界）＋ 既有 10 個測試呼叫點補 token</name>
  <files>server/app.py, tests/test_e2e.py, tests/test_app_profile.py, tests/test_app_directive.py, tests/test_consent_gate.py</files>
  <read_first>
    - server/app.py:206-259（`api_network_mode` 目前的簽名 `async def api_network_mode(body: NetworkModeBody):` — 完全沒有 `authorization` 參數，也沒有任何驗證）
    - server/app.py:262-270（既有 `identity_from_header(authorization)`：缺/格式錯 → 401「缺少或格式錯誤的 token」；`auth.InvalidToken` → 401「token 無效或過期」）
    - server/app.py:282-296（`api_interactions` / `api_diagnoses`：`authorization: str | None = Header(default=None)` + 首行 `claims = identity_from_header(authorization)` 的既有慣例，逐字照抄這個形狀）
    - web/index.html:707-732（`airplaneSwitch` click handler 已用 `headers: authHeaders({ "Content-Type": "application/json" })`，line 712）與 web/index.html:332-336（`authHeaders` 只在 `authToken` 存在時加 header；登入遮罩保證 app 啟動時 authToken 必定已設）
    - server/auth.py:50-52（`issue_token(sub, role, ttl=86400)` — 24 小時效期，對單場 demo 足夠；過期只會讓開關回既有的「切換失敗，請再試一次」toast）
    - tests/test_app_authz.py:13-16（`test_interactions_requires_token`：無 token → 401 的既有測試形狀）
  </read_first>
  <behavior>
    - 不帶 `Authorization` header POST `/api/network_mode {"mode":"edge"}` → 401，且 `pipeline.network_mode` 維持原值未被改動
    - 帶格式錯誤的 header（例如 `Authorization: Token abc`）→ 401
    - 帶有效 student token POST `{"mode":"cloud"}` → 200，行為與現況完全一致（`mark_all_synced` + `generate_diagnosis` + 回 `new_diagnosis`）
    - 帶有效 token 但 `{"mode":"wifi"}` → 仍是 400（驗證先行不改變既有輸入驗證結果）
    - 帶有效 tutor 或 device role 的 token 也可切換（不做角色限制；D-04 鎖定主持人用學生畫面的按鈕）
  </behavior>
  <action>
    步驟 1（server）：把 `server/app.py` 的 `api_network_mode` 簽名改成 `async def api_network_mode(body: NetworkModeBody, authorization: str | None = Header(default=None)):`，並在函式 docstring 之後、`mode = body.mode` 之前插入一行 `identity_from_header(authorization)`（逐字沿用 `api_interactions`/`api_diagnoses` 的既有慣例；本端點不需要 claims 內容，只需要「已驗證」這個事實，故不必接收回傳值——若專案 lint 偏好，可寫成 `_ = identity_from_header(authorization)`）。驗證必須排在 `mode not in ("edge","cloud")` 的 400 檢查**之前**，讓未授權請求在碰到任何業務邏輯前就被擋掉。`Header` 已於檔案既有 import 中可用（`api_interactions` 已在用），不需新增 import。在 docstring 補一句繁中說明：此端點是 D-01 的 kill-switch，uvicorn 綁 0.0.0.0（見 `edge/runtime/run_edge.sh:63`），故必須帶 JWT，避免同網段任何裝置翻轉離線宣稱。**不要**加角色限制（student/tutor/device 皆放行），**不要**改動 uvicorn 綁定，**不要**改動 consent gate 分支的既有邏輯與回應形狀。

    步驟 2（測試呼叫點）：把下列 10 個既有 `/api/network_mode` POST 呼叫點補上有效 token header，一律沿用各檔既有的 token 產生方式（`server.auth.issue_token(...)`）：
      - `tests/test_e2e.py:93, 104, 142, 152` — 該檔第 21 行已有模組級 `_STUDENT_AUTH` 常數，直接加 `headers=_STUDENT_AUTH`
      - `tests/test_app_profile.py:22, 36` — 檔頭新增 `from server import auth` 與一個等價的 `_AUTH` 常數後帶入 `headers=_AUTH`
      - `tests/test_app_directive.py:16, 32` — 同上
      - `tests/test_consent_gate.py:29, 50` — 同上
    只加 header，不改任何既有斷言。

    步驟 3（新測試）：在 `tests/test_e2e.py` 新增兩條：`test_post_network_mode_requires_token`（不帶 header → 401，且 `pipeline.network_mode` 未變）與 `test_post_network_mode_invalid_token_returns_401`（`Authorization: Bearer not-a-real-token` → 401）。沿用 `tests/test_app_authz.py:13-16` 的形狀與同檔既有 async client 慣例。
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_e2e.py tests/test_app_profile.py tests/test_app_directive.py tests/test_consent_gate.py tests/test_app_authz.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 上述五個測試檔全綠
    - `grep -n 'identity_from_header' server/app.py` 顯示 `api_network_mode` 內含一次呼叫，且位置在 `mode not in ("edge", "cloud")` 的 400 檢查之前
    - 無 token 的 `POST /api/network_mode` 回 401（由新測試 `test_post_network_mode_requires_token` 斷言），且該請求不改變 `pipeline.network_mode`
    - `api_network_mode` 未新增任何角色判斷（`claims["role"]` 不出現在該函式內）
    - `edge/runtime/run_edge.sh` 與 `scripts/run.sh` 的 uvicorn 啟動行未被改動：`grep -c 'host 0.0.0.0' edge/runtime/run_edge.sh scripts/run.sh` 結果不變
    - 前端 `web/index.html` 本 task 完全不動（airplaneSwitch 已送 authHeaders）
  </acceptance_criteria>
  <done>kill-switch 從「同網段任何人可翻轉」變成「須持有效 JWT」，前端零改動、既有測試全部補齊 token 後仍綠。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 全域 `pipeline.network_mode` ↔ per-connection `conn_pipe.network_mode` | 操作者宣稱「現在離線」的唯一依據是全域旗標；已開連線若讀到陳舊副本，該宣稱即不成立（孩子語音衍生文字仍會出境） |
| LAN/tailnet ↔ `POST /api/network_mode`（uvicorn 綁 0.0.0.0:8787） | kill-switch 是 PRIV-01/02 隱私硬限制的操作者可觀察控制點；任何能打到 8787 埠的裝置目前都能翻轉它 |
| 瀏覽器 ↔ 本機 server 的 `/ws/talk` loopback | NETCUT-01 明文要求此路徑不受斷網影響；任何「切換時重連 WS」的做法都會違反此邊界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `conn_pipe.network_mode` 陳舊副本（`server/app.py:369` 連線期唯一一次複製，之後永不更新） | high | mitigate | Task 1：在 `process_audio_buffer()` 與 `text_input` 兩個 dispatch 點於每回合前從全域重新同步；以 `tests/test_e2e.py::test_network_mode_switch_affects_live_ws_session` 活體 WS 回歸測試守護，缺陷回歸即變紅 |
| T-09-02 | Tampering / Elevation of Privilege | `POST /api/network_mode` 完全無身份驗證，而 uvicorn 綁 `0.0.0.0:8787`（`edge/runtime/run_edge.sh:63`，既有已接受風險） | high | mitigate | Task 2：handler 首行呼叫既有 `identity_from_header(authorization)`，缺/壞/過期 token → 401；前端 `web/index.html:712` 已送 `authHeaders`，UX 零變更；token TTL 86400s 對單場 demo 足夠，過期僅退回既有「切換失敗，請再試一次」toast |
| T-09-03 | Tampering | 兩個並發 `POST /api/network_mode`，或「每回合再同步」與 pipeline 消費該值之間的競態 | low | accept | `api_network_mode` 是 async handler 且 cloud 分支以同步阻塞呼叫佔住 event loop → 實際被序列化；`conn_pipe.network_mode = pipeline.network_mode` 是單一 str 屬性讀寫、中間無 `await`，對 event loop 為原子。回合進行中翻轉仍以回合起始模式路由，此為 D-03 明示接受的行為（不做取消機制），並由 09-04 彩排的 M1 指標實測其代價 |
| T-09-SC | Tampering | npm/pip/cargo 套件安裝 | low | accept | 本 phase 不安裝任何套件（09-RESEARCH.md：Package Legitimacy Audit **not applicable**，全部改動皆為既有第一方 Python 常數與 handler），無供應鏈攻擊面，故不需 legitimacy checkpoint |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_e2e.py tests/test_app_profile.py tests/test_app_directive.py tests/test_consent_gate.py tests/test_app_authz.py -x -q`（新測試綠 + 四個既有檔無回歸）
- `[ "$(grep -c 'conn_pipe.network_mode = pipeline.network_mode' server/app.py)" = "3" ]`（連線期初值 1 次 + 兩個 dispatch 點各 1 次）
- `.venv/bin/python -m pytest tests/ -x -q`（全套件回歸，確認補 token 沒有漏掉任何呼叫點）
</verification>

<success_criteria>
- 已開著的 `/ws/talk` 連線在切換飛航模式後，下一回合實際改走 edge 引擎（有測試守護）
- 第二回合寫入 DB 的 interaction row `network_mode == "edge"`
- `POST /api/network_mode` 無有效 JWT 一律 401，且不改變全域模式
- 全測試套件綠；`web/index.html`、uvicorn 綁定、`VoicePipeline` 內部邏輯皆未被本 plan 改動
</success_criteria>

## Artifacts this phase produces（本 plan 產出符號）
- `server/app.py::ws_talk.process_audio_buffer()` — 新增每回合前的 `conn_pipe.network_mode = pipeline.network_mode` 再同步（行為變更，非新符號）
- `server/app.py::ws_talk` 的 `text_input` 分支 — 同上第二處再同步
- `server/app.py::api_network_mode(body, authorization)` — 簽名新增 `authorization: str | None = Header(default=None)`；行為新增 401 閘門
- `tests/test_e2e.py::test_network_mode_switch_affects_live_ws_session`（新測試，NETCUT-01 回歸守護）
- `tests/test_e2e.py::test_post_network_mode_requires_token`（新測試）
- `tests/test_e2e.py::test_post_network_mode_invalid_token_returns_401`（新測試）
- `tests/test_app_profile.py` / `tests/test_app_directive.py` / `tests/test_consent_gate.py` — 各新增模組級 `_AUTH` 常數（測試輔助）

<output>
Create `.planning/phases/09-network-cut-demo-hardening/09-01-SUMMARY.md` when done
</output>
