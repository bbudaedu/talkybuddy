---
phase: 07-day-0-config-hardening-board-bring-up-spike
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - edge/README.md
  - edge/runtime/run_edge.sh
  - edge/runtime/README.md
  - edge/deploy/build.sh
  - edge/deploy/push.sh
  - edge/deploy/run.sh
  - edge/deploy/README.md
  - edge/models/README.md
  - docs/DEPLOY_EDGE.md
autonomous: true
requirements: [EDGE-03, EDGE-04]
must_haves:
  truths:
    - "頂層 edge/ 骨架存在且含三子目錄：edge/deploy、edge/models、edge/runtime"
    - "edge/runtime/run_edge.sh 可執行、set -euo pipefail、注入 TALKYBUDDY_PIPELINE_PROFILE=edge、以相對定位（非硬編個人絕對路徑）進 proot Debian 後 exec uvicorn 起既有 server.app:app 於 port 8787"
    - "edge/deploy 內 build→push→run 腳本可執行、set -euo pipefail、分階段 echo，對應 adb push 既有 server/ 與 edge/runtime 到裝置 proot rootfs 後執行 launcher"
    - "edge/models/README.md 為 placeholder，說明未來放邊緣專屬量化產物（INT8 tflite / GGUF），且不與既有頂層 models/ 混用"
    - "docs/DEPLOY_EDGE.md 對稱 docs/DEPLOY_CLOUD.md 結構：環境變數表（含 TALKYBUDDY_PIPELINE_PROFILE=edge）、啟動指令（呼叫 run_edge.sh）、adb 部署迴圈、health-check 驗證（僅 server 起來 + 回 health，per D-03）"
  artifacts:
    - edge/README.md
    - edge/runtime/run_edge.sh
    - edge/runtime/README.md
    - edge/deploy/build.sh
    - edge/deploy/push.sh
    - edge/deploy/run.sh
    - edge/deploy/README.md
    - edge/models/README.md
    - docs/DEPLOY_EDGE.md
  key_links:
    - "edge/deploy/push.sh（adb push server/ + edge/runtime）→ 裝置 proot rootfs → edge/deploy/run.sh（adb shell 進 proot 執行 run_edge.sh）→ 既有 server/（uvicorn edge profile, port 8787）"
    - "docs/DEPLOY_EDGE.md §啟動 → edge/runtime/run_edge.sh；§adb 迴圈 → edge/deploy/*.sh"
  prohibitions:
    - "edge/runtime 不得複製 server/ 程式碼（引用既有 server/，避免雙份 server 維護發散，per D-05）"
    - "run_edge.sh 不得硬編個人 home 絕對路徑；須以相對定位或 push 目標路徑決定（per PATTERNS §run_edge.sh）"
    - "edge/models 本 phase 只放 README placeholder，不得放置實際模型檔（實際量化產物 Phase 8/10 才產出，per D-04）"
  assumptions:
    - "[FLAGGED · EDGE-03/EDGE-04 unresolved] spec-less probe 將 EDGE-03、EDGE-04 標為 unclassified/unresolved；本 plan 以 success criteria #3 與 CONTEXT D-01~D-06 為可驗證依據。腳本只驗『可執行 + 語法正確』，真機 adb 實跑一次由 07-03 承接（需硬體）"
    - "[FLAGGED · proot provisioning] proot-distro Debian 內套件安裝沿用既有 scripts/setup_env.sh 之釘版清單（M1 已審），本 phase 以文件描述、不在 repo 引入新/未釘版套件"
---

<objective>
立起邊緣工作的檔案地基（EDGE-04）與 adb 部署管線腳本（EDGE-03）：
1. 建頂層 edge/ 骨架（edge/deploy、edge/models、edge/runtime）。
2. edge/runtime/run_edge.sh launcher：進 proot Debian → 以 TALKYBUDDY_PIPELINE_PROFILE=edge 起既有 server.app:app（引用既有 server/、不複製 code）。
3. edge/deploy build→push→run 最小可跑 adb 腳本（可執行、非空殼）。
4. edge/models/README.md placeholder。
5. docs/DEPLOY_EDGE.md 對稱 docs/DEPLOY_CLOUD.md。

Purpose: success criteria #4 要求 07-03 在真機 adb 跑一次，故 deploy 與 runtime 腳本必須先可執行；此 plan 全為新檔、與 07-01 無檔案衝突，可平行（Wave 1）。
Output: 9 個新檔（骨架 + 腳本 + 文件）。

依 spec-less probe，EDGE-03/EDGE-04 為 unresolved edge（見 frontmatter assumptions）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-CONTEXT.md
@.planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-PATTERNS.md
@docs/DEPLOY_CLOUD.md
@scripts/run.sh
@scripts/setup_env.sh
</context>

<artifacts>
## Artifacts this plan produces
- 新目錄骨架：`edge/`、`edge/deploy/`、`edge/models/`、`edge/runtime/`
- 新檔（launcher）：`edge/runtime/run_edge.sh`（proot → uvicorn edge profile, port 8787）
- 新檔（adb 部署腳本）：`edge/deploy/build.sh`、`edge/deploy/push.sh`、`edge/deploy/run.sh`
- 新檔（文件/placeholder）：`edge/README.md`、`edge/runtime/README.md`、`edge/deploy/README.md`、`edge/models/README.md`
- 新檔（部署文件）：`docs/DEPLOY_EDGE.md`（對稱 docs/DEPLOY_CLOUD.md）
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: 建 edge/ 骨架 + runtime launcher + models placeholder</name>
  <files>edge/README.md, edge/runtime/run_edge.sh, edge/runtime/README.md, edge/models/README.md</files>
  <read_first>
    - scripts/run.sh（set -euo pipefail + exec uvicorn 的 launcher 範式，全文）
    - docs/DEPLOY_CLOUD.md:25-58（TALKYBUDDY_PIPELINE_PROFILE 啟動慣例、edge 段落）
    - server/config.py:131-139（PIPELINE_PROFILE 與 default_network_mode，確認 edge 為預設）
    - .planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-PATTERNS.md（§edge/runtime/run_edge.sh、§edge/models/README.md、No Analog Found）
    - .planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-CONTEXT.md（D-01、D-02、D-04、D-05、D-06）
  </read_first>
  <action>
    建 edge/ 目錄與三子目錄 edge/deploy、edge/models、edge/runtime。
    edge/runtime/run_edge.sh：沿用 scripts/run.sh 的 set -euo pipefail + exec 慣例；export TALKYBUDDY_PIPELINE_PROFILE=edge 後以 venv python exec uvicorn 起 server.app:app --host 0.0.0.0 --port 8787。路徑不可硬編個人 home 絕對路徑（scripts/run.sh 內那種寫死開發者家目錄的形式），改用相對定位（如 `cd "$(dirname "$0")/../.."` 定位到 repo 根或 push 目標 rootfs 路徑）；只針對 Android 14 proot 這一條路徑，不做 dual-host（Yocto native）抽象（YAGNI，D-02）。設定檔案可執行位元（chmod +x）。
    edge/runtime/README.md：說明此 launcher 引用既有 server/、不複製 code（D-05）；標注 proot-distro Debian（glibc）為刻意選擇（與最終 Yocto Linux 一致、降二次移植成本，D-01）；標注不裝 ffmpeg（ALSA 直擷 16k mono WAV，fast path 永遠命中）。
    edge/models/README.md：placeholder，說明未來放邊緣專屬量化產物（INT8 tflite / GGUF），與既有頂層 models/（PC onnx 原型）分離、不混用；實際模型 Phase 8/10 才產出（D-04）。
    edge/README.md：一頁總覽三子目錄用途（deploy=adb 部署腳本、runtime=launcher、models=量化產物 placeholder），指向 docs/DEPLOY_EDGE.md。
  </action>
  <verify>
    <automated>test -d edge/deploy && test -d edge/models && test -d edge/runtime && test -x edge/runtime/run_edge.sh && bash -n edge/runtime/run_edge.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -d edge/deploy && test -d edge/models && test -d edge/runtime` 成立（三子目錄存在）
    - `bash -n edge/runtime/run_edge.sh` 語法檢查通過且檔案有可執行位元（`test -x`）
    - `grep -c 'TALKYBUDDY_PIPELINE_PROFILE=edge' edge/runtime/run_edge.sh` >= 1
    - `grep -c '8787' edge/runtime/run_edge.sh` >= 1（起 server 於既有 port）
    - `grep -Rc '/home/budaedu' edge/runtime/run_edge.sh` == 0（無硬編個人絕對路徑）
    - edge/models/README.md 提及 INT8/tflite/GGUF 且不與 models/ 混用（`grep -Eic 'tflite|gguf' edge/models/README.md` >= 1）
  </acceptance_criteria>
  <done>edge/ 三子目錄骨架就緒；run_edge.sh 可執行、語法正確、注入 edge profile、無硬編個人路徑；models/runtime README placeholder 到位。</done>
</task>

<task type="auto">
  <name>Task 2: edge/deploy adb 腳本（build→push→run）+ docs/DEPLOY_EDGE.md</name>
  <files>edge/deploy/build.sh, edge/deploy/push.sh, edge/deploy/run.sh, edge/deploy/README.md, docs/DEPLOY_EDGE.md</files>
  <read_first>
    - scripts/setup_env.sh:1-30（set -uo pipefail + 分階段 echo + cd 慣例；釘版套件清單作為 proot provisioning 引用來源）
    - scripts/run.sh（exec uvicorn 慣例）
    - docs/DEPLOY_CLOUD.md（全文，對稱結構範本：§1 環境變數表、§2 啟動指令、§3+ 額外章節）
    - edge/runtime/run_edge.sh（Task 1 產出，push 目標與 run 呼叫對象）
    - .planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-PATTERNS.md（§edge/deploy/*.sh、§docs/DEPLOY_EDGE.md 對應章節建議）
    - .planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-CONTEXT.md（D-03 驗證範圍界線、D-06 完成度、canonical_refs 外部 SDK 來源）
  </read_first>
  <action>
    建 edge/deploy 三個可執行腳本（非空殼），皆用 `#!/usr/bin/env bash` + set -euo pipefail + 分階段 echo（沿用 scripts/setup_env.sh 風格），假設於 repo 根目錄執行：
    - build.sh：準備要 push 的載荷（打包/列出 server/ 與 edge/runtime 目錄清單；本 phase 不需編譯 native binary，llama.cpp native build 屬 Phase 8）。
    - push.sh：以 adb push 把既有 server/ 與 edge/runtime 送到裝置 proot rootfs 的目標路徑（目標路徑以變數頂部宣告，可覆寫）。
    - run.sh：以 adb shell 進裝置 → 進 proot Debian → 執行 edge/runtime/run_edge.sh 起 server，並提供 health-check 步驟（curl 裝置上 server 的 health/根路徑）。
    腳本內對「需 G520 SDK 具體燒錄/provisioning 指令」處以 echo/註解標示 TODO 並指向 docs/DEPLOY_EDGE.md 與 ~/hackathon/ G520 SDK（canonical_refs），不臆造 SDK 專屬指令。
    edge/deploy/README.md：說明三腳本用途與執行順序、adb 前置（USB 授權/裝置連線）、proot provisioning 沿用 scripts/setup_env.sh 釘版清單（不引入新套件）。
    docs/DEPLOY_EDGE.md：對稱 docs/DEPLOY_CLOUD.md 章節——§1 環境變數表（含 TALKYBUDDY_PIPELINE_PROFILE=edge，表頭比照 cloud 版 `| 變數 | 用途 | 範例／預設 |`）、§2 啟動指令（呼叫 edge/runtime/run_edge.sh，非直接 uvicorn，因需先進 proot）、§3 adb 部署迴圈（build→push→run 腳本用法）、§4 驗證（D-03 範圍：只驗 server 起來 + health check，不含完整聲音迴路）、§5 proot-distro provisioning（Debian，引用 setup_env.sh 釘版）。
  </action>
  <verify>
    <automated>for f in edge/deploy/build.sh edge/deploy/push.sh edge/deploy/run.sh; do bash -n "$f" || exit 1; test -x "$f" || exit 1; done && test -f docs/DEPLOY_EDGE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n` 對 edge/deploy 三腳本皆語法通過，且三者皆有可執行位元
    - `grep -lc 'set -euo pipefail' edge/deploy/*.sh` 三檔皆命中
    - `grep -c 'adb ' edge/deploy/push.sh` >= 1 且 `grep -c 'adb ' edge/deploy/run.sh` >= 1（部署走 adb）
    - docs/DEPLOY_EDGE.md 含 `TALKYBUDDY_PIPELINE_PROFILE=edge`、`run_edge.sh`、`health`（或健康檢查）三關鍵字（各 `grep -c` >= 1）
    - docs/DEPLOY_EDGE.md 具備 cloud 版對稱的環境變數表頭 `| 變數 | 用途 |`（`grep -c '| 變數 |' docs/DEPLOY_EDGE.md` >= 1）
  </acceptance_criteria>
  <done>edge/deploy build/push/run 三腳本可執行、語法正確、走 adb；DEPLOY_EDGE.md 對稱 cloud 版含 env 表、run_edge.sh 啟動、adb 迴圈、health-check 驗證章節。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → device（adb） | build/push/run 腳本經 adb 把程式碼推到裝置並執行 |
| proot Debian provisioning | 於裝置 proot rootfs 內安裝 Python 相依（沿用既有釘版清單） |

## STRIDE Threat Register（ASVS L1，block on high）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | edge/deploy/push.sh adb push 範圍 | medium | mitigate | push 目標與來源以頂部變數明列（server/ + edge/runtime），set -euo pipefail，不用萬用字元刪除；不 push 秘密/env 檔 |
| T-07-05 | Elevation of Privilege | run_edge.sh 於裝置執行權限 | low | mitigate | 於 proot 非 root 使用者執行，launcher 內不含 sudo；只 exec uvicorn，不改系統設定 |
| T-07-SC | Tampering（supply-chain） | proot Debian apt/pip provisioning | high | mitigate | provisioning 沿用 scripts/setup_env.sh 之 M1 已審釘版清單，不在本 phase 引入新/未釘版套件；DEPLOY_EDGE.md 明列來源，實際模型與 native build 延至 Phase 8 |
</threat_model>

<verification>
- edge/ 三子目錄與 9 個檔存在
- 全部 .sh 通過 `bash -n` 且具可執行位元
- docs/DEPLOY_EDGE.md 對稱 docs/DEPLOY_CLOUD.md 結構且含 health-check（D-03）章節
</verification>

<success_criteria>
- ROADMAP §Phase 7 success criteria #3 達成：edge/ 骨架 + docs/DEPLOY_EDGE.md 已建立、可被 07-03 與後續 phase 直接使用
- deploy/runtime 腳本可執行（非空殼），為 success criteria #4 的真機 adb 跑一次提供可執行載體
</success_criteria>

<output>
Create `.planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-02-SUMMARY.md` when done
</output>
