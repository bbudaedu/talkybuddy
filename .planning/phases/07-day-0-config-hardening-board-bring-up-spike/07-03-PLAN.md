---
phase: 07-day-0-config-hardening-board-bring-up-spike
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - edge/BOARD_BRINGUP_DECISION.md
autonomous: false
requirements: [EDGE-02, EDGE-03]
user_setup:
  - service: hti-g520-board
    why: "Board bring-up spike 與 adb 部署驗證需實體 Hti G520 板卡、USB/adb 連線與（嘗試）Yocto 燒錄工具"
    dashboard_config:
      - task: "準備 Hti G520 板卡：供電、USB 連線、adb 授權；備妥官方 IoT Yocto v25.1 映像與 Genio Tools v1.7+（燒錄嘗試用）"
        location: "實體硬體 + ~/hackathon/ G520 SDK 文件"
must_haves:
  truths:
    - "adb build→push→run 迴圈已在板卡（Android 14 或已燒錄的 Yocto 映像）上完整跑過一次，裝置上 server 起來並回應 health check（僅驗 server 起來 + health，per D-03）"
    - "已產出一份有日期的 go/no-go 決策紀錄檔，記錄 Yocto BSP 燒錄是否成功、後續走 Yocto 或 fallback Android 14，以及 fallback 的新增成本（如 Java/NDK shim）"
    - "決策紀錄反映實測結果（health check 實際 pass/fail、燒錄實際成敗），不得以假設或未驗證狀態偽裝成功"
  artifacts:
    - edge/BOARD_BRINGUP_DECISION.md
  key_links:
    - "07-02 edge/deploy/*.sh + edge/runtime/run_edge.sh → adb 部署到板卡 → health check → 決策紀錄"
    - "燒錄嘗試結果（Yocto go/no-go）→ edge/BOARD_BRINGUP_DECISION.md → 後續 Phase 8/10 邊緣工作的 OS 路徑依據"
  prohibitions:
    - "不靜默偽成功：health check 未實際回應、或燒錄未實際驗證時，不得在決策紀錄標記為成功；需如實記錄失敗與 fallback"
    - "本 phase 只驗 server 起來 + health check；不得擴張到完整聲音迴路 / 前端 loopback（屬 Phase 8，per D-03）"
    - "不預先抽象 Yocto native 啟動路徑（dual-host launcher）；待 Yocto go 才補（YAGNI，per D-02）"
  assumptions:
    - "[FLAGGED · EDGE-02 unresolved] spec-less probe 將 EDGE-02 標為 unclassified/unresolved；go/no-go 準則採 D-11 預設（Yocto time-box ~2 天；成功=能開機+adb 可連+跑得動 stack；未過 fallback Android 14 並記成本）"
    - "[FLAGGED · EDGE-03 unresolved] 真機 adb 一次跑通依賴實體硬體現況；誰按 adb / NB 分攤由執行時視硬體決定（D-11、Discretion）"
    - "[FLAGGED · 硬體可用性] 若 spike 期間板卡不可用，決策紀錄須如實記錄 blocker 與時間盒狀態，不得偽造 pass"
---

<objective>
Board bring-up spike 與部署驗證（EDGE-02、EDGE-03）：
1. 用 07-02 的 adb 部署腳本，在實體 Hti G520 板卡上完整跑一次 build→push→run，驗證裝置上 server 起來並回 health check（範圍嚴格限 D-03）。
2. 對 G520 OS 路徑做出有日期的 go/no-go 決策：嘗試燒官方 IoT Yocto v25.1（Genio Tools v1.7+），~2 天 time-box；未過則 fallback Android 14 並記錄新增成本。
3. 產出 edge/BOARD_BRINGUP_DECISION.md 決策紀錄檔。

Purpose: 讓後續所有邊緣工作有已驗證的 OS 路徑與部署迴圈可站立（success criteria #4、#5）。此 plan 依賴 07-01（硬化後的 server config）與 07-02（部署腳本），且需實體硬體 → 含 human-action / human-verify 檢查點，autonomous=false。

依 spec-less probe，EDGE-02/EDGE-03 為 unresolved edge（見 frontmatter assumptions）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-CONTEXT.md
@.planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-PATTERNS.md
@docs/DEPLOY_EDGE.md
</context>

<artifacts>
## Artifacts this plan produces
- 新檔：`edge/BOARD_BRINGUP_DECISION.md` — 有日期的 go/no-go 決策紀錄（Yocto 燒錄成敗、後續 OS 路徑選擇、fallback 新增成本、adb health-check 實測結果）
</artifacts>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 1: 板卡就緒與 Yocto go/no-go 燒錄嘗試</name>
  <action>人工於實體 Hti G520 板卡執行：連線授權 adb、time-box ~2 天嘗試燒 IoT Yocto v25.1、未過則 fallback Android 14 並記成本、完成 proot-distro Debian provisioning。Claude 無法代按硬體，需人工回報結果後續行。</action>
  <what-built>07-02 已產出 edge/deploy 的 adb build→push→run 腳本與 edge/runtime/run_edge.sh launcher；07-01 已硬化 server config（edge profile n_ctx=512、WAV fast path）。以下步驟需實體 Hti G520 板卡，Claude 無法代按硬體。</what-built>
  <how-to-verify>
    1. 供電並以 USB 連接 Hti G520，`adb devices` 確認裝置已授權可見。
    2.（Yocto 嘗試，time-box ~2 天，per D-11）依 ~/hackathon/ G520 SDK 文件，用 Genio Tools v1.7+ 嘗試燒官方 IoT Yocto v25.1；記錄「能否開機 + adb 可否連 + 能否跑得動我們的 stack」。
    3. 若 Yocto 未過或超時 → 切 fallback Android 14，記錄新增成本（如 proot-distro provisioning、Java/NDK shim 等）。
    4. 於選定 OS（Yocto 或 Android 14 proot）上完成 proot-distro Debian provisioning（沿用 scripts/setup_env.sh 釘版清單），使 server 相依就緒。
    回報：所選 OS 路徑、燒錄成敗、time-box 使用狀況、fallback 新增成本、板卡目前是否可執行 adb 部署。
  </how-to-verify>
  <resume-signal>回報上述結果（含「Yocto go/no-go」與「板卡已就緒可 adb 部署 / 或 blocker」）後輸入 "approved" 繼續</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: adb 部署迴圈跑一次 + health check 實測</name>
  <action>於已就緒板卡執行 edge/deploy build→push→run 一次，對裝置上 server（edge profile, port 8787）做 health check，記錄實際 curl 狀態與啟動 log。不靜默偽成功：未實際回應即記 fail。</action>
  <what-built>在已就緒的板卡上，用 07-02 的 adb 腳本執行一次 build→push→run 部署迴圈並做 health check（範圍嚴格限 D-03：只驗 server 起來 + 回 health，不含聲音迴路 / 前端 loopback）。</what-built>
  <how-to-verify>
    1. 於 repo 根目錄依序執行 `edge/deploy/build.sh`、`edge/deploy/push.sh`、`edge/deploy/run.sh`（或 DEPLOY_EDGE.md §3 所述流程），把既有 server/ 與 edge/runtime 推到裝置 proot rootfs 並以 run_edge.sh 起 server（TALKYBUDDY_PIPELINE_PROFILE=edge, port 8787）。
    2. 對裝置上 server 做 health check：於裝置端（或經 adb forward）curl server 根路徑/健康端點，確認回應（HTTP 可達、非逾時、非 hang）。
    3. 記錄實際 curl 輸出/狀態碼與 server 啟動 log 摘要。
    不靜默偽成功：若 health check 未實際回應，如實記為 fail，不得標成功。
  </how-to-verify>
  <resume-signal>貼上 adb 部署 + health-check 的實際輸出（成功或失敗），輸入 "approved" 或描述問題後繼續</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 寫入有日期的 go/no-go 決策紀錄</name>
  <files>edge/BOARD_BRINGUP_DECISION.md</files>
  <read_first>
    - .planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-CONTEXT.md（D-11 go/no-go 準則、D-03 驗證範圍）
    - .planning/ROADMAP.md:108-118（Phase 7 Goal 與 success criteria #4、#5）
    - .planning/REQUIREMENTS.md（EDGE-02 全文：Yocto v25.1 / Genio Tools v1.7+ / ~2 天 time-box / fallback Android 14 記成本）
    - docs/DEPLOY_EDGE.md（部署迴圈與 health-check 章節，作為紀錄引用）
  </read_first>
  <action>
    依前兩個 checkpoint 的實測回報，建立 edge/BOARD_BRINGUP_DECISION.md，內容須含：
    - 標題 + 決策日期（實際執行當日日期，非臆造）。
    - Yocto 燒錄嘗試結果：能否開機 / adb 可連 / 能否跑得動 stack；time-box（~2 天）使用狀況。
    - go/no-go 決策：後續走 Yocto 或 fallback Android 14；若 fallback，明列新增成本（如 proot-distro provisioning、Java/NDK shim 等）。
    - adb 部署 + health-check 實測結果（實際狀態碼/輸出摘要；成功或失敗如實記）。
    - 對後續 Phase 8/10 的影響（選定 OS 路徑 = 後續邊緣工作地基）。
    如實反映實測；若某項未能實測（如板卡不可用），標記為 blocker 與 time-box 狀態，不得偽裝成功。
  </action>
  <verify>
    <automated>test -f edge/BOARD_BRINGUP_DECISION.md && grep -Eic 'yocto|android 14' edge/BOARD_BRINGUP_DECISION.md && grep -Eic '2026-' edge/BOARD_BRINGUP_DECISION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - edge/BOARD_BRINGUP_DECISION.md 存在
    - 含有日期（`grep -Ec '2026-[0-9]{2}-[0-9]{2}' edge/BOARD_BRINGUP_DECISION.md` >= 1）
    - 含 go/no-go 決策關鍵字與 OS 路徑（`grep -Eic 'yocto' >= 1` 且 `grep -Eic 'android 14' >= 1`）
    - 含 adb / health-check 實測結果段落（`grep -Eic 'health' >= 1`）
    - 若 fallback，含新增成本敘述（`grep -Eic 'shim|成本|ndk|proot' >= 1`）
  </acceptance_criteria>
  <done>有日期的 go/no-go 決策紀錄檔存在，如實記錄 Yocto 燒錄成敗、選定 OS 路徑、fallback 新增成本與 adb health-check 實測結果。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host → device（adb over USB） | 部署指令與程式碼經 adb 通道進入實體板卡並執行 |
| device network → uvicorn 0.0.0.0:8787 | 裝置上 server 綁 0.0.0.0，health check 經裝置網路可達 |

## STRIDE Threat Register（ASVS L1，block on high）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Information disclosure | uvicorn 綁 0.0.0.0:8787 對裝置 LAN 曝露 | medium | mitigate | 僅 demo/開發網路；health 端點不回 PII；DEPLOY_EDGE.md 記錄現場應限制網段/防火牆；本 phase 不對外開放公網 |
| T-07-07 | Tampering | adb push→run 於裝置執行程式碼 | medium | mitigate | 經受信 USB 線與已授權裝置；push 範圍由 07-02 腳本明列；不推秘密/env 檔到裝置 |
| T-07-08 | Denial of Service / Spoofing | health check hang 被誤判為成功（靜默偽成功） | low | mitigate | health check 設逾時、斷言明確回應（狀態碼 + 可達）；決策紀錄要求如實記錄 pass/fail，未實測不得標成功 |
| T-07-SC | Tampering（supply-chain） | proot Debian provisioning 套件安裝 | high | mitigate | 沿用 scripts/setup_env.sh M1 已審釘版清單，不引入新/未釘版套件（同 07-02） |
</threat_model>

<verification>
- checkpoint 1（human-action）：板卡就緒 + Yocto go/no-go 回報完成
- checkpoint 2（human-verify）：adb 部署迴圈跑一次 + health-check 實測回報
- edge/BOARD_BRINGUP_DECISION.md 存在且含日期、OS 路徑、health 實測、fallback 成本
</verification>

<success_criteria>
- ROADMAP §Phase 7 success criteria #4 達成：adb build→push→run 在板卡上完整跑過一次、server 回 health check
- ROADMAP §Phase 7 success criteria #5 達成：有日期的 go/no-go 決策紀錄產出（Yocto vs Android 14 + 新增成本）
- 「不靜默偽成功」prohibition 落實：紀錄如實反映實測，未驗證不標成功
</success_criteria>

<output>
Create `.planning/phases/07-day-0-config-hardening-board-bring-up-spike/07-03-SUMMARY.md` when done
</output>
