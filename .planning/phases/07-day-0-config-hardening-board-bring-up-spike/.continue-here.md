---
context: phase
phase: 07-day-0-config-hardening-board-bring-up-spike
task: 3
total_tasks: 3
status: complete
last_updated: 2026-07-25T16:30:00.000Z
---

# Phase 7 — Complete

<current_state>
Phase 7 = 3/3 plans complete. 07-01（config/音訊技術債）、07-02（edge 骨架 + 部署腳本）、
07-03（board bring-up spike）皆已完成。

**2026-07-25：Checkpoint 1（OS go/no-go）與 checkpoint 2（部署迴圈 + health check 實測）
皆已通過**，取代 2026-07-20 暫定的 Android 14 主線決策——使用者實體燒錄官方 IoT Yocto
成功（`Rity Demo Layer 25.1.1-release scarthgap`），Yocto **GO**，無 fallback 成本。
真機 SSH/rsync 部署迴圈已跑通，health check 取得真實 `HTTP 200` 輸出。完整決策與實測
證據見 `edge/BOARD_BRINGUP_DECISION.md`。
</current_state>

<completed_work>
- 07-01 Config/音訊技術債清償 (EDGE-01) — Done. commits b8c9ae0→8ee2aac。
- 07-02 Edge 骨架 + 部署腳本 (EDGE-03/04) — Done. commits 8175b4d→60017bb。
- 07-03 Board bring-up spike (EDGE-02/03) — Done, 2026-07-25。Yocto 燒錄 GO；SSH/rsync
  部署迴圈 + health check 實測 pass；`edge/BOARD_BRINGUP_DECISION.md` 已產出。
  `edge/deploy/{push,run}.sh`、`edge/runtime/provision_device.sh`、`docs/DEPLOY_EDGE.md`、
  `edge/deploy/README.md` 已從 adb/proot-distro 假設同步改為實測驗證過的 SSH/rsync 路徑。
- out-of-band（07-20，於 checkpoint 2 卡住期間完成）：G1 cloud-TTS consent gate 缺口修復
  （`server/pipeline.py` `_synth_tts`，23 個測試綠燈，`.planning/STATE.md` Known-Gaps 已標記已解）。
</completed_work>

<remaining_work>
無——Phase 7 三個 plan 全部完成。詳見 `07-03-SUMMARY.md`。
</remaining_work>

<decisions_made>
- Worktree 隔離自動降級 sequential(origin/HEAD 無法解析, #683)——執行時全程序列跑,不影響結果。
- 決賽 MVP 主線用 CPU-only LLM(llama.cpp GGUF,即 07-01 硬化的 edge profile);NPU LLM 不排關鍵路徑。
- **[2026-07-25 最終] OS 路徑 = Yocto GO**：官方 IoT Yocto v25.1.1 燒錄成功
  （`Rity Demo Layer 25.1.1-release scarthgap`），推翻 07-20 暫定的 Android 14 主線；
  無需 proot-distro/Java/NDK shim，Yocto 本身即原生 glibc Linux 環境，成本比 Android 14
  fallback 更低。
- **[2026-07-25] 部署傳輸層改 SSH/rsync**：Yocto 板卡無 adb 介面，`edge/deploy/*` 已改寫
  並實測跑通。`edge/runtime/run_edge.sh`（07-02 產物）無需修改即可直接用於 Yocto。
- **[2026-07-25] 裝置端無 gcc/cmake 不構成阻塞**：pip 可直接抓 manylinux aarch64 cp312
  預編譯 wheel（fastapi/uvicorn[standard]/websockets/pydantic 皆已驗證免編譯安裝）。
- **[2026-07-25 安全發現，非阻塞]** 板卡目前 sshd 無驗證機制（root 免密碼/免 key）——已記錄
  於 `edge/BOARD_BRINGUP_DECISION.md` §4，決賽前應補 key-only 登入或限制網段。
</decisions_made>

<blockers>
無。working tree 仍有多筆未提交變更（G1 修復、Android 14 provisioning 腳本、本次 Yocto
board bring-up 相關的 8 個檔案），下次 session 開始前建議先確認 commit 策略。
</blockers>

## Required Reading (in order)
1. `edge/BOARD_BRINGUP_DECISION.md` — Yocto GO 決策 + 真實 SSH/curl 實測輸出
2. `07-03-SUMMARY.md` — Phase 7 收尾摘要
3. `.planning/ROADMAP.md` Phase 8 — 下一步（CPU-only 離線迴路）

## Infrastructure State
- 分支 gsd/2-genio-520-edge-mvp；working tree 有未提交變更，需下次 session 決定 commit 時機。
- 測試須用既有 venv /home/budaedu/hackathon/talkybuddy/.venv(本 repo 無 .venv)。
- 硬體:Genio 520 已燒 Yocto 成功並可透過 Tailscale + SSH 遠端接管（板卡 IP 為 DHCP 配發，
  非固定值，見 `docs/DEPLOY_EDGE.md` `TALKYBUDDY_EDGE_SSH_HOST`）。

## Follow-up TODOs(下次處理)
- 把「NPU LLM 需 MediaTek Online/GAI Toolkit 存取閘(非公開下載)」記進 Phase 10 決策/known-gaps。
- 決賽前補 SSH key-only 登入（見 blockers 上方安全發現）。
- 決定所有未提交變更的 commit 切分方式（建議至少拆：G1 修復 / Android 14→Yocto provisioning
  腳本改寫 / board bring-up 決策與部署腳本 三個 commit）。
- 下一步：Phase 8（CPU-only 離線迴路），依賴本 phase 已確認的 Yocto 地基。

<context>
使用者主動燒板成功並經 Tailscale + SSH 讓 Claude 直接接管，完成真機部署驗證與 health check，
Phase 7 收尾。
</context>

<next_action>
Phase 7 已完成。下一步是 Phase 8（CPU-only 離線迴路，ELOOP-01~04）規劃，或先處理未提交變更的
commit。可執行 `/gsd-plan-phase 8` 或直接請 Claude 規劃 Phase 8。
</next_action>
