---
phase: 10-npu-accelerated-perception
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - edge/npu_spike/__init__.py
  - edge/npu_spike/inspect_model.py
  - edge/npu_spike/fix_shape.py
  - tests/test_npu_spike_tools.py
autonomous: true
requirements: [NPU-01]
must_haves:
  truths:
    - "執行者能在 Genio 520 真機上，用單一指令印出 onnxruntime 版本與 available providers，並看到 NeuronExecutionProvider 是否存在"
    - "執行者能印出 SenseVoice model.int8.onnx 的實際 input 名稱／shape／dtype，以及 ONNX custom metadata map（LFR/CMVN/blank_id 等），不必猜測"
    - "執行者能用固定 argv 產生 shape 固定後的 model.int8.fixed.onnx，不需手改 protobuf"
    - "上述三項的解析邏輯皆為純函式，能在無 NPU 硬體的 dev 機以 pytest 驗證"
  artifacts:
    - path: "edge/npu_spike/inspect_model.py"
      provides: "on-device ORT provider 探測 + ONNX 簽章/metadata dump"
      exports: ["probe_runtime", "format_provider_report", "describe_graph_io", "format_metadata_map", "main"]
    - path: "edge/npu_spike/fix_shape.py"
      provides: "make_dynamic_shape_fixed 固定 argv 包裝"
      exports: ["build_fix_shape_argv", "run_fix_shape", "main"]
    - path: "tests/test_npu_spike_tools.py"
      provides: "兩支診斷工具純函式的單元測試"
  key_links:
    - from: "edge/npu_spike/fix_shape.py"
      to: "onnxruntime.tools.make_dynamic_shape_fixed"
      via: "subprocess.run 固定 argv 串列呼叫，無 shell 字串插值"
      pattern: "make_dynamic_shape_fixed"
    - from: "edge/npu_spike/inspect_model.py"
      to: "server/config.py SENSEVOICE_DIR"
      via: "main() 內 lazy import 取得預設模型路徑"
      pattern: "SENSEVOICE_DIR"
---

<objective>
建立 Phase 10 的「先量測再動手」診斷基座：在真機上以最低成本回答兩個會決定整個 phase 生死的問題——
(1) 這顆已燒錄的 Yocto image 到底有沒有帶 `NeuronExecutionProvider` 的 onnxruntime？
(2) SenseVoice `model.int8.onnx` 的 graph 輸入長什麼樣、動態軸在哪、metadata 帶了哪些特徵前處理參數？

Purpose: RESEARCH.md Assumption A1/A3 與 Open Question 1 全部指向「Day 1 的第一個動作必須是診斷，不是寫引擎」。
沒有這兩份輸出，10-03 的 raw NeuronEP session 只能靠猜測填 shape 與 provider，等於把 D-02 的停損判斷建立在假資料上。

Output: `edge/npu_spike/` 目錄與兩支可在真機獨立執行的診斷腳本，加上其純函式的單元測試。
本計畫**不**建立任何 ONNX session、**不**碰 `server/` 既有程式碼，因此對 Phase 8 CPU 基線零風險。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-npu-accelerated-perception/10-CONTEXT.md
@.planning/phases/10-npu-accelerated-perception/10-RESEARCH.md
@.planning/phases/10-npu-accelerated-perception/10-PATTERNS.md
@edge/runtime/measure_peak_rss.py
@server/config.py
</context>

## Artifacts this phase produces（本計畫負責的部分）

| 類型 | 符號／路徑 | 說明 |
|------|-----------|------|
| 新目錄 | `edge/npu_spike/` | Phase 10 spike 腳本專用目錄（不進 `server/`，與正式 runtime 隔離）|
| 新檔 | `edge/npu_spike/__init__.py` | 讓 `python -m edge.npu_spike.<mod>` 可執行 |
| 新檔 | `edge/npu_spike/inspect_model.py` | 診斷腳本 |
| 函式 | `probe_runtime(ort_module) -> dict` | 回傳 `{"version": str, "providers": list[str], "has_neuron": bool}` |
| 函式 | `format_provider_report(info: dict) -> str` | 人類可讀報表字串（含 PASS/FAIL 判定行）|
| 函式 | `describe_graph_io(graph) -> list[dict]` | 每個 input/output 的 `{"name","shape","dtype","dynamic_dims"}` |
| 函式 | `format_metadata_map(meta: dict) -> str` | ONNX custom metadata 排序後的表格字串 |
| 函式 | `main() -> None` | 唯一觸及真實 I/O 的進入點 |
| 新檔 | `edge/npu_spike/fix_shape.py` | shape 固定包裝 |
| 函式 | `build_fix_shape_argv(...) -> list[str]` | 純函式組 argv |
| 函式 | `run_fix_shape(argv) -> tuple[int, str]` | 固定 argv subprocess 呼叫 |
| 新檔 | `tests/test_npu_spike_tools.py` | 上述純函式測試 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 edge/npu_spike/inspect_model.py — 真機 ORT provider 與 ONNX 簽章診斷</name>

  <files>
    edge/npu_spike/__init__.py,
    edge/npu_spike/inspect_model.py,
    tests/test_npu_spike_tools.py
  </files>

  <read_first>
    - `edge/runtime/measure_peak_rss.py`（必讀，全檔）— 本檔要**逐字沿用**其結構：純函式在上、單一 `main()` 在下、`if __name__ == "__main__": main()` 結尾、模組 docstring 說明「為什麼拆純函式」。
    - `edge/runtime/dump_recent_turns.py` 第 1-40 行 — 沿用其「main() 內 lazy import `server.*`，讓 import 本模組不觸發任何副作用」寫法。
    - `.planning/phases/10-npu-accelerated-perception/10-RESEARCH.md` 的 `## Code Examples` 兩段診斷片段，與 `## Assumptions Log` A1/A3。
    - `server/config.py` 第 1-45 行 — 取得 `MODELS_DIR` / `SENSEVOICE_DIR` 的實際定義。
    - `edge/runtime/provision_device.sh`（必讀，全檔）— 確認裝置端 venv 是以 `python3 -m venv .venv` 建立（**未加** `--system-site-packages`），因此 Yocto 系統層若有內建 onnxruntime，在 venv 內是看不到的。
    - `tests/test_measure_peak_rss.py` — 沿用其測試風格。
  </read_first>

  <behavior>
    - `probe_runtime(ort_module)`：傳入一個具 `__version__` 與 `get_available_providers()` 的物件（真實 onnxruntime 或測試 stub），回傳 `{"version": ..., "providers": [...], "has_neuron": bool}`；`has_neuron` 為 `"NeuronExecutionProvider" in providers`。傳入 `None` 回傳 `{"version": None, "providers": [], "has_neuron": False}`。
    - `format_provider_report(info)`：輸出多行字串，最後一行固定為 `NEURON_EP: PRESENT` 或 `NEURON_EP: ABSENT`，供人眼與 grep 兩用。
    - `describe_graph_io(graph)`：吃 `onnx.ModelProto.graph`（或等價的 stub），每個 input/output 回一個 dict，`dynamic_dims` 列出所有 `dim_param` 非空或 `dim_value == 0` 的軸索引。無 input 時回空 list，不拋例外。
    - `format_metadata_map(meta)`：dict 依 key 排序輸出 `key = value` 每行一筆；空 dict 輸出固定字串 `(no custom metadata)`。
    - 全部四個函式對格式異常輸入回安全預設值，不拋例外（沿用本 repo 診斷工具慣例）。
  </behavior>

  <action>
    先建立空的 `edge/npu_spike/__init__.py`（僅需 `# -*- coding: utf-8 -*-` 與一行 docstring 說明此目錄為 Phase 10 spike 腳本，非正式 runtime）。

    接著建立 `edge/npu_spike/inspect_model.py`，模組 docstring 需寫明三件事：本檔是 NPU-01 Day-1 的第一個動作；純函式／`main()` 分離的理由（比照 `edge/runtime/measure_peak_rss.py`）；以及裝置端 venv 未帶 `--system-site-packages` 這個已知陷阱，故 `main()` 必須把「目前直譯器」的路徑一併印出來，讓人看得出這次量到的是 venv 還是系統 python3。

    實作 behavior 段列出的四個純函式。`main()` 負責：
    1. 印 `sys.executable` 與 `sys.version`。
    2. 以 try/except 包住 `import onnxruntime as ort`；import 失敗時印明確訊息（含「請改用系統 python3 重跑，或以 --system-site-packages 重建 venv」提示）並讓 `probe_runtime(None)` 走空結果路徑，**不得拋例外中止**。
    3. 呼叫 `probe_runtime` + `format_provider_report` 並印出。
    4. 解析 `--model` 參數（`argparse`），預設值由 `main()` 內 lazy import `server.config.SENSEVOICE_DIR` 組出 `SENSEVOICE_DIR / "model.int8.onnx"`；lazy import 失敗則要求使用者顯式給 `--model`。
    5. 以 `onnx.load()` 載入模型，印 `describe_graph_io(model.graph)` 的表格；並以 `onnxruntime.InferenceSession(model_path, providers=["CPUExecutionProvider"])` 取 `get_modelmeta().custom_metadata_map`，交給 `format_metadata_map` 印出。這一步是刻意用 CPU provider 開 session——目的只是讀 metadata，不是測 NPU。RESEARCH.md 指出 sherpa-onnx 的 SenseVoice 匯出檔會把特徵前處理參數放在 custom metadata，預期會看到 `lfr_window_size`、`lfr_window_shift`、`neg_mean`、`inv_stddev`、`blank_id`、`normalize_samples` 這類鍵；請以實際印出的鍵為準記錄，不要假設鍵名。
    6. 每一步都各自 try/except，任一步失敗只印該步的錯誤並繼續下一步——診斷工具的價值在於「盡量多印」，不在於「第一個錯就停」。

    所有子行程呼叫（若有）一律固定 argv 串列，不使用 shell 模式或字串插值（比照 `edge/runtime/measure_peak_rss.py` 模組 docstring 已宣告的慣例）。

    同時在 `tests/test_npu_spike_tools.py` 建立這四個純函式的測試：以 stub 物件（具 `__version__`、`get_available_providers()`）驗證 `probe_runtime` 的 `has_neuron` 兩種結果；以最小 stub graph（具 `input`/`output`、每個元素有 `name` 與 `type.tensor_type.shape.dim`）驗證 `describe_graph_io` 能標出動態軸；驗證 `format_provider_report` 末行在有／無 Neuron 兩種情況下的固定字串；驗證 `format_metadata_map` 空 dict 的固定輸出。測試不得 import 真實 `onnxruntime` 或 `onnx`（dev 機不保證有）。
  </action>

  <acceptance_criteria>
    - `pytest tests/test_npu_spike_tools.py -x` 全綠。
    - `python -c "import edge.npu_spike.inspect_model"` 在**沒有** onnxruntime 的環境下成功（證明無 import 期副作用）。
    - `edge/npu_spike/inspect_model.py` 檔尾存在 `if __name__ == "__main__":` 區塊且呼叫 `main()`。
    - `format_provider_report` 對 `{"has_neuron": True, ...}` 的輸出最後一行等於 `NEURON_EP: PRESENT`；對 `has_neuron=False` 等於 `NEURON_EP: ABSENT`（由測試斷言）。
    - `describe_graph_io` 對含一個 `dim_param="T"` 軸的 stub input，回傳的 `dynamic_dims` 包含該軸索引（由測試斷言）。
    - 測試檔內不 import `onnxruntime` 或 `onnx`：`pytest tests/test_npu_spike_tools.py -x` 在未安裝這兩個套件的環境仍可通過。
  </acceptance_criteria>

  <verify>
    <automated>pytest tests/test_npu_spike_tools.py -x</automated>
    <human-check>在 Genio 520 上分別以系統 `python3` 與 `.venv/bin/python` 執行 `python -m edge.npu_spike.inspect_model`，把兩次的 `NEURON_EP:` 行與 metadata 表格原文貼進 SUMMARY。</human-check>
  </verify>

  <done>
    兩支直譯器下都能跑出 provider 報表；SenseVoice graph 的 input 名稱、shape、動態軸與 custom metadata 皆有原文紀錄，10-03 不需要再猜任何一個欄位。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 edge/npu_spike/fix_shape.py — make_dynamic_shape_fixed 固定 argv 包裝</name>

  <files>
    edge/npu_spike/fix_shape.py,
    tests/test_npu_spike_tools.py
  </files>

  <read_first>
    - `edge/npu_spike/inspect_model.py`（Task 1 產物）— 沿用同一份結構與 docstring 語氣。
    - `edge/runtime/measure_peak_rss.py` 第 69-93 行 `_pgrep_first_pid` — 這是本 repo 的 subprocess 安全呼叫範本：固定 argv、`check=False`、`timeout`、例外只回安全值。
    - `.planning/phases/10-npu-accelerated-perception/10-RESEARCH.md` 的 `### Pattern 2`（含兩種 `make_dynamic_shape_fixed` 呼叫形式與 caveat）。
    - `tests/test_npu_spike_tools.py`（Task 1 產物）— 新測試接在同一檔。
  </read_first>

  <behavior>
    - `build_fix_shape_argv(model_in, model_out, dim_param=None, dim_value=None, input_name=None, input_shape=None) -> list[str]`：
      - 給 `dim_param` + `dim_value` → `[sys.executable, "-m", "onnxruntime.tools.make_dynamic_shape_fixed", "--dim_param", <dim_param>, "--dim_value", str(<dim_value>), <model_in>, <model_out>]`
      - 給 `input_name` + `input_shape` → 以 `--input_name` / `--input_shape` 取代上面兩個旗標，`input_shape` 以逗號串接（例：`1,200,80`）。
      - 兩組都給或都沒給 → 拋 `ValueError`（這是**參數組裝錯誤**，屬程式員錯誤，應該早炸；與「執行期外部失敗要安全降級」是不同情境）。
    - `run_fix_shape(argv) -> tuple[int, str]`：固定 argv 呼叫，回 `(returncode, 合併後的 stdout+stderr)`；OSError／SubprocessError 回 `(-1, 錯誤訊息)`，不拋例外。
  </behavior>

  <action>
    建立 `edge/npu_spike/fix_shape.py`。模組 docstring 需寫明：MediaTek 官方文件明載 Genio NPU 不支援動態 shape，故此步驟是 `NeuronExecutionProvider` 的**硬性前置**而非優化；並警告 caveat——固定 shape 後，餵資料端必須自行 pad/truncate，這個 caller-side 義務在 10-05 由 `server/asr_npu.py` 承擔，本檔只負責產出固定後的模型檔。

    實作 behavior 段兩個函式。`main()` 以 `argparse` 收 `--model-in`、`--model-out`、`--dim-param`、`--dim-value`、`--input-name`、`--input-shape`，組 argv 後執行，印出實際執行的 argv（讓真機輸出可直接貼進 ADR 當證據）與 returncode，成功印 `FIX_SHAPE: OK`、失敗印 `FIX_SHAPE: FAILED`。`--model-out` 預設為 `--model-in` 同目錄下、副檔名前插入 `.fixed` 的路徑（`model.int8.onnx` → `model.int8.fixed.onnx`），這個檔名會被 10-05 的 `NPU_ASR_MODEL_PATH` 預設值引用，兩處必須一致。

    在 `tests/test_npu_spike_tools.py` 追加：`build_fix_shape_argv` 兩種合法形式各自的完整 argv 斷言（含旗標順序與 `input_shape` 逗號串接結果）；兩組參數都給與都不給時各自 `pytest.raises(ValueError)`；`run_fix_shape` 以 monkeypatch 把 `subprocess.run` 換成拋 `OSError` 的假物件，斷言回傳 `(-1, ...)` 而非拋例外。
  </action>

  <acceptance_criteria>
    - `pytest tests/test_npu_spike_tools.py -x` 全綠（含 Task 1 既有測試）。
    - `build_fix_shape_argv("a.onnx", "b.onnx", dim_param="T", dim_value=200)` 回傳的 list 中，`"--dim_param"` 的下一個元素為 `"T"`、`"--dim_value"` 的下一個元素為 `"200"`（字串），且最後兩個元素依序為 `"a.onnx"`、`"b.onnx"`（由測試斷言）。
    - `build_fix_shape_argv("a.onnx", "b.onnx", input_name="x", input_shape=[1, 200, 80])` 回傳中 `"--input_shape"` 的下一個元素為 `"1,200,80"`（由測試斷言）。
    - 同時給 `dim_param` 與 `input_name` 時拋 `ValueError`（由測試斷言）。
    - `run_fix_shape` 在 subprocess 拋 `OSError` 時回傳 tuple 第一項為 `-1`（由測試斷言）。
    - `edge/npu_spike/fix_shape.py` 的 subprocess 呼叫以 list 形式的 argv 傳入且未啟用 shell 模式（人工審閱確認；`run_fix_shape` 的簽章即為 `argv: list[str]`）。
  </acceptance_criteria>

  <verify>
    <automated>pytest tests/test_npu_spike_tools.py -x</automated>
    <human-check>在 Genio 520 上依 Task 1 印出的實際動態軸資訊執行 `python -m edge.npu_spike.fix_shape --model-in models/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17/model.int8.onnx --dim-param &lt;實際軸名&gt; --dim-value 200`，確認產生 `model.int8.fixed.onnx` 並貼上 `FIX_SHAPE:` 那行輸出。</human-check>
  </verify>

  <done>
    可用一行指令、以固定 argv 產出 shape 固定後的 SenseVoice 模型檔，且參數組裝邏輯有測試覆蓋；10-03 不必再手動拼 `make_dynamic_shape_fixed` 指令。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 參數 → subprocess argv | `--model-in` / `--dim-param` 等使用者輸入會被組進外部指令的 argv |
| 磁碟模型檔 → onnx.load / InferenceSession | 任意路徑的 `.onnx` 會被載入並解析 |
| 診斷輸出 → 裝置 console / SUMMARY 文件 | 絕對路徑、模型內部結構會被印出並可能貼進 repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | `fix_shape.run_fix_shape` 的外部指令呼叫 | medium | mitigate | `run_fix_shape(argv: list[str])` 僅接受已組好的 argv 串列並直接傳給 `subprocess.run`，不啟用 shell 模式、不做字串插值；沿用 `edge/runtime/measure_peak_rss.py` 既有慣例 |
| T-10-02 | Tampering | `inspect_model.main()` 的 `--model` 路徑 | low | accept | 本檔為裝置上人工執行的唯讀診斷工具，操作者已具 root SSH 權限，限制路徑不會提高實際安全性；載入行為僅為 `onnx.load` + CPU-only session 讀 metadata，不執行推論。真正需要路徑約束的是 10-05 的正式引擎（見 T-10-05／T-10-07） |
| T-10-03 | Information Disclosure | 診斷輸出含絕對路徑與模型 metadata | low | accept | 輸出僅送到本機 console；貼進 SUMMARY 前由執行者自行判讀。模型本身為公開的 sherpa-onnx release，metadata 不含機密 |
</threat_model>

<verification>
1. `pytest tests/test_npu_spike_tools.py -x` 全綠。
2. `pytest` 全套綠（確認未影響既有 333 項測試）。
3. `python -c "import edge.npu_spike.inspect_model, edge.npu_spike.fix_shape"` 在無 onnxruntime／onnx 的環境成功。
4. 真機執行輸出（provider 報表 + graph 簽章 + metadata map + FIX_SHAPE 行）原文貼進 SUMMARY。
</verification>

<success_criteria>
- 兩支診斷腳本存在、可 import、可 `-m` 執行，且純函式皆有測試。
- 真機上已取得 `NEURON_EP: PRESENT`／`ABSENT` 的明確答案（這是 RESEARCH.md Open Question 1 的直接回答）。
- SenseVoice graph 的實際 input 名稱／動態軸／custom metadata 已有原文紀錄，10-03 與 10-05 可直接引用。
</success_criteria>

<output>
完成後建立 `.planning/phases/10-npu-accelerated-perception/10-01-SUMMARY.md`，務必包含真機兩支直譯器的原始輸出（不要摘要成「有／沒有」，貼原文）。
</output>
