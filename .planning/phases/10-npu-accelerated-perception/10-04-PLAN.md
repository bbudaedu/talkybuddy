---
phase: 10-npu-accelerated-perception
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - edge/npu_spike/ADR-npu-path.md
autonomous: true
requirements: [NPU-01]
user_setup: []
must_haves:
  truths:
    - "repo 內存在一份書面 ADR，說明為什麼選 ORT-NeuronEP 而非 TFLite 轉檔，且結論由 10-03 的真機數字支撐而非推測"
    - "ADR 明文記錄 NDA-gated 路徑（ncc-tflite/DLA、GAI Toolkit）被排除的理由，滿足 NPU-01「排除 NDA-gated 路徑」的字面要求"
    - "ADR 含一行機器可讀的判定標記，讓 10-05 能在開工前以 grep 確認自己該不該被執行"
    - "若 Day-1 檢查點 FAIL，ADR 誠實記錄停損觸發與各需求的實際達成狀態，不粉飾為部分成功"
  artifacts:
    - path: "edge/npu_spike/ADR-npu-path.md"
      provides: "NPU-01 要求的書面決策紀錄，含 Day-1 證據、路徑比較、停損判定與後續範圍指示"
  key_links:
    - from: "edge/npu_spike/ADR-npu-path.md"
      to: "edge/npu_spike/DAY1-EVIDENCE.md"
      via: "ADR 的證據段引用證據檔的 DAY1_NPU_PROBE 行，不重新轉述數字"
      pattern: "DAY1_NPU_PROBE"
    - from: "edge/npu_spike/ADR-npu-path.md"
      to: "server/asr_npu.py（10-05）"
      via: "NPU_PATH_DECISION 標記行作為 10-05 的開工前置條件"
      pattern: "NPU_PATH_DECISION"
---

<objective>
把 10-03 的真機數字轉成 NPU-01 要求的**書面決策**，並讓 D-02 的停損條款產生實際約束力。

Purpose: ROADMAP Phase 10 成功條件 #1 逐字要求「產出書面決策（ADR）：ORT-NeuronEP vs TFLite 轉檔，
排除 NDA-gated 路徑」；成功條件 #5 要求停損點在檢查點未過時真的收斂回 CPU 基線。這兩條都不是程式碼，
是文件與流程約束——但若沒有一個機器可讀的判定標記，在 `auto_advance: true` 的設定下，
NO-GO 的結果會被後續 plan 無聲地輾過去，停損條款形同虛設。本計畫同時解決這兩件事。

ADR 必須在 spike 結果出來後才下筆：RESEARCH.md 已明確指出 TFLite 不是比 ORT-NeuronEP 更便宜的
fallback（兩條路徑共用「在 sherpa-onnx 外重建 fbank 前處理與後處理」的相同負擔，TFLite 還多一段
onnx2tf 轉換與其自身的算子覆蓋失敗模式），所以「Day-1 FAIL 就轉 TFLite」在 1–2 天預算內不成立——
正確動作是直接觸發停損。這個判斷本身就是 ADR 要記錄的核心內容之一。

Output: 一份 `edge/npu_spike/ADR-npu-path.md`。本計畫**不**碰任何程式碼。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-npu-accelerated-perception/10-CONTEXT.md
@.planning/phases/10-npu-accelerated-perception/10-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/10-npu-accelerated-perception/10-03-SUMMARY.md
@edge/npu_spike/DAY1-EVIDENCE.md
</context>

## Artifacts this phase produces（本計畫負責的部分）

| 類型 | 符號／路徑 | 說明 |
|------|-----------|------|
| 新檔 | `edge/npu_spike/ADR-npu-path.md` | NPU-01 的書面決策紀錄 |
| 標記行 | `NPU_PATH_DECISION: GO-ORT-NEURONEP` | Day-1 檢查點 PASS 時的判定標記；10-05 開工前置條件 |
| 標記行 | `NPU_PATH_DECISION: STOP-LOSS-CPU-BASELINE` | Day-1 檢查點 FAIL 時的判定標記；10-05／10-06 不得執行 |
| 章節 | `## 6. 停損與後續範圍` | 兩個分支各自的後續 plan 執行指示與需求達成狀態 |

<tasks>

<task type="auto">
  <name>Task 1: 撰寫 ADR 主體 — 路徑比較、Day-1 證據與決策</name>

  <files>
    edge/npu_spike/ADR-npu-path.md
  </files>

  <read_first>
    - `edge/BOARD_BRINGUP_DECISION.md`（必讀，全檔）— 本 repo 的 ADR 既有格式範本。特別注意：
      標題含 phase 編號、開頭三行是「決策日期／狀態」、每個決策段都附**實測輸出**而非論述、
      並有獨立的「對後續 Phase 的影響」段。本檔要逐項對齊這個骨架。
    - `edge/npu_spike/DAY1-EVIDENCE.md`（10-03 產物，必讀全檔）— 本 ADR 的唯一事實來源。
    - `.planning/phases/10-npu-accelerated-perception/10-03-SUMMARY.md` — 探針的 exit code 與原始 stdout。
    - `.planning/phases/10-npu-accelerated-perception/10-RESEARCH.md` `## Summary` 全文
      （特別是「TFLite 為何不是更便宜的 fallback」那一段）、`## Standard Stack` 的
      `### Alternatives Considered` 表、`## Assumptions Log` A1–A5。
    - `.planning/research/STACK.md` — milestone 層級的 NDA-gated 對照表（GAI Toolkit、
      Neuron SDK All-in-One Bundle / ncc-tflite DLA），本 ADR 的排除理由要引用此表而非重新論證。
    - `.planning/research/PITFALLS.md` Pitfall 3 — 「先試 ORT+NeuronEP」建議的原始出處。
    - `.planning/REQUIREMENTS.md` §NPU-01 原文 — 確認 ADR 涵蓋其字面要求的每一項。
  </read_first>

  <action>
    建立 `edge/npu_spike/ADR-npu-path.md`，骨架比照 `edge/BOARD_BRINGUP_DECISION.md`：

    標題 `# NPU Path Decision — SenseVoice ASR on Genio 520 MDLA 5.3 (Phase 10-04)`，
    其下三行分別為 `**決策日期：**`、`**狀態：**`（GO 或 STOP-LOSS，與 Task 2 的標記一致）、
    `**需求：** NPU-01`。

    `## 1. 決策脈絡` — 用三到五句說明：Phase 8 已交付 CPU-only 基線可退可守；本 phase 為加值軌道；
    D-02 鎖定 1–2 天硬性 time-box 並在 Day 1 結束設檢查點；決賽剩餘天數使 Phase 11/12 的機會成本很高。
    引用 CONTEXT.md D-02 原文一句，不要改寫其語意。

    `## 2. 候選路徑與評估` — 一張表，三個候選各一列，欄位為
    「路徑 | NDA 狀態 | 需重建的工作量 | 本輪是否採用 | 依據」：
    - **ORT + NeuronExecutionProvider**：NDA-free；需在 sherpa-onnx 外重建 fbank 前處理 + CTC 解碼 +
      固定 shape 的 pad/truncate；依據為 RESEARCH.md Summary 與 Pitfall N1。
    - **TFLite（onnx2tf → NP8 Converter → Neuron Stable Delegate）**：NDA-free（D-01 已持有
      NeuroPilot Public 帳號，故帳號不是阻擋因素——這點要明寫，避免日後誤記為「因為沒帳號所以沒做」）；
      工作量為**上一列的全部**再加一段 onnx2tf 轉換與其自身的算子覆蓋失敗模式；依據為 RESEARCH.md
      Summary 的「strictly not-cheaper」推論與 A5。
    - **Neuron SDK All-in-One Bundle（ncc-tflite → .dla → neuronrt）／GAI Toolkit**：NDA-gated
      且無 Python API（僅 C/C++）／Android-only；依 REQUIREMENTS.md Out of Scope 與 CONTEXT.md
      Deferred Ideas 明確排除。此列即為 NPU-01「排除 NDA-gated 路徑」的字面交付物。

    `## 3. Day-1 實測證據` — **不要重新敘述數字**。以引用形式貼入 `DAY1-EVIDENCE.md` 的
    `NEURON_EP:` 行、`FIX_SHAPE:` 行與 `DAY1_NPU_PROBE:` 行原文，並註明完整證據見
    `edge/npu_spike/DAY1-EVIDENCE.md`。同時記錄探針的 exit code。

    `## 4. 決策` — 一段話寫明選了哪條路徑、為什麼，以及**這個決策在什麼條件下會被推翻**
    （例如：若 10-05 實作後量到的加速算子全是 reshape/add 這類瑣碎算子而非 matmul/conv，
    則「NPU 加速」技術上為真但效能無感，屆時應在 10-06 的 SUMMARY 誠實標註，見 Open Question 2）。

    `## 5. 已知風險與未驗證假設` — 條列 RESEARCH.md A1–A5 中在 Day-1 後**仍未被證實或證偽**的項目，
    每項註明目前狀態（已證實／已證偽／仍未知）。A2（provider_options 鍵名）特別重要：
    若 10-03 曾觸發空 options 重試，要記錄哪一種 options 才真的有效。

    寫作紀律：本檔是決策紀錄，不是敘事文件。每個結論後面都要有「依據：<出處>」，出處只能是
    `DAY1-EVIDENCE.md` 的實測輸出、RESEARCH.md 的 CITED 條目、或 CONTEXT.md 的鎖定決策三者之一。
    若某個結論找不到這三類出處，代表它是推測，必須改寫成「未驗證假設」放進第 5 節。
  </action>

  <acceptance_criteria>
    - `test -f edge/npu_spike/ADR-npu-path.md` 成立。
    - 檔案含第 1–5 節：`grep -c '^## [1-5]\.' edge/npu_spike/ADR-npu-path.md` 回傳 5。
    - 第 2 節的候選表同時提及 ORT-NeuronEP、TFLite、以及 NDA-gated 路徑三者：
      `grep -c 'NeuronExecutionProvider' edge/npu_spike/ADR-npu-path.md` ≥ 1、
      `grep -c 'onnx2tf' edge/npu_spike/ADR-npu-path.md` ≥ 1、
      `grep -c 'ncc-tflite' edge/npu_spike/ADR-npu-path.md` ≥ 1（三者皆需，缺一即未滿足 NPU-01 字面要求）。
    - 第 3 節含 `DAY1_NPU_PROBE:` 原文行：`grep -q 'DAY1_NPU_PROBE:' edge/npu_spike/ADR-npu-path.md`。
    - 第 3 節引用的 `DAY1_NPU_PROBE:` 行與 `edge/npu_spike/DAY1-EVIDENCE.md` 內的同名行**逐字相同**
      （人工複核；不同即代表 ADR 竄改了證據）。
    - 第 2 節明寫 D-01（已持有 NeuroPilot Public 帳號）使帳號不構成 TFLite 路徑的阻擋因素。
  </acceptance_criteria>

  <verify>
    <automated>test -f edge/npu_spike/ADR-npu-path.md && [ "$(grep -c '^## [1-5]\.' edge/npu_spike/ADR-npu-path.md)" = "5" ] && grep -q 'DAY1_NPU_PROBE:' edge/npu_spike/ADR-npu-path.md && grep -q 'ncc-tflite' edge/npu_spike/ADR-npu-path.md && grep -q 'onnx2tf' edge/npu_spike/ADR-npu-path.md</automated>
  </verify>

  <done>
    NPU-01 的書面交付物存在，三條候選路徑（含 NDA-gated 排除項）都有明確處置，
    且每個結論都能追溯到實測輸出或 CITED 研究條目。
  </done>
</task>

<task type="auto">
  <name>Task 2: 停損判定標記與後續範圍指示</name>

  <files>
    edge/npu_spike/ADR-npu-path.md
  </files>

  <read_first>
    - `edge/npu_spike/ADR-npu-path.md`（Task 1 產物，全檔）— 本任務追加第 6 節。
    - `.planning/phases/10-npu-accelerated-perception/10-CONTEXT.md` `### 時間預算與 stop-loss` —
      D-02 原文，特別是「立即收斂回 Phase 8 CPU-only 基線，記為 not-attempted 或部分達成」這句。
    - `.planning/ROADMAP.md` `### Phase 10` 的成功條件 #5 原文。
    - `.planning/REQUIREMENTS.md` §NPU-01/02/03 三條原文。
    - `.planning/phases/08-cpu-only-offline-edge-turn-loop/08-CONTEXT.md` — 確認停損時要退回的
      基線究竟是什麼（避免把「停損」寫成需要額外開發的東西；Phase 8 基線本身即為 fallback，
      停損不需任何新工作）。
  </read_first>

  <action>
    在 ADR 追加 `## 6. 停損與後續範圍`。

    本節開頭放一行**機器可讀的判定標記**，獨立成行、前後無其他文字，內容依 Day-1 結果二選一：
    - Day-1 檢查點 PASS（`DAY1_NPU_PROBE:` 為 PASS，即 NeuronExecutionProvider 算子數 > 0）：
      寫 `NPU_PATH_DECISION: GO-ORT-NEURONEP`
    - Day-1 檢查點 FAIL：寫 `NPU_PATH_DECISION: STOP-LOSS-CPU-BASELINE`

    這個標記的判定**完全由 D-02 的客觀條件決定**，不是撰寫者的裁量空間：條件就是
    「per-op placement log 顯示 NPU 執行比例 > 0」。撰寫者的唯一工作是照著 `DAY1_NPU_PROBE:`
    行照抄結論，不得因為「差一點」「再試試看」而改判。

    標記行之後，依實際分支撰寫其中一組內容（另一組不要寫，避免文件同時存在兩套矛盾指示）：

    **若為 `GO-ORT-NEURONEP`：**
    - 條列 10-05／10-06／10-07 各自的範圍，以及各自的前置條件。
    - 明寫 Day-2 的剩餘預算（依 D-02 的 1–2 天硬性 time-box，扣掉 Day-1 已用時間），
      並指出若 Day-2 結束時 10-05 的正式引擎仍未跑通，NPU-03 品質閘（10-07）必須降級為
      「未執行 + 誠實記錄」而非延長 time-box。
    - 記錄三條需求的預期狀態：NPU-01 = Complete（本 ADR 即交付物）、NPU-02 = 待 10-06 驗證、
      NPU-03 = 待 10-07 驗證。

    **若為 `STOP-LOSS-CPU-BASELINE`：**
    - 明寫「10-05 與 10-06 不執行」，並說明理由：依 RESEARCH.md Summary 的 A5 推論，
      TFLite 不是更便宜的 fallback（共用相同的前處理／後處理重建負擔，還多一段轉換），
      因此 Day-1 FAIL 的正確動作是直接停損，**不是**改試 TFLite。
    - 明寫 10-07 降級為「NPU-03 not-attempted 的誠實紀錄」：不做 A/B 聽測（沒有 NPU 路徑可比），
      但仍要留下一份說明為何未做的紀錄。
    - 記錄三條需求的實際狀態：NPU-01 = Complete（決策已做出，「決定不走」也是決策）、
      NPU-02 = Not attempted（附 Day-1 證據）、NPU-03 = Not attempted（依賴 NPU-02）。
    - 明寫「Phase 8 CPU-only 基線不受影響、無需任何回退工作」——因為本 phase 至此未修改
      `server/` 任何既有檔案（10-01/10-02/10-03 皆為新增檔）。這句要作為事實陳述，
      執行者需先以 `git diff --stat` 確認為真再寫。

    最後，無論哪個分支，本節結尾都要有一行 `**簽核：**` 留給開發者填入確認。
  </action>

  <acceptance_criteria>
    - `grep -c '^NPU_PATH_DECISION: ' edge/npu_spike/ADR-npu-path.md` 回傳**恰好 1**
      （兩個標記同時存在或都不存在，皆為失敗——下游 grep 會取到歧義結果）。
    - 該標記行的值為 `GO-ORT-NEURONEP` 或 `STOP-LOSS-CPU-BASELINE` 二者之一：
      `grep -qE '^NPU_PATH_DECISION: (GO-ORT-NEURONEP|STOP-LOSS-CPU-BASELINE)$'` 成立。
    - 標記值與 `edge/npu_spike/DAY1-EVIDENCE.md` 第 4 節的 PASS/FAIL 判定一致
      （PASS↔GO、FAIL↔STOP-LOSS；人工複核，不一致即為停損條款被繞過）。
    - `grep -c '^## 6\.' edge/npu_spike/ADR-npu-path.md` 回傳 1。
    - 第 6 節含三條需求 ID 的狀態陳述：`grep -c 'NPU-0[123]' edge/npu_spike/ADR-npu-path.md` ≥ 3。
    - 若標記為 `STOP-LOSS-CPU-BASELINE`，執行者已實際跑過 `git diff --stat` 確認本 phase
      未修改 `server/` 既有檔案，並將輸出貼進 SUMMARY。
  </acceptance_criteria>

  <verify>
    <automated>[ "$(grep -c '^NPU_PATH_DECISION: ' edge/npu_spike/ADR-npu-path.md)" = "1" ] && grep -qE '^NPU_PATH_DECISION: (GO-ORT-NEURONEP|STOP-LOSS-CPU-BASELINE)$' edge/npu_spike/ADR-npu-path.md && [ "$(grep -c '^## 6\.' edge/npu_spike/ADR-npu-path.md)" = "1" ]</automated>
    <human-check>開發者閱讀 ADR 第 3 節的證據與第 6 節的判定，確認兩者一致且判定符合 D-02 的客觀條件，然後在第 6 節結尾的 `**簽核：**` 填入姓名與日期。這是 ROADMAP 成功條件 #5 停損條款的實際生效點。</human-check>
  </verify>

  <done>
    停損條款從文件裡的一句話變成一個可被 grep 的閘門：10-05 開工前會檢查這個標記，
    NO-GO 時不會有任何後續 plan 悄悄執行下去。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DAY1-EVIDENCE.md → ADR 決策段 | 實測數字在此被轉成對外決策；轉錄失真會汙染整條宣稱鏈 |
| ADR 判定標記 → 後續 plan 的執行與否 | 一行文字決定 10-05/10-06 是否動工，是停損條款的唯一實體 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-12 | Repudiation | ADR 第 3 節的證據轉錄 | high | mitigate | ADR 只允許**引用** `DAY1-EVIDENCE.md` 的原文行，不得改寫數字；驗收條件要求 `DAY1_NPU_PROBE:` 行與證據檔逐字相同（人工複核）；每個結論強制標註「依據：<出處>」且出處限定為實測輸出／CITED 研究條目／鎖定決策三類，無出處者必須降級為第 5 節的未驗證假設 |
| T-10-13 | Tampering | 停損標記被繞過或寫成歧義值 | high | mitigate | 驗收條件以 `grep -c '^NPU_PATH_DECISION: '` 斷言**恰好 1** 行（防止兩個標記並存造成下游 grep 取到任意一個）；值以正則限定為兩個列舉值之一；標記判定綁死 D-02 的客觀條件（NPU 算子數 > 0），撰寫者無裁量空間；10-05 以此標記為開工前置條件（見 10-05 Task 1 驗收條件），使 `auto_advance: true` 無法無聲越過 NO-GO |
</threat_model>

<verification>
1. `grep -c '^## [1-6]\.' edge/npu_spike/ADR-npu-path.md` 回傳 6（六節齊備）。
2. `grep -c '^NPU_PATH_DECISION: ' edge/npu_spike/ADR-npu-path.md` 回傳恰好 1。
3. ADR 第 3 節的 `DAY1_NPU_PROBE:` 行與 `DAY1-EVIDENCE.md` 內同名行逐字相同（人工複核）。
4. `pytest` 全套綠（本計畫不改程式碼，此項用於確認無意外副作用）。
5. 第 6 節 `**簽核：**` 已由開發者填寫。
</verification>

<success_criteria>
- ROADMAP Phase 10 成功條件 #1（書面 ADR，含 NDA-gated 排除）已交付，NPU-01 可標記為 Complete。
- ROADMAP Phase 10 成功條件 #5（停損點）從條款變成可執行的閘門。
- 若判定為 STOP-LOSS，本 phase 至此的產出（診斷工具 + 證據層 + 探針 + ADR）仍是完整且誠實的交付，
  且 Phase 8 CPU-only 基線零損傷、無需任何回退工作。
</success_criteria>

<output>
完成後建立 `.planning/phases/10-npu-accelerated-perception/10-04-SUMMARY.md`。
SUMMARY 開頭第一行必須逐字複製 ADR 的 `NPU_PATH_DECISION:` 標記行——
這是 orchestrator 判斷是否繼續執行 10-05／10-06 的依據。
若判定為 STOP-LOSS，SUMMARY 另需附上 `git diff --stat` 證明 `server/` 既有檔案未被修改。
</output>
