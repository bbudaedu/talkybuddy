# Secrets & credentials
*.key
*.pem
.env
.env.*
credentials.json
token.json

# Python
__pycache__/
*.pyc
.venv/
.venv
venv/
# Streaming venv (A2 real-wiring; built from requirements-streaming.txt)
.venv-streaming/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Model artifacts (fetched at install time by scripts/setup_env.sh; not committed).
# No trailing slash: in git worktrees these may be symlinks to a shared checkout,
# and a dir-only pattern (trailing /) would not match a symlink.
models/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17
models/_sherpa_cache
# Large model binaries — LLM (qwen .gguf) and piper TTS voices (.onnx)
models/*.gguf
models/*.onnx
# espeak-ng 音素化資料（piper-tts 內附，~19MB）。上游授權 GPL-3.0——本 repo
# 為公開 repo，不把它納入版控以免擴大授權汙染面；由 setup_env.sh / 容器
# build 階段各自取得。
models/espeak-ng-data

# A1 wake assets — Porcupine params/keyword models (fetched at verify time, not committed)
web/assets/*.pv
web/assets/*.ppn
# sherpa KWS WASM 資產（fetch-on-setup，勿進版控）
web/vendor/sherpa-kws/

# gstack
.gstack/

# Phase 8 (ELOOP-02): cross-compiled llama.cpp native binaries + cloned
# upstream source — large binaries/repo, never committed; edge/deploy/build.sh
# regenerates both from the official ggml-org/llama.cpp repo.
edge/deploy/bin/
third_party/llama.cpp/

# Runtime SQLite DB — seeded fresh on boot via seed_demo(); never commit demo state
data/*.db
# Runtime logs (server / verify / rehearsal); keep setup.log as install record
logs/*.log
!logs/setup.log
# Spike scratch logs (pron_assess etc.) — throwaway experiment output, never committed
spike/**/*.log

# ── GSD baseline (auto-generated) ──
.gsd
.gsd-worktrees/
.gsd-backups/
.gsd-id
.mcp.json
.bg-shell/
nul
nul.*
con
con.*
prn
prn.*
aux
aux.*
com[1-9]
com[1-9].*
lpt[1-9]
lpt[1-9].*
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace
!.env.example
.next/
dist/
build/
target/
vendor/
*.log
coverage/
.cache/
tmp/

# 註：競賽規範要求 /.kiro 及其子資料夾不得 gitignore（展示 specs/hooks/steering）

# NP8 Converter 專用 Python 3.11 環境（mtk_converter 需 3.11，專案主環境為 3.12）
.venv-np8/

# NP8 SDK 下載物與轉換產物（大檔，從 MediaTek NeuroPilot 取得，不入版控）
edge/np8/*.zip
edge/np8/_extract/
edge/np8/_mobilenet/
edge/np8_out/
# 轉換出來的 tflite 模型（如 dec_T200.tflite，6.6MB）。本 repo 為公開 repo，
# 模型二進位一律由轉換腳本各自產生，不入版控。
edge/np8/*.tflite
