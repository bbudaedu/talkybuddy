# Continue Here — TalkyBuddy（說說學伴）

**Paused:** 2026-07-18 · **Reason:** context exhaustion (~76%)
**Milestone:** 1 — Delivered Baseline (verified) · **Status:** baseline-delivered

---

## Where we are

用 `/gsd-ingest-docs` 把既有專案（30 份設計/計畫文件）接進 GSD 管理。Milestone 1（6 phases）已對照 codebase 驗證為**已交付基線**。下一步是開 **Milestone 2** 規劃新功能。

## Completed this session (all committed)

- `67b8f18` — ingest 30 docs → PROJECT / REQUIREMENTS / ROADMAP / STATE + intel + INGEST-CONFLICTS
- `72daeec` — 6 phases 標記已交付基線 + 5 known-gaps backlog（ROADMAP/STATE）
- `e330dee` / `d344dfa` / `f631b4d` — 建 config.json 並設 `granularity=standard`、`git.branching_strategy=milestone`、`workflow.test_command=pytest`、`response_language=zh-TW`

## Baseline verification result (2026-07-18)

- 🟢 DELIVERED: Phase 4 (Nova Sonic live S2S)、Phase 5 (teaching + pronunciation)
- 🟡 DELIVERED w/ gap: Phase 1、2、3、6 — 見 STATE.md「Known-Gaps Backlog」(G1–G5)

## Known-Gaps Backlog (in STATE.md — 不阻擋新功能)

- **G1 🔴** Phase 3: cloud-TTS 缺 consent 閘（cloud-profile 開機可繞過家長同意）— `server/pipeline.py:321-326`
- **G2 🟠** Phase 2: `run_realwire.py` 漏接 `BargeInGate` → 真機 barge-in 失效 — `server/streaming/run_realwire.py:45-52`
- **G3 🟠** Phase 6: TLS/WSS proxy 僅文件化，無提交設定/VM 實跑驗證
- **G4 🟡** Phase 3: 無原生 Bedrock 後端，僅 relay（by-design 可接受）
- **G5 🟡** Phase 1: SenseVoice→whisper 為手動 flag（符合 ASR-02 原文）

## GSD config state

```
mode                    yolo
granularity             standard
git.branching_strategy  milestone   (base_branch 未設)
workflow.test_command   pytest
response_language        zh-TW
搜尋供應商               全 false（外包給 deep-research / gstack /browse）
```

## Open decisions for next session

1. **Milestone 2 新功能方向**（尚未定義）— 候選：Genio 520 上板移植、家長儀表板、多角色/多語言、離線模式，或先修 backlog G1/G2。
2. **協作分支策略**（使用者自行評估中）— GitHub PR 多人協作下，考慮 `git.branching_strategy=phase` + `git.base_branch=master`；並注意 `.planning/STATE.md` 多人同改的 merge 衝突（可用 `GSD_WORKSTREAM` 隔離）。

## Resume instructions

1. 開新對話（乾淨 context）。
2. 定義 Milestone 2 新功能後執行 **`/gsd-new-milestone`**。
3. 之後照常 `/gsd-plan-phase N` → `/gsd-execute-phase N`。
4. 若要修 backlog 缺口，可把 G1/G2 當成 Milestone 2 的前置 phase。
