# A2 Real-Wiring Slice Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把已在 harness 驗證的 `StreamingTurnManager` barge-in 迴路接上 Pipecat `LocalAudioTransport`（裸麥/喇叭）＋真 Silero VAD，端到端跑通一次真的插話；並落地一個 canned-WAV 自動化驗收與一個獨立 streaming venv。

**Architecture:** 裝置端 Pipecat in-process pipeline：`LocalAudioTransport.input()` → 內建 `SileroVADAnalyzer` 產 VAD edges → `FunASRSTTService`（SenseVoice 批次）→ `TranscriptionFrame` → 現成 `StreamingTurnManager`（`BatchReplySource`）→ 逐句 `TTSSpeakFrame` → `SherpaInterruptibleTTSService` → audio frames → `LocalAudioTransport.output()`。自動化驗收沿用現有 `harness.run_once(no_stt=False)` 用 canned WAV frame 餵完整 VAD→STT→manager→TTS→sink 鏈，不碰真實裝置；手動驗收才起 `LocalAudioTransport`（需 pyaudio + 系統 portaudio）。

**Tech Stack:** Python 3.12、Pipecat 1.5.0、sherpa-onnx（huayan zh VITS）、FunASR/SenseVoiceSmall（批次 STT）、Silero VAD（onnxruntime）、pytest + pytest-asyncio、torch CPU wheel。獨立 venv `talkybuddy/.venv-streaming`。

## Global Constraints

- **只在 `talkybuddy/` 內作業**，全程留在 worktree `/home/budaedu/hackathon/.claude/worktrees/a2-realwire`，勿在共用工作樹切分支（另一 session 正在 master 上做語速工作）。
- **不動這些檔案**：`server/pipeline.py`、`server/app.py`、`_process_text`/`run_turn_audio`、`server/cloud_tts.py`、`server/config.py`。
- **不做**：AEC（留 A2-3）、網路狀態機/離線切換、延遲量測、瀏覽器/WebSocket transport、`SpeechGate`(vad.py) 接入 manager。
- **不改邏輯**：`StreamingTurnManager`、`BatchReplySource`、`SherpaInterruptibleTTSService`、`ReplySource` 介面 — 均現成沿用，本切片只新增組裝層與測試（`sherpa_voice.py` 僅可做 venv-無關的路徑解析微調）。
- **venv 版本比照 spike venv 實測值**（`spike/a2_pipecat/.venv`）：pipecat-ai==1.5.0、torch==2.12.1+cpu、torchaudio==2.11.0+cpu、numpy==2.4.6、onnxruntime==1.24.4、pytest-asyncio==1.4.0、sherpa-onnx==1.13.4、funasr==1.3.14、modelscope==1.38.1。torch/torchaudio 走 CPU index `https://download.pytorch.org/whl/cpu`。
- **模型 cache 唯讀沿用既有位置、不重下**：SenseVoiceSmall 在 `~/.cache/modelscope/models/iic--SenseVoiceSmall/`（跨 worktree 共用，已存在）；sherpa huayan/SenseVoice 大檔在主 repo `/home/budaedu/hackathon/talkybuddy/models/`（本 worktree 因 `.gitignore` 只帶了 `.json`，需 symlink，見 Task 0）。
- **pytest 一律用 `.venv-streaming`**：`talkybuddy/.venv-streaming/bin/python -m pytest ...`，從 `talkybuddy/` 根執行（`PYTHONPATH` 由該根提供 `server.*` import）。

---

## File Structure

新增/修改檔案與職責：

- `talkybuddy/requirements-streaming.txt`（新）— 釘死 streaming 迴路相依版本，建 `.venv-streaming` 用。
- `talkybuddy/.venv-streaming/`（新，不入 git）— 獨立 streaming venv。
- `talkybuddy/server/streaming/tests/sherpa_voice.py`（改）— espeak-ng-data 路徑改為 venv-無關定位（`import piper`），使 `.venv-streaming` 也能載 zh voice。
- `talkybuddy/server/streaming/tests/test_realwire_synth.py`（新）— canned-WAV 餵完整 VAD→STT→manager→TTS→sink 鏈的自動化驗收（判準 A/B）。
- `talkybuddy/server/streaming/run_realwire.py`（新）— 組 `LocalAudioTransport` + Silero VAD + STT + manager + TTS 的可跑 entrypoint（手動真麥驗收），含錯誤處理。
- `talkybuddy/server/streaming/tests/test_run_realwire.py`（新）— `run_realwire.py` 純組裝/錯誤處理的 smoke test（不起真裝置）。
- `talkybuddy/run_tests.sh`（改）— streaming 測試改跑 `.venv-streaming`。
- `talkybuddy/.gitignore`（改，若未涵蓋）— 忽略 `.venv-streaming/`。

**明確不改**：`harness.py`（其 `run_once(no_stt=False)` 已提供完整鏈，Task 3 直接複用，不需改）。若 Task 3 發現需要，才在該 task 內微調並說明。

---

### Task 0: 模型 cache symlink（補齊 worktree 缺失的大檔）

**背景（執行者必讀）**：本 worktree 的 `models/` 因 `.gitignore` 只 checkout 了 `.json` 設定檔，實際模型大檔（`.onnx`/`.gguf`/`_sherpa_cache/`/`sherpa-onnx-sense-voice-.../`）不在。這些是唯讀大檔，主 repo `/home/budaedu/hackathon/talkybuddy/models/` 已有。用 symlink 指過去，避免重下。SenseVoiceSmall 走 `~/.cache/modelscope`（家目錄、已跨 worktree 共用），不需處理。

**Files:**
- 建立 symlink（無檔案內容變更）：`talkybuddy/models/_sherpa_cache`、`talkybuddy/models/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17`、`talkybuddy/models/zh_CN-huayan-medium.onnx`

**Interfaces:**
- Produces：`sherpa_voice.load_zh_voice()` 依賴的 `models/_sherpa_cache/zh_CN-huayan-medium.onnx`＋`.tokens.txt`；`harness._WAV` 依賴的 `models/sherpa-onnx-sense-voice-.../test_wavs/zh.wav`。兩者在 symlink 後於本 worktree 可解析。

- [ ] **Step 1: 確認主 repo 大檔存在、worktree 缺**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
ls -d models/_sherpa_cache models/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17 2>&1  # 預期：No such file
ls -d /home/budaedu/hackathon/talkybuddy/models/_sherpa_cache /home/budaedu/hackathon/talkybuddy/models/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17  # 預期：兩者存在
```
Expected：worktree 缺、主 repo 有。

- [ ] **Step 2: 建 symlink 指向主 repo 唯讀大檔**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
MAIN=/home/budaedu/hackathon/talkybuddy/models
ln -sfn "$MAIN/_sherpa_cache" models/_sherpa_cache
ln -sfn "$MAIN/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17" models/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17
ln -sfn "$MAIN/zh_CN-huayan-medium.onnx" models/zh_CN-huayan-medium.onnx
```

- [ ] **Step 3: 驗證關鍵資產可解析**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
test -f models/_sherpa_cache/zh_CN-huayan-medium.onnx && echo "ONNX OK"
test -f models/_sherpa_cache/zh_CN-huayan-medium.tokens.txt && echo "TOKENS OK"
test -f models/sherpa-onnx-sense-voice-zh-en-ja-ko-yue-2024-07-17/test_wavs/zh.wav && echo "WAV OK"
```
Expected：三行 OK。

- [ ] **Step 4: 確認 symlink 不入 git（`.gitignore` 已忽略 onnx/cache）**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
git status --short models/
```
Expected：無輸出（symlink 目標本就被 `.gitignore` 忽略）。若有輸出，在 `.gitignore` 補上對應 pattern，勿 `git add` 這些 symlink。

- [ ] **Step 5: Commit**

本 task 只建立本機 symlink、無可提交的檔案變更，**略過 commit**。若 Step 4 需動 `.gitignore`，才提交：
```bash
git add .gitignore
git commit -m "chore(a2): ignore worktree-local model symlinks for streaming"
```

---

### Task 1: requirements-streaming.txt + .venv-streaming

**背景**：spec 要求 streaming 迴路獨立於主 `.venv`，落在 `.venv-streaming`。版本比照已驗證的 spike venv。torch/torchaudio 是 CPU wheel（`+cpu` 本地版本標籤），需 CPU index。額外補 `piper-tts`（僅為取用其內建 `espeak-ng-data`，供 sherpa zh voice 的 g2p，見 Task 2）。`pyaudio` 只有 `run_realwire.py` 真麥手動驗收才需要（需系統 portaudio），列為註解／選用，不阻塞自動化驗收。

**Files:**
- Create: `talkybuddy/requirements-streaming.txt`
- Create（產物，不入 git）: `talkybuddy/.venv-streaming/`
- Modify（若需）: `talkybuddy/.gitignore`

**Interfaces:**
- Produces：`.venv-streaming/bin/python` 可 `import pipecat`、`import sherpa_onnx`、`import torch`、`import pytest_asyncio`、`import piper`、`from pipecat.services.funasr.stt import FunASRSTTService`、`from pipecat.audio.vad.silero import SileroVADAnalyzer`。

- [ ] **Step 1: 由 spike venv 導出已驗證版本，作為 requirements 基底**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
/home/budaedu/hackathon/talkybuddy/spike/a2_pipecat/.venv/bin/pip freeze > /tmp/spike-freeze.txt
grep -iE "^(pipecat-ai|sherpa-onnx|torch|torchaudio|numpy|onnxruntime|pytest|pytest-asyncio|funasr|modelscope|scipy)==" /tmp/spike-freeze.txt
```
Expected：印出已釘版本（pipecat-ai==1.5.0、torch==2.12.1+cpu、sherpa-onnx==1.13.4 等）。用此核對下一步手寫的檔案版本；如 spike 版本與下方不符，以 spike 實際值為準。

- [ ] **Step 2: 寫 requirements-streaming.txt**

Create `talkybuddy/requirements-streaming.txt`：
```text
# A2 streaming / barge-in 迴路相依（獨立 .venv-streaming，版本比照 spike venv 實測值）。
# torch/torchaudio 為 CPU wheel，需 CPU index。
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Pipecat 串流框架 + 批次 SenseVoice STT plugin ---
pipecat-ai[funasr]==1.5.0

# --- 可中止 sherpa TTS ---
sherpa-onnx==1.13.4

# --- SenseVoice / FunASR 後端（批次 STT 真模型）---
funasr==1.3.14
modelscope==1.38.1

# --- 推論 runtime ---
torch==2.12.1+cpu
torchaudio==2.11.0+cpu
onnxruntime==1.24.4
numpy==2.4.6
scipy==1.18.0

# --- sherpa zh voice 的 espeak-ng-data 來源（僅取資料檔，g2p 用）---
piper-tts

# --- 測試 ---
pytest==9.1.1
pytest-asyncio==1.4.0

# --- 真麥手動驗收（run_realwire.py）才需要；需先裝系統 portaudio：
#     sudo apt install portaudio19-dev
#     然後手動 pip install：.venv-streaming/bin/pip install pyaudio
# 不放進本檔以免無 portaudio 的 CI 環境安裝失敗、阻塞自動化驗收。
# pyaudio
```

- [ ] **Step 3: 建 .venv-streaming 並安裝**

Run（下載量大、耗時數分鐘，屬預期）：
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
python3 -m venv .venv-streaming
.venv-streaming/bin/pip install --upgrade pip
.venv-streaming/bin/pip install -r requirements-streaming.txt
```
Expected：安裝成功結束、無 dependency resolver 錯誤。若 torch `+cpu` 版本 resolver 抱怨，確認 `--extra-index-url` 生效；如仍失敗，改用 spike venv 完整 freeze（`pip install -r /tmp/spike-freeze.txt` 再補 `piper-tts`）。

- [ ] **Step 4: 冒煙驗證關鍵 import**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
.venv-streaming/bin/python - <<'PY'
import pipecat, sherpa_onnx, torch, pytest_asyncio, piper
from pipecat.services.funasr.stt import FunASRSTTService
from pipecat.audio.vad.silero import SileroVADAnalyzer
print("pipecat", pipecat.__version__ if hasattr(pipecat,"__version__") else "ok")
print("sherpa", sherpa_onnx.__file__ is not None)
print("torch", torch.__version__)
print("imports OK")
PY
```
Expected：印出版本與 `imports OK`，無 traceback。

- [ ] **Step 5: 確認 .venv-streaming 不入 git**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
git check-ignore .venv-streaming && echo "IGNORED" || echo "NEEDS IGNORE"
```
若印 `NEEDS IGNORE`，在 `talkybuddy/.gitignore` 加一行 `.venv-streaming/`。

- [ ] **Step 6: Commit**

```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
git add requirements-streaming.txt .gitignore
git commit -m "build(a2): pin streaming deps + .venv-streaming (requirements-streaming.txt)"
```

---

### Task 2: sherpa_voice.py — espeak-ng-data 改 venv-無關定位

**背景**：`server/streaming/tests/sherpa_voice.py:15` 目前把 `espeak-ng-data` 硬編在主 `.venv` 路徑（`_TB_ROOT/".venv"/...`）。本 worktree 用 `.venv-streaming`，該硬編路徑不存在 → `load_zh_voice()` 會 `FileNotFoundError`。改用 `import piper; Path(piper.__file__).parent/"espeak-ng-data"` 動態定位（與 venv 名稱無關，已驗證 piper 套件內含該資料夾），並保留舊路徑為 fallback（不破壞主 repo 既有跑法）。

**Files:**
- Modify: `talkybuddy/server/streaming/tests/sherpa_voice.py`
- Test: `talkybuddy/server/streaming/tests/test_sherpa_voice_locate.py`（新）

**Interfaces:**
- Consumes：`import piper`（Task 1 已裝）。
- Produces：`load_zh_voice()` 在 `.venv-streaming` 下成功回傳 `sherpa_onnx.OfflineTts` 實例（供 Task 3/4 的 `SherpaInterruptibleTTSService`）。

- [ ] **Step 1: 寫失敗測試**

Create `talkybuddy/server/streaming/tests/test_sherpa_voice_locate.py`：
```python
"""在 .venv-streaming 下，espeak-ng-data 定位需 venv-無關，load_zh_voice() 可成功。"""
from pathlib import Path

from server.streaming.tests import sherpa_voice


def test_espeak_data_dir_resolves_in_current_venv():
    p = sherpa_voice._espeak_data_dir()
    assert p is not None
    assert Path(p).is_dir()
    assert (Path(p) / "phontab").exists() or any(Path(p).iterdir())


def test_load_zh_voice_succeeds():
    voice = sherpa_voice.load_zh_voice()
    assert voice is not None
    assert int(voice.sample_rate) > 0
```

- [ ] **Step 2: 跑測試確認失敗**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
.venv-streaming/bin/python -m pytest server/streaming/tests/test_sherpa_voice_locate.py -v
```
Expected：FAIL — `AttributeError: module ... has no attribute '_espeak_data_dir'`（函式尚未定義）。

- [ ] **Step 3: 改 sherpa_voice.py**

在 `talkybuddy/server/streaming/tests/sherpa_voice.py` 把 `_ESPEAK = ...` 那行（第 15 行）替換為一個容錯定位函式。改動後檔案的相關區塊：
```python
# server/streaming/tests/ 往上三層 = talkybuddy/
_TB_ROOT = Path(__file__).resolve().parents[3]
_CACHE = _TB_ROOT / "models" / "_sherpa_cache"
_ONNX = _CACHE / "zh_CN-huayan-medium.onnx"
_TOKENS = _CACHE / "zh_CN-huayan-medium.tokens.txt"


def _espeak_data_dir() -> str | None:
    """venv-無關定位 espeak-ng-data：先問已安裝的 piper 套件，再退回主 .venv 舊路徑。"""
    try:
        import piper  # piper-tts 套件內含 espeak-ng-data
        cand = Path(piper.__file__).parent / "espeak-ng-data"
        if cand.is_dir():
            return str(cand)
    except Exception:
        pass
    legacy = _TB_ROOT / ".venv" / "lib" / "python3.12" / "site-packages" / "piper" / "espeak-ng-data"
    return str(legacy) if legacy.is_dir() else None
```
並把 `load_zh_voice()` 內對 `_ESPEAK` 的使用改為呼叫 `_espeak_data_dir()`。改後 `load_zh_voice()` 全文：
```python
def load_zh_voice():
    """載入 huayan 中文 sherpa OfflineTts；任何檔缺則 raise FileNotFoundError。"""
    import sherpa_onnx

    espeak = _espeak_data_dir()
    if espeak is None:
        raise FileNotFoundError("espeak-ng-data not found (install piper-tts)")
    for p in (_ONNX, _TOKENS, Path(espeak)):
        if not p.exists():
            raise FileNotFoundError(f"sherpa asset missing: {p}")

    vits = sherpa_onnx.OfflineTtsVitsModelConfig(
        model=str(_ONNX), tokens=str(_TOKENS), data_dir=espeak, lexicon=""
    )
    model_cfg = sherpa_onnx.OfflineTtsModelConfig(vits=vits, num_threads=1, provider="cpu")
    tts_cfg = sherpa_onnx.OfflineTtsConfig(model=model_cfg)
    if not tts_cfg.validate():
        raise RuntimeError("invalid sherpa-onnx tts config")
    return sherpa_onnx.OfflineTts(tts_cfg)
```
（刪除舊的模組級 `_ESPEAK = ...` 賦值行。）

- [ ] **Step 4: 跑測試確認通過**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
.venv-streaming/bin/python -m pytest server/streaming/tests/test_sherpa_voice_locate.py -v
```
Expected：2 passed。

- [ ] **Step 5: 回歸現有 streaming 測試（確認未破壞）**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
.venv-streaming/bin/python -m pytest server/streaming/tests/test_interruptible_synth.py server/streaming/tests/test_turn_manager.py -q
```
Expected：全 pass（這些會用到 `load_zh_voice()` / sherpa）。若 `test_turn_manager.py` 因需 SenseVoice 而慢，屬正常。

- [ ] **Step 6: Commit**

```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
git add server/streaming/tests/sherpa_voice.py server/streaming/tests/test_sherpa_voice_locate.py
git commit -m "fix(a2): locate espeak-ng-data venv-agnostically for .venv-streaming"
```

---

### Task 3: test_realwire_synth.py — canned-WAV 完整鏈自動化驗收（判準 A/B）

**背景**：把現有 `harness.run_once(..., no_stt=False)`（已組 `FunASRSTTService`→`StreamingTurnManager`→`SherpaInterruptibleTTSService`→`OutputSink` 完整鏈、用 canned `zh.wav`）升級為正式自動化驗收。判準 A＝有回覆（sink 收到 ≥1 個 `TTSAudioRawFrame` 且 STT 產非空 `asr_text`）；判準 B＝barge-in 淨停（第一次回覆合成期間注入第二次 VAD 開口 → sink 實際合成句數 ≤2，對照無插話）。sink 用 `TTSAudioRawFrame` 計 `frame_count`（每句一個 audio frame）。這條路徑不需真實裝置、不需 pyaudio。

**Files:**
- Create: `talkybuddy/server/streaming/tests/test_realwire_synth.py`
- 沿用（不改）: `talkybuddy/server/streaming/harness.py`（`run_once`、`OutputSink`、`BargeInDriver`）、`batch_reply_source.py`

**Interfaces:**
- Consumes：`harness.run_once(reply_source, *, barge_in: bool, no_stt: bool=True) -> (OutputSink, StreamingTurnManager)`；`OutputSink.frame_count`（int，TTSAudioRawFrame 數）；`StreamingTurnManager.result.asr_text`（str）。`BatchReplySource()` 預設注入 `EdgeLLM()`（無模型時 `generate` 回 None → 降級用 scaffold 文字，仍會有多句）。
- Produces：可 CI 化的 `test_realwire_synth.py`，兩個判準測試。

**注意（spec 風險段 §「完整 pipeline 邊界」）**：若真跑 `FunASRSTTService` 在本環境對注入 frame 行為異常（例如 SenseVoice cache 未就緒），退回 spec 允許的妥協 — 在 STT 之後注入 `TranscriptionFrame`（即 `no_stt=True`），並在測試 docstring 註記此妥協。判準 B 的 barge-in 淨停在 `no_stt=True` 下仍有效（harness `BargeInDriver` 走 TTSSpeakFrame→UPSTREAM VAD 回注，與 STT 無關）。

- [ ] **Step 1: 先確認 SenseVoice 真 STT 在本環境可跑（決定 no_stt 值）**

Run（一次性探查，非測試）：
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
PYTHONPATH=$(pwd) .venv-streaming/bin/python - <<'PY'
import asyncio
from server.streaming import harness
from server.streaming.reply_source import StubReplySource
sink, mgr = asyncio.run(harness.run_once(StubReplySource(), barge_in=False, no_stt=False))
print("asr_text=", repr(mgr.result.asr_text), "frames=", sink.frame_count)
PY
```
Expected：`asr_text` 非空（如「开放时间早上9点至下午5点。」）、`frames` ≥ 1。若 `asr_text` 空或初始化報 SenseVoice cache 缺 → 走妥協路徑（下方測試改 `no_stt=True` 並註記）。記下本步結果決定 `USE_STT` 布林。

- [ ] **Step 2: 寫判準 A/B 測試（先失敗）**

Create `talkybuddy/server/streaming/tests/test_realwire_synth.py`：
```python
"""A2 real-wiring 自動化驗收：canned zh.wav 餵完整 VAD→STT→manager→TTS→sink 鏈。

判準 A（有回覆）：餵一段語音 → sink 收到 ≥1 個合成音訊 frame，且 STT 產非空 asr_text。
判準 B（barge-in 淨停）：回覆合成期間注入第二次 VAD 開口 → sink 實際合成句數 ≤2，
                         且明顯少於無插話時的完整句數。二元淨判，不量延遲。

不依賴真實音訊裝置（用 InputAudioRawFrame 餵 canned WAV，同 harness）。

USE_STT：若本環境 SenseVoice 真 STT 可用則走完整鏈（no_stt=False）；否則退回 spec
允許的妥協 — 在 STT 之後注入 TranscriptionFrame（no_stt=True），barge-in 淨停仍有效。
"""
import pytest

from server.streaming import harness
from server.streaming.batch_reply_source import BatchReplySource

# 依 Task 3 Step 1 探查結果設定：SenseVoice 真 STT 可用＝True；否則 False（見 docstring 妥協）。
USE_STT = True


@pytest.mark.asyncio
async def test_criterion_a_has_reply():
    sink, mgr = await harness.run_once(
        BatchReplySource(), barge_in=False, no_stt=not USE_STT
    )
    assert sink.frame_count >= 1, "sink 應收到至少一個合成音訊 frame"
    if USE_STT:
        assert mgr.result.asr_text.strip(), "STT 應產出非空 asr_text"


@pytest.mark.asyncio
async def test_criterion_b_barge_in_clean_stop():
    full, _ = await harness.run_once(
        BatchReplySource(), barge_in=False, no_stt=not USE_STT
    )
    cut, mgr = await harness.run_once(
        BatchReplySource(), barge_in=True, no_stt=not USE_STT
    )
    assert cut.frame_count <= 2, f"barge-in 後合成句數應 ≤2，實得 {cut.frame_count}"
    assert cut.frame_count < full.frame_count, (
        f"barge-in（{cut.frame_count}）應明顯少於完整（{full.frame_count}）"
    )
    assert "barge_in" in mgr.result.state_events
```
若 Step 1 判定走妥協，把 `USE_STT = True` 改成 `USE_STT = False`，並在 module docstring 補一行「本環境 SenseVoice 不可用，走 TranscriptionFrame 妥協」。

- [ ] **Step 3: 跑測試（可能先失敗）**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
PYTHONPATH=$(pwd) .venv-streaming/bin/python -m pytest server/streaming/tests/test_realwire_synth.py -v
```
Expected：若 `BatchReplySource` 降級後仍 ≥3 句，判準 B 的 `full` 應有多句、`cut` ≤2 → PASS。若判準 B 的 `full.frame_count` 太少（降級 scaffold 只有 1–2 句，導致 `cut < full` 不成立），進 Step 4 修正。

- [ ] **Step 4: 若降級回覆句數不足 → 顯式注入多句 stub 保證有句界可打斷**

若 Step 3 判準 B 失敗（`full.frame_count` ≤2，barge-in 前就合成完），改用保證多句的 reply source：把兩個測試的 `BatchReplySource()` 換成
```python
from server.streaming.reply_source import StubReplySource
# StubReplySource 預設 yield 4 句中文，確保有足夠句界觀察 barge-in 淨停
```
判準 A 仍可用 `BatchReplySource()`（驗真降級鏈有回覆），判準 B 用 `StubReplySource()`（驗 barge-in 機制）。修改後重跑 Step 3 指令，Expected：2 passed。在測試註記「判準 B 用 StubReplySource 確保多句界」。

- [ ] **Step 5: Commit**

```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
git add server/streaming/tests/test_realwire_synth.py
git commit -m "test(a2): canned-WAV full-chain acceptance (reply + barge-in clean stop)"
```

---

### Task 4: run_realwire.py — 真麥 entrypoint + 錯誤處理

**背景**：手動真麥驗收 entrypoint。組 `LocalAudioTransport`（裸麥/喇叭）→ 內建 `SileroVADAnalyzer` → `FunASRSTTService` → `StreamingTurnManager(BatchReplySource())` → `SherpaInterruptibleTTSService` → transport output。`LocalAudioTransport` 需 `pyaudio`（`pipecat-ai[local]`）＋系統 portaudio；spec 錯誤處理要求：缺音訊裝置/缺 pyaudio → 捕捉並印明確訊息後 exit（非 traceback）；SenseVoice cache 缺 → 明確報「需先備妥 SenseVoiceSmall cache」。把「組 pipeline」與「起 transport」拆成可單元測的函式，真裝置啟動只在 `main()`。

**Pipecat 1.5.0 已驗證 API**（供實作，勿臆測）：
- `from pipecat.transports.local.audio import LocalAudioTransport, LocalAudioTransportParams`（import 時若無 pyaudio 會 raise `ImportError("Missing module: No module named 'pyaudio'")`）。
- `from pipecat.audio.vad.silero import SileroVADAnalyzer`；`SileroVADAnalyzer(sample_rate=None, params=VADParams|None)`。
- `LocalAudioTransportParams(TransportParams)`：`audio_in_enabled`、`audio_out_enabled`、`vad_analyzer`（掛 VAD 的欄位，繼承自 `TransportParams`）。
- `LocalAudioTransport(params)` 提供 `.input()`（`BaseInputTransport`）與 `.output()`（`BaseOutputTransport`）processor。
- `PipelineTask(Pipeline([...]))` + `PipelineRunner().run(task)`（同 harness）。

**Files:**
- Create: `talkybuddy/server/streaming/run_realwire.py`
- Test: `talkybuddy/server/streaming/tests/test_run_realwire.py`

**Interfaces:**
- Consumes：`StreamingTurnManager`、`BatchReplySource`、`SherpaInterruptibleTTSService`（現成）；`LocalAudioTransport`、`SileroVADAnalyzer`（pipecat）。
- Produces：
  - `check_prerequisites() -> list[str]`：回傳缺項訊息列表（空＝就緒）。檢查 pyaudio 可 import、SenseVoice cache 目錄存在、sherpa 資產存在。
  - `build_processors(transport) -> list`：回傳 `[transport.input(), stt, manager, tts, transport.output()]` 的 processor 列表（不啟動）。
  - `main() -> int`：組 transport＋pipeline 跑 runner；缺前置時印訊息回非 0，正常結束回 0。

- [ ] **Step 1: 寫 smoke test（先失敗）**

Create `talkybuddy/server/streaming/tests/test_run_realwire.py`：
```python
"""run_realwire.py 純組裝/錯誤處理 smoke test（不起真實音訊裝置）。"""
from server.streaming import run_realwire


def test_check_prerequisites_returns_list():
    missing = run_realwire.check_prerequisites()
    assert isinstance(missing, list)
    # 每個缺項需是可讀字串（非 traceback）
    assert all(isinstance(m, str) and m for m in missing)


def test_build_processors_shape():
    # 用假 transport（提供 input()/output() 回傳可辨識 sentinel），驗組裝順序不 crash、
    # 不需真裝置或 pyaudio。
    class _FakeProc:
        def __init__(self, tag):
            self.tag = tag

    class _FakeTransport:
        def input(self):
            return _FakeProc("input")
        def output(self):
            return _FakeProc("output")

    procs = run_realwire.build_processors(_FakeTransport())
    assert len(procs) == 5
    assert procs[0].tag == "input"
    assert procs[-1].tag == "output"
    # 中間三個＝STT、manager、TTS（型別非 None 即可）
    assert all(p is not None for p in procs)
```

- [ ] **Step 2: 跑測試確認失敗**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
PYTHONPATH=$(pwd) .venv-streaming/bin/python -m pytest server/streaming/tests/test_run_realwire.py -v
```
Expected：FAIL — `ModuleNotFoundError: No module named 'server.streaming.run_realwire'`。

- [ ] **Step 3: 寫 run_realwire.py**

Create `talkybuddy/server/streaming/run_realwire.py`：
```python
"""A2 real-wiring 真麥 entrypoint：把 StreamingTurnManager barge-in 迴路接上
LocalAudioTransport（裸麥/喇叭）＋真 Silero VAD，端到端跑一次真的插話。

手動驗收（spec 2026-07-10-a2-realwire）：
  1. 講一句 → 聽到回覆（二元：有/無）。
  2. 回覆播到一半插話 → 回覆在句界停下、系統轉去聽新輸入（二元：乾淨停/沒停）。

前置：pyaudio（`pipecat-ai[local]`，需系統 portaudio：sudo apt install portaudio19-dev；
      再 .venv-streaming/bin/pip install pyaudio）、SenseVoiceSmall cache、sherpa 資產。
無 AEC（留 A2-3）：外放時 TTS 可能被麥收回誤觸發 barge-in → 用耳機或 PTT-lite 規避。
"""
from __future__ import annotations

import sys
from pathlib import Path

from server.streaming.turn_manager import StreamingTurnManager
from server.streaming.batch_reply_source import BatchReplySource
from server.streaming.interruptible_tts import SherpaInterruptibleTTSService
from server.streaming.tests import sherpa_voice

_TB_ROOT = Path(__file__).resolve().parents[2]
_SENSEVOICE_CACHE = Path.home() / ".cache" / "modelscope" / "models" / "iic--SenseVoiceSmall"


def check_prerequisites() -> list[str]:
    """回傳缺項的可讀訊息（空 list＝就緒）。不 raise。"""
    missing: list[str] = []
    try:
        import pyaudio  # noqa: F401
    except Exception:
        missing.append(
            "缺 pyaudio（真麥/喇叭需要）：sudo apt install portaudio19-dev；"
            "再 .venv-streaming/bin/pip install pyaudio"
        )
    if not _SENSEVOICE_CACHE.is_dir():
        missing.append(f"缺 SenseVoiceSmall cache：{_SENSEVOICE_CACHE}（需先備妥）")
    if sherpa_voice._espeak_data_dir() is None:
        missing.append("缺 espeak-ng-data（install piper-tts）")
    if not sherpa_voice._ONNX.exists():
        missing.append(f"缺 sherpa zh onnx：{sherpa_voice._ONNX}")
    return missing


def build_processors(transport) -> list:
    """組裝 processor 列表（不啟動裝置）。transport 需提供 input()/output()。"""
    from pipecat.services.funasr.stt import FunASRSTTService

    stt = FunASRSTTService()
    manager = StreamingTurnManager(BatchReplySource())
    tts = SherpaInterruptibleTTSService()
    return [transport.input(), stt, manager, tts, transport.output()]


def _build_transport():
    """建 LocalAudioTransport（真裝置）；import/裝置失敗會 raise，由 main() 捕捉。"""
    from pipecat.transports.local.audio import (
        LocalAudioTransport,
        LocalAudioTransportParams,
    )
    from pipecat.audio.vad.silero import SileroVADAnalyzer

    params = LocalAudioTransportParams(
        audio_in_enabled=True,
        audio_out_enabled=True,
        vad_analyzer=SileroVADAnalyzer(),
    )
    return LocalAudioTransport(params)


async def _run() -> int:
    from pipecat.pipeline.pipeline import Pipeline
    from pipecat.pipeline.task import PipelineTask
    from pipecat.pipeline.runner import PipelineRunner

    transport = _build_transport()
    task = PipelineTask(Pipeline(build_processors(transport)))
    print("[run_realwire] 就緒：對麥克風講話；回覆播放中再開口即測 barge-in。Ctrl-C 結束。")
    await PipelineRunner().run(task)
    return 0


def main() -> int:
    missing = check_prerequisites()
    if missing:
        print("[run_realwire] 無法啟動，缺少前置：", file=sys.stderr)
        for m in missing:
            print("  - " + m, file=sys.stderr)
        return 1
    import asyncio

    try:
        return asyncio.run(_run())
    except KeyboardInterrupt:
        print("\n[run_realwire] 已結束。")
        return 0
    except Exception as exc:  # 真裝置/pyaudio 執行期錯誤 → 明確訊息、非 traceback
        print(f"[run_realwire] 啟動音訊裝置失敗：{exc}", file=sys.stderr)
        return 1


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: 跑 smoke test 確認通過**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
PYTHONPATH=$(pwd) .venv-streaming/bin/python -m pytest server/streaming/tests/test_run_realwire.py -v
```
Expected：2 passed。（`build_processors` 用 fake transport，不觸 pyaudio；`check_prerequisites` 回 list。）

- [ ] **Step 5: 驗證缺 pyaudio 時 main() 優雅報錯（非 traceback）**

若 `.venv-streaming` 尚未裝 pyaudio（預設如此），Run：
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
PYTHONPATH=$(pwd) .venv-streaming/bin/python -m server.streaming.run_realwire; echo "exit=$?"
```
Expected：印「無法啟動，缺少前置」＋ pyaudio 提示、`exit=1`，**無 Python traceback**。

- [ ] **Step 6: Commit**

```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
git add server/streaming/run_realwire.py server/streaming/tests/test_run_realwire.py
git commit -m "feat(a2): run_realwire.py real-mic entrypoint + graceful prerequisite errors"
```

---

### Task 5: run_tests.sh 更新指向 .venv-streaming + 全套回歸

**背景**：目前 `run_tests.sh` 的 streaming 段指向 spike venv（`spike/a2_pipecat/.venv`）。spec 要求改指 `.venv-streaming`。主 `.venv` 在本 worktree 不存在（gitignore、未建），故主套件段在本 worktree 可能無法跑 — 保留其邏輯但讓缺 venv 時明確 SKIP，不讓腳本整體失敗。

**Files:**
- Modify: `talkybuddy/run_tests.sh`

**Interfaces:**
- Produces：`./run_tests.sh` 用 `.venv-streaming` 跑 `server/streaming/tests`（含本切片新測試）。

- [ ] **Step 1: 改 run_tests.sh streaming 段指向 .venv-streaming**

在 `talkybuddy/run_tests.sh` 把 `SPIKE_PY="spike/a2_pipecat/.venv/bin/python"` 改為 streaming venv，並讓主 venv 缺失時 SKIP 而非 exit。修改後 `run_tests.sh` 全文：
```bash
#!/usr/bin/env bash
# Run the full TalkyBuddy test suite across venvs.
#
# 主套件跑主 .venv；streaming/barge-in 測試（server/streaming/tests）需 pytest-asyncio
# ＋ pipecat 全家桶，跑獨立 .venv-streaming（A2 real-wiring，見 requirements-streaming.txt）。
#
# Usage: ./run_tests.sh
set -euo pipefail

cd "$(dirname "$0")"

MAIN_PY=".venv/bin/python"
STREAM_PY=".venv-streaming/bin/python"

echo "=== main suite (.venv) ==="
if [ -x "$MAIN_PY" ]; then
  # server/streaming/tests 的 conftest.py 在缺 pytest-asyncio 時自我 skip，
  # 主 venv 直接跑會 collect 其餘全部而不報錯。
  "$MAIN_PY" -m pytest -q --ignore=server/streaming/tests
else
  echo "SKIP: main venv not found at $MAIN_PY（本 worktree 未建主 .venv）" >&2
fi

echo
echo "=== streaming / barge-in suite (.venv-streaming) ==="
if [ -x "$STREAM_PY" ]; then
  PYTHONPATH="$(pwd)" "$STREAM_PY" -m pytest server/streaming/tests -q
else
  echo "SKIP: streaming venv not found at $STREAM_PY（見 requirements-streaming.txt）" >&2
  exit 1
fi
```

- [ ] **Step 2: 跑 streaming 全套（含本切片新測試）**

Run:
```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
./run_tests.sh
```
Expected：主 venv 段印 SKIP（本 worktree 無主 .venv）；streaming 段全 pass，包含 `test_sherpa_voice_locate.py`、`test_realwire_synth.py`、`test_run_realwire.py`、及既有 streaming 測試。若某既有測試依賴主 .venv-only 套件而失敗，記錄並在該 task 回報（勿為過測而放寬驗收）。

- [ ] **Step 3: Commit**

```bash
cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
git add run_tests.sh
git commit -m "build(a2): run_tests.sh streaming suite -> .venv-streaming"
```

---

## 手動驗收（真麥，需 sudo 安裝 portaudio）

自動化（Task 3）綠燈後，執行真麥手動驗收（spec §驗收/手動）。此步需系統 portaudio + pyaudio，**需 sudo 授權**，執行時請使用者於 session 內以 `! sudo apt install -y portaudio19-dev` 安裝：

1. 裝系統 portaudio 與 pyaudio：
   ```bash
   sudo apt install -y portaudio19-dev
   cd /home/budaedu/hackathon/.claude/worktrees/a2-realwire/talkybuddy
   .venv-streaming/bin/pip install pyaudio
   ```
2. 戴耳機（規避無 AEC 的自體回授），跑：
   ```bash
   PYTHONPATH=$(pwd) .venv-streaming/bin/python -m server.streaming.run_realwire
   ```
3. 判準（二元淨判）：
   - 講一句 → 聽到回覆（有/無）。
   - 回覆播到一半插話 → 回覆在句界停下、系統轉聽新輸入（乾淨停/沒停）。
4. 記錄測試條件（耳機/外放）與結果。若首次真麥接線出現 sample rate / frame size / 裝置權限偏差，於 `run_realwire.py` 容錯處補明確訊息（屬 spec 風險段預期）。

---

## Self-Review（撰寫後對照 spec）

**1. Spec coverage：**
- 架構（LocalAudioTransport + Silero VAD + FunASR + manager + TTS）→ Task 4 `run_realwire.py`、`build_processors`。✅
- 自動化驗收 `test_realwire_synth.py`（判準 A 有回覆、判準 B barge-in 淨停）→ Task 3。✅
- 手動驗收 `run_realwire.py`（講一句/半途插話）→ Task 4 + 手動驗收段。✅
- venv 收編（requirements-streaming.txt、.venv-streaming、run_tests.sh 改指向、模型 cache 唯讀沿用）→ Task 1、Task 5、Task 0。✅
- 錯誤處理（無音訊裝置/缺 pyaudio、空回覆降級、SenseVoice cache 缺失）→ Task 4 `check_prerequisites`/`main`、Task 3 判準 A 涵蓋降級路徑。✅
- 檔案落點（requirements-streaming.txt、run_realwire.py、test_realwire_synth.py、run_tests.sh 微調、可能微調 harness.py）→ 全覆蓋；harness.py 經確認**不需**改（Task 3 直接複用），已在 File Structure 說明。✅
- 風險段（LocalAudioTransport 首次接線偏差、無 AEC 自體回授、完整 pipeline 邊界妥協）→ Task 3 Step 1/註記、Task 4 docstring/手動驗收段、Task 3 妥協 fallback。✅

**2. Placeholder scan：** 無 TBD/TODO/「適當錯誤處理」等占位；每個 code step 均含完整程式碼與確切指令、預期輸出。✅

**3. Type consistency：**
- `harness.run_once(reply_source, *, barge_in, no_stt) -> (OutputSink, StreamingTurnManager)`：Task 3 一致使用。✅
- `OutputSink.frame_count`、`StreamingTurnManager.result.asr_text`/`.state_events`：與 harness.py/turn_manager.py 現有定義一致。✅
- `sherpa_voice._espeak_data_dir()`、`_ONNX`：Task 2 定義，Task 4 `check_prerequisites` 引用，一致。✅
- `run_realwire.check_prerequisites()->list[str]`、`build_processors(transport)->list`：Task 4 定義與測試一致。✅

**額外環境前置（超出 spec 明列，但為 worktree 現實所需，已納入計畫）：**
- Task 0 模型 symlink（worktree 因 gitignore 缺大檔）。
- Task 2 espeak venv-無關定位（`.venv-streaming` 路徑異於硬編 `.venv`）。
- requirements-streaming.txt 補 `piper-tts`（espeak 資料來源）；pyaudio 列為 run_realwire 專用選配（需 sudo portaudio）。
